---
document_id: GOV-002
title: AIX Full Compliance — Open Findings Register
version: 1.0
document_status: APPROVED
implementation_status: N/A
module: N/A
control: Open findings, blockers, deferrals tracking
owner: Unassigned
effective_date: UNKNOWN
last_reviewed: UNKNOWN
supersedes: none
baseline_commit: 780e116
---

# AIX Full Compliance — Open Findings Register

This is the single current register for unresolved findings, deferred controls, known
environment issues, and blockers across the AIX Full Compliance project. It supersedes
scattered findings text embedded in `00_project_state/PROJECT_HANDOVER.md` for
**current** status purposes — the historical narrative in `PROJECT_HANDOVER.md` is left
intact and remains the record of *when* and *how* each finding arose.

**State values:** `OPEN` · `DEFERRED` · `BLOCKED` · `ENVIRONMENT` · `FUTURE_CONSUMER` · `CLOSED`

A finding is marked `CLOSED` only when repository evidence (a commit, a remediation
turn, an independent re-review) proves closure. All statuses/severities are carried
forward unchanged from their source tracking documents except where a specific row's
own citation shows independently-verified closure evidence (commit + reproduced test
result) — see IAM1-FIND-006/IAM1-FIND-007 for the current example.

---

### CLT-01 — Membership Authority

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| CLT-FIND-001 | CLT-01 | Membership Authority | LOW / NON-BLOCKING | Bound `iam_user_id` is absent from authorised-user audit metadata. | No action required for current acceptance; consider adding to audit metadata in a future CLT-01 turn. | None set | CLT-01 Membership Authority independent Opus final acceptance | OPEN |
| CLT-FIND-002 | CLT-01 | Membership Authority | INFORMATIONAL / FUTURE_CONSUMER | Future consumers of the membership-resolution route must: derive `iam_user_id` from authenticated context (never from request input); enforce `userClass ∈ {client, client_approver}`. | Apply this constraint when the public `/wlt1/*` surface or any other consumer integrates against `GET /internal/clt1/principals/:iam_user_id/client-memberships`. | Trigger: any future consumer integration | CLT-01 Membership Authority independent Opus final acceptance | FUTURE_CONSUMER |
| CLT-FIND-003 | CLT-01 | Migration 067 | INFORMATIONAL | Migration 067 uses a non-concurrent index build (`CREATE UNIQUE INDEX`, not `CONCURRENTLY`). | No action required — acceptable for current migration-execution model. Note for any future high-availability migration policy. | None set | CLT-01 Membership Authority independent Opus final acceptance | OPEN |
| CLT-FIND-004 | CLT-01 | Test suite / migration head | LOW / NON-BLOCKING — **CLOSED at commit `e6cf4c7`** | `tests/integration/clt1-db.test.ts` (in the code tree, `aix-platform`) pinned the platform-wide global migration head to `067_clt1_authorised_user_iam_binding`, reproducing the same anti-pattern already fixed for WLT-01's own test. Would have broken when migration 068 (any module) lands. | Same fix pattern already applied to `wlt1-db.test.ts` (commit `0335340`): removed the global-head pin (`SELECT name FROM pgmigrations ORDER BY id DESC LIMIT 1`), replaced with a presence check scoped to CLT-01's own migration (`SELECT count(*) FROM pgmigrations WHERE name = '067_clt1_authorised_user_iam_binding'`) — proves 067 is present/applied without asserting it is the platform's final migration. Adversarially proven on a disposable probe database (hypothetical later migration row inserted, no migration file created): the old query would have failed, the actual suite re-run still passed 446/446; row then removed and the new test correctly failed. Test-only — no production/migration/grant/dependency change; full CLT suite 21/21 files, 739/739 tests; full platform 179/179 files, 4708/4708 tests. | Closed | CLT-01 Membership Authority independent Opus re-review (Finding 5) → test-only remediation `e6cf4c7` | CLOSED |

### FND-01 — Shared Rate-Limit Engine

Architecture ACCEPTED FOR IMPLEMENTATION; v1 numeric policy APPROVED FOR MIGRATION
069 (`DECISION_LOG.md` DEC-009). **Engine IMPLEMENTED and independently accepted
COMPLETE / ACCEPTED at commit `2cdeaa5`** (migrations `068_fnd_rate_limit_engine`,
`069_fnd_rate_limit_policy_seed`). **Post-acceptance hardening COMPLETE / VERIFIED
at commit `eb4a767`** (migration `070_fnd_rate_limit_policy_privilege_hardening`;
migration head `070`, 70 migrations). **Satisfies WLT-01 BLOCKER-2 in full** (see
WLT-FIND-004) — both WLT-01 public-surface prerequisites (BLOCKER-1 + BLOCKER-2)
are now COMPLETE. Full record: `02_modules/FND-01/acceptance/
FND-01_Rate_Limit_Hardening_Opus_v1.0.md`. **DEC-010 Turn 2 Prerequisites
(deployment ownership + numeric pre-auth policy) — independent Opus governance
review, no code — PARTIALLY RESOLVED.** Deployment/infrastructure ownership of
DEC-010 L1/L2 is now RESOLVED to `03_implementation/IMP-02/README.md`
(`IMP-02`, `IN_PROGRESS` — not an 18th module; see `IMP-02` for the ownership
adjudication). Production numeric pre-auth policy is NOT approved (outcome B —
capacity inputs unmeasured); an explicitly non-production INTERNAL-UAT
provisional policy and the M1–M8/A1–A10 calibration and abuse-test framework
are recorded in `IMP-02`. **New finding FND-FIND-010** (MEDIUM) tracks the
shared `@aix/foundation` connection pool's unconfigured `pool.max`/
`connectionTimeoutMillis`, surfaced by this review as a material blocker to
accurate production capacity calibration. **`IMP-02` Turn A (L1 UAT
trusted-edge HTTP reference implementation) is COMPLETE / ACCEPTED at
commit `65fca52`, Turn B (L2 mandatory network isolation, UAT proof) is
COMPLETE / ACCEPTED at commit `7132057`, and Turn C (UAT TLS termination,
functional) is now COMPLETE / ACCEPTED at commit `d568fa0`** (see the
`IMP-02` section below) — production TLS certificate lifecycle/cipher
policy and production deployment of L2 remain outstanding, and no turn
closes this finding.

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| FND-FIND-001 | FND-01 | Shared rate-limit engine — pre-authentication abuse | HIGH | Unauthenticated garbage-bearer-token requests can reach IAM-01 session introspection (one HTTP call + one IAM DB query per attempt, independently confirmed to include an `iam.auth_event` write on the negative branch, with no retention/eviction policy) before any identity-based WLT-01 rate-limit check can apply — no safe bounded-cardinality subject exists pre-authentication (a bearer-token hash or request fingerprint is attacker-chosen), and no per-IP control exists (`trustProxy` is configured nowhere in this platform; `request.ip` is the raw socket peer). A genuine amplification vector against IAM-01 once WLT-01 public routes are internet-exposed. **NOT closed by DEC-009's approved numeric policy, by the engine's implementation, or by its post-acceptance hardening** — that policy is authenticated-only by design; inventing a fake `AUTH_FAILURE` bucket or bolting on unsafe per-IP limiting was explicitly evaluated and rejected. **ARCHITECTURE NOW ACCEPTED (`DECISION_LOG.md` DEC-010, ACCEPTED FOR IMPLEMENTATION, not yet implemented):** a four-layer control — trusted edge (pre-auth source-IP throttling, exact public-path allowlist, header strip/inject), mandatory network isolation (the WLT listener must not be directly internet-reachable), a WLT-01-owned public-surface enablement gate + distinct perimeter-provenance credential (`x-aix-perimeter-token`, checked before any IAM call), and the existing unchanged authenticated chain. The load-bearing acceptance property is a zero-pre-authentication-database-write invariant. Implementation is staged: Turn 1 (WLT-01 enablement gate) is unblocked and COMPLETE/ACCEPTED at `af52fe8`. `IMP-02` Turn A (L1 UAT trusted-edge HTTP reference implementation) is **COMPLETE / ACCEPTED at `65fca52`** (`IMP-02-ACC-001`) — but Turn A is a UAT reference edge with a provisional, unapproved numeric policy and no TLS; it does NOT by itself close this finding. `IMP-02` Turn B (L2 mandatory network isolation — the layer that, combined with an eventually-approved production numeric policy, actually closes THIS finding) is **COMPLETE / ACCEPTED at `7132057`** (`IMP-02-ACC-002`) — but Turn B proves the L2 isolation property only inside a disposable, provider-neutral UAT harness (genuine Linux network namespaces, no deployed topology); it does NOT by itself close this finding, which requires the property proven at a real production deployment. `IMP-02` Turn C (UAT TLS termination, functional) is now **COMPLETE / ACCEPTED at `d568fa0`** (`IMP-02-ACC-003`) — but Turn C proves TLS is functional only inside the same disposable UAT harness; it selects no production certificate authority, cipher policy, or SNI/Host policy, and does NOT by itself close this finding either. `IMP-02` Measurement Harness Turn M-A (the trusted M1–M8 capacity-calibration measurement foundation — result schema, environment fingerprinting, evidence safety, an M2a runtime-observation capability, and pure `C_iam`/DB-budget arithmetic helpers) is now **COMPLETE / ACCEPTED at `d57b436`** (`IMP-02-ACC-004`) — but Turn M-A is FOUNDATION ONLY: no capacity calibration was performed, M1/M3/M4/M5/M6/M7/M8a remain NOT performed, M2 remains NOT complete (M2b — deployment IAM process count — has no evidence source in this repository), and production `C_iam` remains UNDETERMINED; it does NOT by itself close this finding either. Deployment/infrastructure implementation ownership is **RESOLVED**, owned exclusively by `IMP-02` (`03_implementation/IMP-02/README.md`, `IN_PROGRESS`; explicitly not an 18th module — the 17-module delivery taxonomy is unchanged). Numeric pre-auth policy remains **NOT APPROVED (outcome B unchanged)** — production thresholds are NOT approved because the load-bearing capacity inputs (negative-introspection DB service time, deployed IAM pool concurrency, deployment shape, NAT/CGNAT fairness population) remain unmeasured; the explicitly non-production INTERNAL-UAT-only provisional policy that Turn A implements, the capacity-calibration framework (M1–M8), and the abuse-test acceptance matrix (A1–A10) are all recorded in `IMP-02`, authorizing UAT-scoped engineering and testing only — never production configuration or internet exposure. | Resolve via `IMP-02`: prove L2 network isolation (abuse test A3) at a real production deployment, not only inside the accepted UAT harness (`IMP-02-ACC-002`); establish a production TLS certificate lifecycle, cipher policy, and SNI/Host policy, not only the accepted UAT-functional TLS harness (`IMP-02-ACC-003`); obtain the M1–M8 measurement evidence pack (including M7 TLS-handshake capacity, not yet performed) and OPS-05 maker-checker-approved production numeric policy per `IMP-02`'s production approval process; pass the full A1–A10 abuse-test matrix at production shape (including A3 and the A9 multi-node counter-integrity test); perform the M1/M2/M5 measurements `FND-FIND-010`'s closure unblocked (IAM's DB-pool capacity inputs are now explicitly governed — `FND-FIND-010` itself is CLOSED — but M2's deployed-process-count evidence and M5's actual saturation measurement have NOT been performed); then obtain independent acceptance. Mandatory precondition before any WLT-01 public route is internet-exposed. Turn A acceptance, Turn B's UAT-only L2 proof, Turn C's UAT-only TLS proof, FND-FIND-010's governance closure, Turn M-A's measurement foundation, owner designation, and a UAT-only provisional policy together do NOT close this finding. | Trigger: before any WLT-01 public route is internet-exposed | Shared Rate-Limit Engine architecture review (FINDING-2); reaffirmed by the numeric-policy governance review, the implementation acceptance, the post-acceptance hardening review, the WLT-01 Public Client Surface independent acceptance, the DEC-010 architecture acceptance, the DEC-010 Turn 2 Prerequisites governance review, the IMP-02 Turn A independent Opus acceptance (`IMP-02-ACC-001`), the IMP-02 Turn B independent Opus acceptance (`IMP-02-ACC-002`), the IMP-02 Turn C independent Opus acceptance (`IMP-02-ACC-003`), the FND-FIND-010 independent Opus acceptance review, and the IMP-02 Measurement Harness Turn M-A independent Opus acceptance (`IMP-02-ACC-004`) | OPEN |
| FND-FIND-002 | FND-01 | Shared rate-limit engine — grant-ordering / policy immutability | MEDIUM — **CLOSED at commit `eb4a767`** | `foundation.rate_limit_policy` runtime-immutability depended entirely on `infra/grants/fnd_runtime_grants.sql`'s own guarded `REVOKE` running AFTER migration 068 creates the table. Under the opposite deployment order (grants applied pre-068, migrations run later, grants never re-applied), Postgres's own `ALTER DEFAULT PRIVILEGES` left `role_fnd_runtime` able to INSERT/UPDATE the governance-owned policy table. Proven live against a real restricted LOGIN role during independent acceptance. | New migration `070_fnd_rate_limit_policy_privilege_hardening.cjs` performs the identical guarded REVOKE at MIGRATION TIME (guarded on role existence rather than table existence), making policy immutability deployment-order-independent. Independently re-proven under BOTH deployment orderings with a genuine restricted LOGIN role executing real INSERT/UPDATE/DELETE statements (SQLSTATE `42501` on all three); a counterfactual reproduction on the pre-070 migration set confirmed the vulnerability was real and that migration 070 alone closes it. New deployment-order regression test (`fnd-rate-limit-policy-privilege-ordering.test.ts`) validated as a genuine oracle by replaying its assertions against a no-070 database. `068`/`069` left byte-unchanged. | Closed | Shared Rate-Limit Engine independent Opus acceptance (NEW-1) → post-acceptance hardening `eb4a767` → independent narrow verification | CLOSED |
| FND-FIND-003 | FND-01 | Shared rate-limit engine — duplicate consumer-secret-value ambiguity | MEDIUM — **CLOSED at commit `eb4a767`** | Two different module ids configured in `FND_RATE_LIMIT_CONSUMER_SECRETS` with the same secret value booted successfully; `makeRateLimitConsumerGuard` derives module identity from which secret matched, so a shared secret silently collapsed the second module's traffic into the first module's namespace/policy. | `parseRateLimitConsumerSecrets` now rejects a duplicate secret value across module ids with `CONFIGURATION_INVALID` (naming the colliding module ids, never the secret value). Independently probed: shared-value pair rejected, distinct-value pair boots normally, no secret leaked in any error path, every pre-existing validation check (duplicate key, module-id shape, min length, `INTERNAL_SERVICE_TOKEN` collision) confirmed unweakened. | Closed | Shared Rate-Limit Engine independent Opus acceptance (NEW-2) → post-acceptance hardening `eb4a767` → independent narrow verification | CLOSED |
| FND-FIND-004 | FND-01 | Shared rate-limit engine — environment template completeness | MEDIUM — **CLOSED at commit `eb4a767`** | `FND_RATE_LIMIT_CONSUMER_SECRETS` is boot-required (startup aborts if unset) but was absent from the canonical `platform/.env.example` template. | `.env.example` now documents the variable in the Required/critical section with a safe placeholder, the required JSON shape, and the uniqueness/length constraints. Independently confirmed the template loads through the real config loader; all 8 FND production env vars now present (was 7/8). | Closed | Shared Rate-Limit Engine independent Opus acceptance (NEW-3) → post-acceptance hardening `eb4a767` → independent narrow verification | CLOSED |
| FND-FIND-005 | FND-01 | Shared rate-limit engine — escaped-JSON-key duplicate detection | LOW | The pre-parse duplicate-raw-key text scan can be bypassed by an escaped-key variant of an existing module id (e.g. a Unicode-escaped hyphen). `JSON.parse` itself still collapses both occurrences to exactly one surviving entry, so no ambiguous module identity ever results — the discarded credential simply becomes unusable and fails closed (401). Deliberately NOT fixed; independently reconfirmed unchanged and still fail-closed after the FND-FIND-003 remediation (orthogonal check). | No action required at this severity. Track for a future config-validation hardening pass if the pattern recurs elsewhere. | None set | Shared Rate-Limit Engine independent Opus acceptance (NEW-4); reconfirmed by post-acceptance hardening independent verification | OPEN |
| FND-FIND-006 | FND-01 | Shared rate-limit engine — mixed-window-rollover transition evidence | LOW/INFORMATIONAL | Independent simulation across 2160 synthetic requests (36 policy/cadence combinations) found 0 lost-evidence cases and 0 decision mismatches, but a small number (4) of "extra" transition-log rows in mixed-window-rollover edges (one window rolls while the other independently crosses). No denial evidence is ever lost; only limited extra evidence is possible. | No action required — evidence-loss direction is safe. | None set | Shared Rate-Limit Engine independent Opus acceptance (NEW-5) | OPEN |
| FND-FIND-007 | FND-01 | Shared rate-limit engine — test-quality observations | INFORMATIONAL | Two carried observations: (1) the committed multi-app-instance concurrency test shares one module-level connection-pool singleton rather than genuinely separate instances; (2) the original acceptance suite lacked a restricted-role policy-immutability test (the engine tests connect as superuser). | (1) not fixed — true multi-process concurrency was independently reproduced separately by the reviewer, outside the committed suite. (2) materially mitigated, not fully superseded — the post-acceptance hardening turn added `fnd-rate-limit-policy-privilege-ordering.test.ts`, which does exercise a genuine restricted LOGIN role end-to-end; the historical observation is preserved here rather than silently dropped. | None set | Shared Rate-Limit Engine independent Opus acceptance (NEW-6); partially mitigated by post-acceptance hardening `eb4a767` | OPEN |
| FND-FIND-008 | FND-01 | Shared rate-limit engine — schema-shape notes | INFORMATIONAL | `foundation.rate_limit_policy.policy_id` is UNIQUE + NOT NULL but not declared PRIMARY KEY. `rate_limit_counter.subject_hash varchar(128)` stores a 64-character server-derived SHA-256 hex digest with no DB-level hex-format CHECK constraint. No current attacker-controlled write path to either column. | No action required at this severity. | None set | Shared Rate-Limit Engine independent Opus acceptance (NEW-7) | OPEN |
| FND-FIND-009 | FND-01 | Shared rate-limit engine — migration 070 DOWN privilege widening | LOW | Migration `070_fnd_rate_limit_policy_privilege_hardening`'s DOWN issues an explicit `GRANT INSERT, UPDATE` back to `role_fnd_runtime`, which can widen policy privileges on a deliberate `migrate down` past 070, even against a correctly-restricted database — proven live. Classified non-blocking: requires a deliberate operator rollback action, affects no forward deployment path, leaves the database no worse than the pre-070 accepted baseline, and is fully recoverable by either re-`up`-ing 070 or re-applying the canonical grants file (both independently confirmed to restore the restriction). Does NOT reopen FND-FIND-002 (which concerns normal forward deployment-order independence, unaffected). | Recommended future action: change migration 070's DOWN to a no-op or an evidence-preserving refusal, matching this repository's established DOWN-migration convention. Not fixed in this turn — migration 070 left unmodified per explicit instruction. | None set | Shared Rate-Limit Engine post-acceptance hardening independent narrow verification (NEW-8, newly opened by this review) | OPEN |
| FND-FIND-010 | FND-01 | Shared `@aix/foundation` connection pool — unconfigured capacity/timeout | MEDIUM — **CLOSED at commit `6af0d25`** | `packages/foundation/src/db.ts`'s `initPool()` constructed `new Pool({ connectionString, application_name })` with no `max` (node-pg library default: 10) and no `connectionTimeoutMillis` (library default: unbounded pool-acquire wait) — a module-level singleton instantiated identically by every service (`fnd`, `iam`, `iam2`, `clt1`, `wlt1`, `aml1`, `kyc1`, `cfg1`, `sec1`). Consequence, surfaced during the DEC-010 Turn 2 Prerequisites review: (1) under saturation, pool exhaustion manifested as unbounded request latency rather than a fast, diagnosable failure; (2) the undetermined deployed concurrency ceiling (`pool.max` × process/instance count) is a direct input to the `IMP-02` capacity formula (`C_iam = pool.max × process count`) — leaving it unconfigured/unmeasured materially blocked accurate production numeric pre-auth policy calibration (M1/M2/M5). | **CLOSURE MEANS: IAM database-pool capacity inputs are now explicitly governed** — it does NOT mean capacity calibrated, production pool values approved, or M2/M5 complete. `initPool()` (`packages/foundation/src/db.ts`) gained a deliberately narrow optional `{ max?, connectionTimeoutMillis? }` seam (never an arbitrary `PoolConfig` pass-through); independently proven, via constructor key-sequence comparison against the pre-remediation shape, that every caller passing no options constructs the pool identically to before this change — all eight non-IAM services (`fnd`/`iam2`/`clt1`/`wlt1`/`aml1`/`kyc1`/`cfg1`/`sec1`) remain byte-for-byte unaffected. IAM-01 alone gained `IAM_DB_POOL_MAX`/`IAM_DB_CONNECTION_TIMEOUT_MS` (`services/iam/src/config.ts`): strictly-positive-integer only (no fractional/negative/zero/non-numeric value silently coerced), **required when `ENVIRONMENT=prod`** (startup aborts via the existing `CONFIGURATION_INVALID` path if either is missing/empty/invalid — independently reproduced live: the real IAM process fails before any listener binds), optional and genuinely absent (no substituted default) in every other environment. Independently verified against the REAL IAM service under a controlled TEST-ONLY configuration (`pool.max=1`, `connectionTimeoutMillis=800ms` — not a production value): saturating the sole pooled connection and issuing a second concurrent `POST /internal/auth/session/validate` returned `503 AUTH_SESSION_INTROSPECTION_UNAVAILABLE` in ~0.8s (bounded, not a hang, not a misleading 401, not a silent fallback to another pool), with clean recovery (`401 AUTH_SESSION_REQUIRED`) once the connection was released. The `iam_service_started` startup log now records the effective `dbPoolMax`/`dbConnectionTimeoutMs` for auditability — independently confirmed to never log `DATABASE_URL`, credentials, or tokens. **Measurement-planning notes preserved for M1/M2/M5, not resolved by this closure:** (a) `pool.max` is IAM's per-process DB-connection ceiling, NOT guaranteed dedicated introspection capacity — other IAM DB consumers (login/refresh/MFA/step-up/password-reset/bootstrap) compete for the same pool, so M1/M5 must isolate introspection traffic or explicitly account for competing consumers; (b) `connectionTimeoutMillis` bounds pool ACQUISITION only, never total request execution once a connection is held (independently demonstrated: a request already holding the pool's only connection ran ~22s despite an 800ms acquisition timeout) — the eventual production value must be chosen together with WLT's caller-side IAM HTTP timeout to avoid reintroducing abandoned-work accrual; (c) the shared pool's `application_name` remains `"aix-fnd"` for all nine services, so `pg_stat_activity` cannot attribute connections to IAM by name alone — M2/M5 must use in-process pool counters (`pool.totalCount`/`idleCount`/`waitingCount`) and/or deployment evidence instead. No migration, no grant, no schema change; migration head unchanged at `070`, all 9 grants byte-unchanged; `platform/edge/**` untouched, no IMP-02 re-acceptance implied. | Trigger: required before `IMP-02` M1/M2/M5 measurements can be considered complete, and before production numeric pre-auth policy approval — **capacity GOVERNANCE is now in place; measurement itself remains a separate, later M1–M8 activity, not performed by this closure** | DEC-010 Turn 2 Prerequisites independent Opus governance review (deployment ownership + numeric pre-auth policy), newly surfaced by that review → architecture decision (Opus, mechanism/ownership selection, no code) → implementation `6af0d25` (`fix(iam): govern database pool capacity inputs`) → independent Opus acceptance review (cold-environment reproduction of the constructor-shape proof, prod fail-fast, strict-parsing matrix, and the real IAM introspection saturation test) | CLOSED |
| FND-FIND-011 | FND-01 | IAM DB-pool governance (FND-FIND-010) — independent-review test-quality observations | INFORMATIONAL | Three carried observations from the FND-FIND-010 independent Opus acceptance review, none a behavioural defect: (1) IAM's non-production startup-log branch (`dbPoolMax ?? "unset (node-postgres library default)"`) is unreachable at runtime — `services/iam/src/server.ts`'s pre-existing, unmodified `logger.level` is `"info"` only when `environment === "prod"` (else `"warn"`), and `iam_service_started` logs at `"info"`, so the fallback string can never actually print outside prod; independently confirmed a `dev` boot that reached LISTEN produced no startup log at all, while a `prod` boot logged the full event — no correctness/security/auditability consequence, since the auditability goal is fully met where it matters (prod). (2) `tests/unit/foundation-db-pool.test.ts`'s absent-options assertions (`pool.options.max === 10`, `connectionTimeoutMillis` `toBeUndefined()`) do not by themselves distinguish an omitted constructor key from an explicit `{ max: undefined }` — the underlying property is nonetheless true, independently proven via constructor key-sequence comparison against a pre-remediation control construction (see the FND-FIND-010 closure evidence above). (3) One `it.each` row in `tests/unit/iam-config.test.ts`'s strict-parsing table (the whitespace-only case) returns before asserting, so it reports as a passing test while asserting nothing — mitigated: whitespace-only rejection is genuinely covered by an adjacent dedicated test and was independently reproduced (`ENVIRONMENT=prod` + whitespace-only → `CONFIGURATION_INVALID`). | No action required at this severity. Consider strengthening the constructor-shape assertion (e.g. `"max" in arg` on a captured constructor argument rather than the resolved `.options`) and removing the vacuous `it.each` row in a future IAM/foundation test-hardening pass. Not fixed — informational only, does not reopen FND-FIND-010. | None set | FND-FIND-010 independent Opus acceptance review (REV-001, REV-002, REV-003) | OPEN |

### IAM-01 — Internal Session Introspection

`POST /internal/auth/session/validate` — COMPLETE / ACCEPTED (implementation `5794ffe`,
test-harness remediation `5a29559`). Satisfies WLT-01 BLOCKER-1 (see WLT-FIND-004). Full
detail: `02_modules/IAM-01/acceptance/IAM-01_Session_Introspection_Opus_v1.0.md`.

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| IAM1-FIND-001 | IAM-01 | Session introspection — userClass semantics | INFORMATIONAL / carried | `user_class` returned by the seam is `iam.session`'s own snapshot column at session creation, not a live read of `iam.user_identity.user_class` — matches pre-existing `requireUserSession` semantics exactly (not new/introduced behaviour). | None required now. Any future user-class mutation path must revoke affected sessions or resolve `userClass` live — a separate IAM-01 task. | Trigger: a future user-class mutation path is introduced | IAM-01 Session Introspection independent Opus acceptance (FINDING-A) | OPEN |
| IAM1-FIND-002 | IAM-01 | Session introspection — last_seen write volume | LOW / carried | Every successful introspection call advances the canonical `iam.session.last_seen_at_utc` touch (existing `validateAccessToken` behaviour, unmodified). Once WLT-01 public traffic exists this raises write volume, though semantics are unchanged and idle-timeout is not enforced today. | No action required now. Raises priority of the already-documented deferred S3 throttle (`services/iam/src/lib/session.ts`). | None set | IAM-01 Session Introspection independent Opus acceptance (FINDING-B) | DEFERRED |
| IAM1-FIND-003 | IAM-01 | Session introspection — caller authority | MEDIUM / carried | The seam is guarded by a dedicated `IAM_INTROSPECTION_SERVICE_TOKEN` shared secret — capability scoping, not true per-caller cryptographic service identity. IAM-01 has no per-caller service-identity model for any `/internal/auth/*` route. | Accepted interim architecture, consistent platform-wide. Should be an early adopter of real per-caller service identity when that model lands. | Trigger: platform-wide service-identity model work begins | IAM-01 Session Introspection independent Opus acceptance (FINDING-C) | OPEN |
| IAM1-FIND-004 | IAM-01 | Session introspection — negative-code exhaustiveness | INFORMATIONAL | `SESSION_VALIDATION_NEGATIVE_CODES` has no compile-time exhaustiveness binding to every code `validateAccessToken` can throw; a future untracked code would fall through to the safe (503 unavailable) direction rather than a false 401/200. | None required — failure direction is already safe. | None set | IAM-01 Session Introspection independent Opus acceptance (NEW-3) | OPEN |
| IAM1-FIND-005 | IAM-01 | Session introspection — denial-audit write failure | INFORMATIONAL | If the `iam.session_introspection_denied` write itself fails, the route surfaces a canonical 500 `INTERNAL_ERROR` rather than the seam's own 503 — independently reproduced by probe. Still fail-closed (never 200/401). | None required. WLT's future IAM client must treat any non-200/non-401 response as unavailable, per the accepted architecture. | None set | IAM-01 Session Introspection independent Opus acceptance (NEW-4) | OPEN |
| IAM1-FIND-006 | IAM-01 | Session introspection — test-harness fail-loud canary | MEDIUM — **CLOSED at commit `5a29559`** | The new introspection acceptance suite lacked the platform's H-D3C-1 fail-loud DB-readiness canary; against an unmigrated database it reported 48/48 passing (every DB-gated test silently early-returned). Zero production impact. | Applied the established H-D3C-1 canary pattern (mirrors `iam-db.test.ts`/`iam2-db.test.ts`/`sec1-db.test.ts`/`cfg1-db.test.ts`/`aml1-db.test.ts`/`wlt1-db.test.ts`). Empirically re-verified: an unmigrated DB now makes the suite fail loudly (vitest exit code 1). | Closed | IAM-01 Session Introspection independent Opus acceptance (NEW-1) → test-harness remediation `5a29559` | CLOSED |
| IAM1-FIND-007 | IAM-01 | Session introspection — dead test assertion | LOW — **CLOSED at commit `5a29559`** | A no-op loop (`for (const c of cases) { void c; }`) asserted nothing under a comment claiming the 403 `AUTH_ACCOUNT_FROZEN` oracle never surfaces from the seam. | Removed without a replacement cosmetic assertion; the adjacent real test already proves the property end-to-end. | Closed | IAM-01 Session Introspection independent Opus acceptance (NEW-2) → test-harness remediation `5a29559` | CLOSED |

### IAM-02

Approval-control observations surfaced by UI Phases 2L/2R were independently adjudicated at commit `49498f9`
(`02_modules/IAM-02/acceptance/IAM-02_Approval_Control_Adjudication_Opus_v1.0.md`): three findings registered below;
narrow SoD coverage, terminal SoD blocking, unreadable decision/SoD evidence and unvalidated action strings on request
creation were adjudicated as approved deferrals or expected design and are **not** findings — see that record for the
reasoning. Carry-forwards **L1/L2/L3** from `IAM-02_Security_Review_Opus_v0.1.md` remain open and are tracked in that
review, not here.

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| IAM2-FIND-001 | IAM-02 | RBAC / permission consumption | Requires triage | IAM-02 double-consume issue requires dedicated triage. | Triage and remediate in a separate, dedicated IAM-02-scoped turn. Explicitly not addressed in this or any adjacent turn. | None set | `IAM-02_Security_Review_Opus_v0.1.md` / `v0.2_reverify.md` | OPEN |
| IAM2-FIND-002 | IAM-02 | Approval authorization (create / approve / reject) | HIGH | The three governed approval permissions `iam2.approval.create`, `iam2.approval.approve` and `iam2.approval.reject` are seeded `active` in migration `006` but are **never evaluated by any code in the repository**. `routes/approvals.ts` calls no permission guard in any of its three handlers, so no entitlement is required to create, approve or reject an approval request; the `requires_step_up = true` flag on the `sensitive` permission `iam2.approval.approve` is therefore never reached (`lib/guard.ts` step 6 would return the blocking decision `step_up_required`); and `approval_policy.required_approver_roles` is read by no code path. Maker != approver and the maker-vs-approver SoD check **are** enforced on approve, so "a different user with no seeded conflict" is the entire approver test. IAM-02's own `routes/roles.ts` does call `evaluatePermission`, so this is an omission, not an architectural limit. Six modules (CLT-01, WLT-01, KYC-01, AML-01, SEC-01, CFG-01) rely on this flow as the sole authorization gate for regulated actions. Currently behind the internal-service-token guard, not internet-exposed, and no `role_permission` rows are seeded anywhere (every effective permission set is empty), so enforcement cannot be switched on without provisioning role grants — a pre-operational RBAC state, not a compensating control. | Evaluate the acting user's entitlement in all three handlers via `evaluatePermission`, consuming its decision (including `step_up_required`). Decide and record whether `required_approver_roles` becomes enforced or is removed as dead metadata, and whether `reject` requires maker separation / SoD or is deliberately asymmetric. Requires `role_permission` provisioning. **Closure:** all three handlers gated, regression tests prove refusal without the permission on each of create/approve/reject and that `requires_step_up` is honoured, the `required_approver_roles` decision recorded, independent re-review. Does **not** supersede carry-forward L3 (caller-asserted identity). | Trigger: before any browser-originated or real-actor approval integration, before UAT evidencing maker-checker with real actors, and before production exposure. Does **not** block UI Phase 2S (read-only `B`-classified prototype). | `IAM-02_Approval_Control_Adjudication_Opus_v1.0.md` (IAM-02-ACC-003); observations surfaced by UI Phases 2L/2R | OPEN |
| IAM2-FIND-003 | IAM-02 | Approval policy configuration | MEDIUM | No `INSERT INTO iam2.approval_policy` exists anywhere (migrations, seeds or service code) and the runtime role holds `SELECT` only, so no policy row exists and no route can create one. `lookupPolicy` returns `undefined` and `routes/approvals.ts` silently applies the weakest setting — one approval, no step-up, 24-hour expiry — with no warning, audit event or fail-closed branch, making an absent policy indistinguishable from a configured single-approval policy. Blueprint §4 lists sixteen action classes requiring maker-checker and §6 ten requiring step-up, including payout destination approval; WLT-01's live `wlt1.destination.approve_apply` carries `requires_step_up = false` on its own seeded permission, so step-up for that workflow is enforced neither at the consumer permission, nor by policy, nor via `iam2.approval.approve` (see IAM2-FIND-002). n-of-m is implemented correctly but nothing can configure it above one. | Seed and govern approval policies for the implemented approval-gated actions with `required_approval_count` / `requires_step_up` derived from blueprint §4/§6, and make a missing policy fail closed (or fall back explicitly and auditably) for any permission marked `sensitive`, `financial_critical` or `requires_step_up`. Policy content is a compliance decision. **Closure:** policy rows exist for every integrated module's approval-gated actions; a test proves a step-up/multi-approval action cannot be approved under the bare default; unmatched-action fallback decided, implemented and tested; independent re-review. | Trigger: before production exposure; conditionally before any UAT intended to evidence n-of-m or step-up. Does **not** block UI Phase 2S. Remediate with or after IAM2-FIND-002. | `IAM-02_Approval_Control_Adjudication_Opus_v1.0.md` (IAM-02-ACC-003); observations surfaced by UI Phases 2L/2R | OPEN |
| IAM2-FIND-004 | IAM-02 | Approval audit completeness | LOW | `08_Audit_Log_Events.md` requires `iam2.approval_expired`. The approve handler's expiry branch (`routes/approvals.ts:247-258`) persists `status = 'expired'` and publishes the event; the reject handler's expiry branch (`:450-453`) persists the identical transition and publishes nothing. Expiry is written lazily (no sweeper), so whether a governed audit event exists for a real state transition depends on which route observed it first. Evidence completeness only — the state change is durably persisted either way and no authorization decision depends on it. | Publish `iam2.approval_expired` in the reject handler's expiry branch, inside the same transaction, matching the approve handler's payload shape. **Closure:** emitted on both paths with a regression test asserting it for the reject path. | None set — remediate opportunistically; does not block UI Phase 2S, integration, UAT or production. | `IAM-02_Approval_Control_Adjudication_Opus_v1.0.md` (IAM-02-ACC-003); observations surfaced by UI Phases 2L/2R | OPEN |

### IMP-02 — UAT Trusted Edge

`IMP-02` is the `03_implementation/` (not `02_modules/`) controlled owner of
`DECISION_LOG.md` DEC-010's L1 (trusted edge) and L2 (network isolation) —
see `03_implementation/IMP-02/README.md`. **Turn A (L1 UAT trusted-edge HTTP
reference implementation) is COMPLETE / ACCEPTED at commit `65fca52`** — full
record `03_implementation/IMP-02/acceptance/
IMP-02_UAT_Trusted_Edge_Turn_A_Opus_Acceptance_v1.0.md` (`IMP-02-ACC-001`).
**Turn B (L2 mandatory network isolation, UAT proof) is COMPLETE /
ACCEPTED at commit `7132057`** — full record `03_implementation/IMP-02/
acceptance/IMP-02_UAT_L2_Network_Isolation_Turn_B_Opus_Acceptance_v1.0.md`
(`IMP-02-ACC-002`). Turn B independently proves, inside a disposable
provider-neutral Lima UAT harness, that a direct probe to the real WLT-01
backend fails at the network layer (`ENETUNREACH`) while the same client
through the real Turn-A edge reaches real WLT-01 (`401
WLT1_AUTH_REQUIRED`), and that the isolated topology's L3 gate still rejects
an invalid edge provenance token (`404`). **Turn C (UAT TLS termination,
functional) is now COMPLETE / ACCEPTED at commit `d568fa0`** — full record
`03_implementation/IMP-02/acceptance/
IMP-02_UAT_TLS_Termination_Turn_C_Opus_Acceptance_v1.0.md`
(`IMP-02-ACC-003`). Turn C terminates TLS functionally at the accepted L1
edge inside the same disposable UAT harness, via a narrow guarded
modification to the accepted `haproxy.base.cfg` independently proven inert
when TLS is unset (Turn A's HTTP mode reproduces 47/47, 6/6, 23/23
unchanged); all six governed routes, request-header stripping, the
rate-limit ladder, the connection ceiling, and Turn-B's own L2/A3/positive/
A4 property are all independently confirmed to hold under TLS. **Production
TLS certificate lifecycle, cipher policy, SNI/Host policy, and backend/
service-to-service TLS or mTLS all REMAIN PENDING IMP-02 work** — Turn C
proves TLS is functional only inside a disposable UAT harness. **Neither
Turn A, Turn B, nor Turn C acceptance closes `FND-FIND-001` or approves
internet exposure** — see that finding under FND-01, below. **Measurement
Harness Turn M-A (the trusted M1-M8 capacity-calibration foundation — result
schema, environment fingerprinting, evidence safety, run identity, an M2a
runtime-observation capability, and pure `C_iam`/DB-budget arithmetic
helpers) is now COMPLETE / ACCEPTED at commit `d57b436`** — full record
`03_implementation/IMP-02/acceptance/
IMP-02_Measurement_Harness_Turn_M-A_Opus_Acceptance_v1.0.md`
(`IMP-02-ACC-004`). **Turn M-A is FOUNDATION ONLY — no capacity calibration
was performed, M1/M3/M4/M5/M6/M7/M8a were NOT performed, M2 is NOT complete
(M2a observation capability was built; M2b — deployment IAM process
count — remains BLOCKED, with no evidence source anywhere in this
repository), and production `C_iam` remains UNDETERMINED.** Zero
`platform/edge/**`/`services/**`/`packages/**` changes — Turns A/B/C are
otherwise unaffected. Four LOW, non-blocking findings were registered
during independent review of Turn A (`IMP-02-FIND-001` through
`IMP-02-FIND-004`); three more during Turn B (`IMP-02-FIND-005` through
`IMP-02-FIND-007`); two more during Turn C (`IMP-02-FIND-008`,
`IMP-02-FIND-009`); three LOW plus one INFORMATIONAL during Turn M-A
(`IMP-02-FIND-010` through `IMP-02-FIND-013`) — none blocked any
acceptance. **`IMP-02-FIND-010` and `IMP-02-FIND-011` are a mandatory gate
on Turn M-B: they MUST be closed before Turn M-B introduces any
externally-influenced path segment or untyped/CLI/JSON entrypoint.** **Both were closed by `IMP02-MA-HARDEN-001` at commit `9b0bab7` (`IMP-02-FIND-010`, `IMP-02-FIND-011`; see their rows below): the gate is satisfied. Turn M-B has not started.**

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| IMP-02-FIND-001 | IMP-02 | Tier-3 fail-loud edge canary | LOW | The live-edge test suite's fail-loud canary (intended to prove `TEST_EDGE_BASE_URL` points at the real HAProxy edge, not directly at WLT-01) was independently reproduced passing against a WLT-01-impersonating target with no edge in the request path — it does not reliably detect that condition. Mitigated: 9 of the other 22 Tier-3 assertions independently fail loudly against the same non-edge target, so the suite as a whole is not vacuous. | Strengthen the canary to assert a property only the genuine edge can produce (e.g. a value observable only via the edge's own stick-table/admin surface) in a future IMP-02 hardening turn. Not fixed — explicitly out of scope for Turn-A E1/E2 remediation. | None set | IMP-02 UAT Trusted Edge independent Opus acceptance (E3, carried from first review of `3d4c00f`) | OPEN |
| IMP-02-FIND-002 | IMP-02 | Edge-generated response consistency | LOW | Locally-generated `404`/`429` denials do not carry the same `Cache-Control`/`X-Content-Type-Options` headers as proxied `200` responses (`http-response` rules do not apply to a locally-generated denial), and `404` uses HAProxy's default `text/html` body/content-type rather than the platform's JSON error envelope shape. No topology leak — no HAProxy version, backend address, table identifier, or source-bucket key is ever disclosed. | Add explicit `http-response` header rules and a JSON error body to the edge-local `404`/`429` deny actions in a future IMP-02 hardening turn. Not fixed — out of scope for Turn-A E1/E2 remediation. | None set | IMP-02 UAT Trusted Edge independent Opus acceptance (E4, carried from first review) | OPEN |
| IMP-02-FIND-003 | IMP-02 | Base-config timeout-literal separation | LOW | `platform/edge/haproxy.base.cfg` contains literal `timeout connect 5s` / `timeout server 10s` in its `defaults` section, despite the file's own header comment claiming it contains no body/timeout value (those are meant to live only in `uat/haproxy.limits.cfg`, labelled PROVISIONAL). No behavioural impact today — `b_gate` in `uat/haproxy.limits.cfg` sets the same values explicitly, so nothing currently depends on the `defaults`-section literals — but a hypothetical future production limits file that omits its own values could silently inherit these UAT-shaped defaults rather than failing closed. | Remove the literal `defaults`-section timeout values (or env-guard them the same way client-facing timeouts already are) in a future IMP-02 hardening turn. Not fixed — out of scope for Turn-A E1/E2 remediation. | None set | IMP-02 UAT Trusted Edge independent Opus acceptance (E5, carried from first review) | OPEN |
| IMP-02-FIND-004 | IMP-02 | Tier-1 comment-satisfiable assertions | LOW | The majority (27 of ~30) of `platform/tests/unit/imp02-edge-config.test.ts`'s pre-existing Tier-1 positive assertions check raw (non-comment-stripped) config text, and are therefore theoretically satisfiable by an explanatory comment rather than a real directive. The four new assertions added at `65fca52` for the E1 fix are correctly comment-safe (independently verified as a genuine oracle — fails against the pre-fix config, passes against the post-fix config). | Migrate the remaining raw-text positive assertions to the existing comment-stripped `baseCfgCode`/`limitsCfgCode` helpers in a future IMP-02 hardening turn. Not fixed — out of scope for Turn-A E1/E2 remediation. | None set | IMP-02 UAT Trusted Edge independent Opus acceptance (E6, carried from first review) | OPEN |
| IMP-02-FIND-005 | IMP-02 | Turn-B evidence capture — stale response body on failed probe | LOW | `platform/edge/uat-topology/run-a3.sh` captures response bodies via `curl -o <file>`, which does not truncate the target file when the connection itself fails. Independently reproduced: a deliberately broken edge path produced a positive-control evidence file reading `status: 000` alongside a `WLT1_AUTH_REQUIRED` body retained, byte-for-byte, from the immediately preceding successful run (confirmed via unchanged file mtime). Cannot produce a false PASS — the pass/fail gate reads only the freshly-captured HTTP status code (`curl -w '%{http_code}'`), never the body file. Evidence-integrity/diagnostics defect only. | Truncate/remove each response-body output file immediately before its probe (e.g. `rm -f` or `curl --create-dirs -o` semantics that guarantee truncation) so a failed probe cannot retain prior content, in a future IMP-02 hardening turn. Not fixed — out of scope for this turn. | None set | IMP-02 UAT L2 Network Isolation independent Opus acceptance (Turn B, newly opened by this review) | OPEN |
| IMP-02-FIND-006 | IMP-02 | Turn-B guest/tooling version pinning | LOW | `platform/edge/uat-topology/lima.yaml` references a floating Ubuntu 24.04 release image with no `digest:` field (Lima independently confirmed to perform no image-integrity verification on this path), and `topology.sh` accepts any `limactl` on PATH without asserting it reports version 2.2.0 (documented in `README.md` prose only, not enforced). Bit-exact UAT reproducibility gap only — does not undermine the accepted network-isolation property, which is a stable Linux kernel guarantee independent of the exact 24.04 point release or Lima patch version. | Pin/verify the Lima version at harness startup and pin the guest image identity/digest where practical, before treating this topology as a stronger bit-exact reproducible reference, in a future IMP-02 hardening turn. Not fixed — out of scope for this turn. | None set | IMP-02 UAT L2 Network Isolation independent Opus acceptance (Turn B, newly opened by this review) | OPEN |
| IMP-02-FIND-007 | IMP-02 | Turn-B test-architecture and assertion-strength observations | LOW | Three carried observations from independent review: (1) `platform/tests/integration/imp02-uat-topology-live.test.ts` invokes `run-a3.sh` via `child_process`, which itself restarts the HAProxy edge process for A4 — a minor deviation from the convention Turn A established (`edge:validate`/Tier 2 deliberately kept outside Vitest) of never managing service lifecycle from a test file; (2) the static no-TLS-claim assertion in `imp02-uat-topology-config.test.ts` (`/TLS (is )?implemented\|HTTPS production.ready/i`) is narrow and would not catch every possible TLS-implemented wording; (3) namespace-membership static assertions partly rely on the `v-pub-*`/`v-bck-*` veth naming convention rather than pure topology structure. No demonstrated correctness failure in any case — test architecture/maintainability/assertion-robustness only. | Consider an `edge:topology:verify`-style external entry point for the live test's lifecycle needs consistent with Tier 2's convention; broaden the TLS-claim regex; strengthen namespace-membership assertions to be naming-independent, in a future IMP-02 hardening turn. Not fixed — out of scope for this turn. | None set | IMP-02 UAT L2 Network Isolation independent Opus acceptance (Turn B, newly opened by this review) | OPEN |
| IMP-02-FIND-008 | IMP-02 | Turn-C live-test evidence freshness | LOW | `platform/tests/integration/imp02-uat-tls-live.test.ts` consumes the lexicographically-latest `evidence/` directory with no freshness or run-identity check. Independently reproduced: evidence from a prior successful `tls-verify.sh` run, backdated to 2026-01-01 with the harness never re-run, still reported 22/22 passed. The fail-loud canary correctly catches *absent* evidence and assertions correctly catch *wrong* evidence, but not *stale* evidence — a deliberate architectural trade-off (avoiding `child_process`-driven service lifecycle inside Vitest, the `IMP-02-FIND-007` lesson) whose cost is a freshness gap the live suite alone cannot close. Mitigated in practice: `evidence/` is git-ignored, so a clean checkout has none and the canary fires. | Add a deterministic freshness or run-identity check (e.g. a timestamp/nonce cross-referenced against the invoking shell, or a lock file cleared only by a genuine `tls-verify.sh` run) before Tier-3 alone is treated as proof of the current run, in a future IMP-02 hardening turn. Not fixed — out of scope for this turn. | None set | IMP-02 UAT TLS Termination independent Opus acceptance (Turn C, newly opened by this review) | OPEN |
| IMP-02-FIND-009 | IMP-02 | Turn-C certificate-negative harness assertion strength | LOW | `platform/edge/uat-tls/tls-verify.sh` scores T2/T3/T4 (untrusted CA / wrong hostname / expired certificate) solely on transport-layer `000`, without asserting the specific intended failure cause. Any TLS-layer failure — including one caused by an unintended harness defect — would satisfy the current assertion. Independent review separately verified each test's actual failure reason (issuer error for T2, `subjectAltName does not match` for T3, `certificate expired` TLS alert for T4) and confirmed all three fail for their intended cause in the harness's actual sequence; risk is materially mitigated because T1 immediately precedes and requires a genuine `401` through a working edge before each negative test's own explicit restart. | Strengthen `tls-verify.sh`'s T2/T3/T4 scoring to assert the OpenSSL-reported verification-failure reason, not merely the transport-layer status, in a future IMP-02 hardening turn. Not fixed — out of scope for this turn. | None set | IMP-02 UAT TLS Termination independent Opus acceptance (Turn C, newly opened by this review) | OPEN |
| IMP-02-FIND-010 | IMP-02 | Turn-M-A evidence writer — symlink escape from `perf/evidence/` | LOW — **CLOSED at commit `9b0bab7`** | `platform/perf/src/evidence-store.ts`'s path confinement uses string normalization (`path.resolve`/`path.relative`) with no `realpath` check. Independently proven concretely: a symlink placed inside `perf/evidence/` is followed, landing a written file outside the evidence root (`/tmp/.../pwned.json`). Non-blocking today because Turn M-A has no untrusted/externally-influenced path entrypoint — no CLI, no JSON ingestion, no network-facing path — and the evidence directory is git-ignored, so no symlink can arrive via checkout. | Applied a `realpathSync` containment check on the resolved target directory, after `mkdirSync` and before the final write, in `platform/perf/src/evidence-store.ts`; a regression test proves that an in-root symlink resolving outside the real evidence root is rejected with `EvidencePathError` and that nothing is written outside the root. Re-verified on `main` after the fast-forward: targeted tests, perf regression tests and typecheck PASS. Accepted residual (`REVIEW_CONCERN-001`): the directory is created before the check, so a symlink escape may create an empty directory outside the root before the write is refused; no evidence file is written outside the root. | Closed | IMP-02 Measurement Harness Turn M-A independent Opus acceptance (`IMP-02-ACC-004`, newly opened by this review) → hardening `9b0bab7` (task IMP02-MA-HARDEN-001; independent GPT review REV-20260920T063148Z-001: ACCEPT; human acceptance record `docs/03_implementation/tasks/IMP02-MA-HARDEN-001/06-acceptance.md`) | CLOSED |
| IMP-02-FIND-011 | IMP-02 | Turn-M-A result schema — `status`/`measurement_id` not runtime-validated | LOW — **CLOSED at commit `9b0bab7`** | `platform/perf/src/schema.ts`'s `createMeasurementResult` validates `status`/`measurement_id` only via TypeScript's type system, not at runtime. Independently proven: a type-bypassing (untyped) caller can construct a result carrying `status: "APPROVED"` (which visually reads as a pass, with no threshold check at all, since the guard special-cases only the literal strings `"PASS"`/`"FAIL"`) or `measurement_id: "M2"`/`"M8b"`. Non-blocking today because every in-repo producer is `strict`-TypeScript, typechecked by `tsc -b`; the attack required deliberately bypassing the compiler. | Added runtime allowlist validation inside `createMeasurementResult` (`platform/perf/src/schema.ts`): `status` against the five controlled states and `measurement_id` against `ALL_MEASUREMENT_IDS`, so a type-bypassing caller passing `APPROVED`, `M2` or `M8b` is rejected with `MeasurementResultValidationError`; regression tests cover the invalid values and every controlled identifier and status. Re-verified on `main` after the fast-forward: targeted tests, perf regression tests and typecheck PASS. | Closed | IMP-02 Measurement Harness Turn M-A independent Opus acceptance (`IMP-02-ACC-004`, newly opened by this review) → hardening `9b0bab7` (task IMP02-MA-HARDEN-001; independent GPT review REV-20260920T063148Z-001: ACCEPT; human acceptance record `docs/03_implementation/tasks/IMP02-MA-HARDEN-001/06-acceptance.md`) | CLOSED |
| IMP-02-FIND-012 | IMP-02 | Turn-M-A result schema — whitespace-only `threshold_ref` on non-PASS/FAIL results | LOW | `validateStatusThresholdPairing`'s `hasThreshold` check trims before testing, but the object-spread that copies `threshold_ref` onto the built result tests only `!== undefined` — so `OBSERVED`/`INVALID` + `threshold_ref: "   "` or `""` is accepted and copied onto the result. Not a bypass of the load-bearing PASS/FAIL control (independently re-verified airtight against ~24 direct attack payloads) — schema cleanliness only, since the result still correctly reads `OBSERVED`/`INVALID` and conveys no false authority. | Normalize/reject empty or whitespace-only `threshold_ref` consistently regardless of status, in a future IMP-02 hardening turn. Not fixed — out of scope for Turn M-A. | None set | IMP-02 Measurement Harness Turn M-A independent Opus acceptance (`IMP-02-ACC-004`, newly opened by this review) | OPEN |
| IMP-02-FIND-013 | IMP-02 | Turn-M-A bundled test/scanner refinement observations | INFORMATIONAL | Five carried observations from independent review: (1) the secret scanner does not match lowercase PEM markers, `PGP PRIVATE KEY BLOCK`, or lowercase `bearer` (HTTP auth schemes are case-insensitive per RFC 7235); (2) `buildEnvironmentManifest` accepts an empty `commit_sha` from an injected test provider — contained at the result-creation layer (`createMeasurementResult` rejects it), and the real `readRealGitInfo` production path throws on an empty SHA; (3) the Vitest-collection-boundary test is near-tautological (asserts only that the string `"perf"` does not start with `"tests/"`); (4) the `K_max`-field and vitest-glob static tests use source-text regexes where a runtime check is feasible; (5) the rejected-write evidence-store test asserts file-absence rather than the stronger directory-non-creation property the implementation actually provides. No demonstrated correctness failure in any case. | Consider strengthening each item in a future IMP-02 hardening turn; none is required for Turn M-A acceptance. Not fixed — out of scope for this turn. | None set | IMP-02 Measurement Harness Turn M-A independent Opus acceptance (`IMP-02-ACC-004`, newly opened by this review) | OPEN |

### WDR-01

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| WDR-FIND-001 | WDR-01 | Implementation prerequisite | BLOCKED | WDR-01 implementation is blocked because KMS (key management service) is UNSATISFIED as a platform prerequisite. | Resolve KMS provisioning/integration before WDR-01 implementation can begin. | Trigger: KMS availability | `MODULE_STATUS.md` | BLOCKED |

### WLT-01

**Client-facing public `/wlt1/*` surface — both prerequisites (BLOCKER-1 + BLOCKER-2)
were COMPLETE; implementation done and independently accepted COMPLETE / ACCEPTED at
commit `7f9fc8a`** (implementation `12cedda`, remediation `7f9fc8a`; see WLT-FIND-004
and the full record at `02_modules/WLT-01/acceptance/
WLT-01_Public_Client_Surface_Opus_Acceptance_v1.0.md`). **This accepts the public
surface only — WLT-01 as a whole remains PARTIAL** (see `00_project_state/
MODULE_STATUS.md`), and internet exposure of any WLT-01 public route remains
PROHIBITED pending FND-FIND-001 (HIGH, separately tracked under FND-01).
**DEC-010 Turn 1 (WLT-01's own public-surface enablement gate + perimeter-provenance
admission) implemented and independently accepted at commit `af52fe8`; WLT-FIND-010
CLOSED** — see `02_modules/WLT-01/acceptance/
WLT-01_Public_Perimeter_Application_Gate_Opus_Acceptance_v1.0.md`. DEC-010 Turn 2
(trusted edge + network isolation, the layers that actually close FND-FIND-001)
is staged as two IMP-02 sub-turns. **Turn A (L1 UAT trusted-edge HTTP
reference implementation) is COMPLETE / ACCEPTED at commit `65fca52`**
(`IMP-02-ACC-001`), **Turn B (L2 mandatory network isolation, UAT proof)
is COMPLETE / ACCEPTED at commit `7132057`** (`IMP-02-ACC-002`), and
**Turn C (UAT TLS termination, functional) is now COMPLETE / ACCEPTED at
commit `d568fa0`** (`IMP-02-ACC-003`) — see the `IMP-02` section above.
**Turn B proves L2 isolation, and Turn C proves TLS termination, only
inside a disposable UAT harness, not at a production deployment; production
TLS certificate lifecycle, cipher policy, and backend/service-to-service
TLS or mTLS all remain PENDING IMP-02 work.** Production numeric pre-auth
policy remains NOT approved (an explicitly non-production INTERNAL-UAT-only
provisional policy is what Turn A implements, authorized for `IMP-02`
engineering/abuse-test purposes only) —
see `IMP-02` and `DECISION_LOG.md` DEC-010. **Neither Turn A, Turn B, nor
Turn C acceptance closes FND-FIND-001. Internet exposure remains
PROHIBITED.**

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| WLT-FIND-001 | WLT-01 | Monetary amount wire contract | LOW / NON-BLOCKING (2 routes hardened; broader rollout pending) | Ajv/Fastify `coerceTypes: true` allows a JSON numeric `amount` to enter a monetary string schema, silently losing precision beyond ~17 significant digits, unless explicitly rejected pre-coercion. | Two routes already hardened via `preValidation` (`rejectNonStringAmount`) and independently accepted (L-1 closed). Broader precision-hardening across remaining monetary-amount routes remains a dedicated, separate control. | None set | WLT-01 Monetary Amount Wire-Contract Hardening (L-1) independent Opus acceptance | OPEN |
| WLT-FIND-002 | WLT-01 | Concentration control | DEFERRED (by design) | Concentration control deferred at architecture stage. | None — deferred by design, not a defect. | None set | WLT-01 architecture review | DEFERRED |
| WLT-FIND-003 | WLT-01 | Step-up control | DEFERRED (by design) | Step-up control deferred at architecture stage. | None — deferred by design, not a defect. | None set | WLT-01 architecture review | DEFERRED |
| WLT-FIND-004 | WLT-01 | Client-facing public `/wlt1/*` surface | OPEN — prerequisites COMPLETE, implementation COMPLETE, independent acceptance COMPLETE at commit `7f9fc8a` | The client-facing public `/wlt1/*` surface (6-route contract, was FROZEN PENDING PREREQUISITES) was gated on two blockers, both since satisfied: **BLOCKER-1 (IAM-01 internal session-introspection seam)** — `POST /internal/auth/session/validate`, COMPLETE/ACCEPTED, commits `5794ffe`/`5a29559` (see IAM1-FIND-001…007). **BLOCKER-2 (shared rate-limit engine)** — COMPLETE/ACCEPTED at `2cdeaa5`, hardening COMPLETE/VERIFIED at `eb4a767` (see FND-FIND-002/003/004, all CLOSED; `02_modules/FND-01/acceptance/FND-01_Rate_Limit_Hardening_Opus_v1.0.md`). **Implementation is now COMPLETE**: the exact six-route contract was implemented at `12cedda` and independently reviewed, surfacing three MEDIUM findings (WLT-FIND-005/006/007) plus one LOW (WLT-FIND-008); all four were remediated and independently re-verified CLOSED at `7f9fc8a`. **WLT-01 PUBLIC CLIENT SURFACE: COMPLETE / ACCEPTED.** Full record: `02_modules/WLT-01/acceptance/WLT-01_Public_Client_Surface_Opus_Acceptance_v1.0.md`. This finding stays OPEN (not CLOSED) because it tracks the broader public-surface subject area, including five non-blocking carry-forwards (WLT-FIND-009 through WLT-FIND-013) and the fact that public-surface acceptance is explicitly NOT WLT-01 module completion and NOT internet-exposure approval — see FND-FIND-001 (HIGH), NOT solved by this work, remaining the separate mandatory precondition before any WLT-01 public route is internet-exposed. | No further action to implement the contract — COMPLETE. Track WLT-FIND-009 through WLT-FIND-013 (non-blocking) at their own severities. FND-FIND-001 must independently resolve before any internet exposure — a SEPARATE gate, not satisfied by public-surface acceptance. | None set | WLT-01 Client-Facing Public `/wlt1/*` Surface architecture review; IAM-01 Session Introspection independent Opus acceptance; Shared Rate-Limit Engine independent Opus acceptance (`2cdeaa5`) and post-acceptance hardening (`eb4a767`); WLT-01 Public Client Surface independent Opus acceptance (`12cedda` → `7f9fc8a`) | OPEN |
| WLT-FIND-005 | WLT-01 | Public client surface — address-integrity evidence | MEDIUM — **CLOSED at commit `7f9fc8a`** | Public wallet registration (`POST /wlt1/wallet-destinations`, implemented at `12cedda`) preserved address canonicalisation/validation but omitted the controlled evidence chain the accepted internal route produces: `wlt1.address_integrity_check` and the blueprint High-severity `wlt1.address_integrity_checked` audit event (mapped to WLT1-RISK-018), on both the success and refusal paths. | The internal route's private refusal-evidence helper was relocated to `lib/destinations.ts` and parameterized by actor identity so both routes reuse identical evidence-writing code (independently diffed byte-identical modulo actor parameterization); the public route now writes the same success-path and refusal-path evidence, correctly attributed to the human IAM actor and derived client. Independently proven transactionally fail-closed by a destructive trigger test (forced evidence-write failure → 503, zero destination rows, zero orphaned audits) and reproduced with production-shaped identifiers against a real Postgres. | Closed | WLT-01 Public Client Surface independent Opus acceptance (origin label W1) → remediation `7f9fc8a` → independent narrow re-review | CLOSED |
| WLT-FIND-006 | WLT-01 | Public client surface — idempotency client-authority binding | MEDIUM — **CLOSED at commit `7f9fc8a`** | Public mutation idempotency scope (wallet registration, payout registration, PoC challenge — all implemented at `12cedda`) was effectively `(iam_user_id, action, key)`, omitting derived client authority. A caller holding memberships in two clients could submit the identical Idempotency-Key and body narrowed to a different client each call and have the first client's result silently returned for the second — most severely on the PoC challenge route, where the idempotent-replay branch resolved BEFORE its own destination-ownership check. | `publicIdempotencyActorId(iamUserId, clientId)` folds the derived client into the foundation idempotency scope's `actor_id` (colon-composite, an established repository convention) on all three eligible public mutation routes; PoC verify remains unchanged, outside foundation idempotency, per the already-accepted frozen design. Collision-safety independently verified against the ACTUAL identifier grammar (each of `iam.user_identity.user_id`/`clt1.client_profile.client_id` has exactly one server-side minting path, no migration seeds either, UUID alphabet cannot produce a colon). Cross-client independence proven on all three routes with a real multi-membership actor, including the severe PoC-challenge case (a non-owning client now receives a safe 404, never a replayed cross-client challenge or message); same-client replay and audit `actor_id` (remains the plain human `iamUserId`, never the composite) both independently confirmed intact. | Closed | WLT-01 Public Client Surface independent Opus acceptance (origin label W2) → remediation `7f9fc8a` → independent narrow re-review | CLOSED |
| WLT-FIND-007 | WLT-01 | Public client surface — dependency-failure error mapping | MEDIUM — **CLOSED at commit `7f9fc8a`** | The public wire response when FND-01's rate-limit engine was unavailable/indeterminate (implemented at `12cedda`) returned the internal `RATE_LIMIT_UNAVAILABLE` code verbatim, rather than DEC-009's required generic public mapping (`503 SERVICE_UNAVAILABLE`). | `checkPublicRateLimit`'s enforcement-indeterminate branch now throws WLT-01's own already-established generic-unavailable code (`WLT1_SERVICE_UNAVAILABLE`), matching DEC-009's failure-semantics clause verbatim. Independently probed across a 7-mode FND failure matrix (503/401/400/418/malformed/timeout/network) — all map to public 503 generic, never the internal code, never 429; a genuine FND 429 independently confirmed to remain public 429 `RATE_LIMITED` with `Retry-After` preserved, on both read and mutation routes. No secret/internal-dependency detail leaks in the response body. | Closed | WLT-01 Public Client Surface independent Opus acceptance (origin label W3) → remediation `7f9fc8a` → independent narrow re-review | CLOSED |
| WLT-FIND-008 | WLT-01 | Public client surface — regression coverage | LOW — **CLOSED at commit `7f9fc8a`** | The committed test suite (implemented at `12cedda`) lacked committed regression coverage for bidirectional auth-channel separation (verified only ad hoc during review) and asserted only `client_id === undefined` rather than a full positive DTO allowlist on 5 of 6 public routes. | Four committed auth-channel-separation tests (asserting both status and error code); exact positive `Object.keys(data).sort()` allowlists committed for all six public routes, including a genuine crypto-fixture-based PoC-verify success (real production signature-canonicalisation path, not mocked) and a payout success via a temporarily-activated-then-restored rail fixture (independently confirmed to leave no leak across the full canonical suite and repeated reruns). | Closed | WLT-01 Public Client Surface independent Opus acceptance (origin label W6) → remediation `7f9fc8a` → independent narrow re-review | CLOSED |
| WLT-FIND-009 | WLT-01 | Public client surface — Sensitive Read actor-type attribution | LOW | Public-surface Sensitive Read disclosure evidence (`lib/sensitive-read.ts`) records `actor_type: "service"` for a human-driven public disclosure; `actor_id` correctly identifies the human IAM user, but the actor CLASS is mis-attributed in the very log that exists to attribute disclosures. | No action required at this severity for current acceptance. `logSensitiveDestinationRead` takes no `actorType` parameter today; a future WLT-01 turn should add one and have the public routes pass `"user"`. | None set | WLT-01 Public Client Surface independent Opus acceptance (origin label W4) | OPEN |
| WLT-FIND-010 | WLT-01 | Public client surface — no technical enablement gate | LOW — **CLOSED at commit `af52fe8`** | The six public routes previously registered unconditionally in `buildApp` — there was no feature flag, environment switch, or CFG-01 integration gating them; the `INTERNET EXPOSURE: PROHIBITED` posture was enforced procedurally only. `DECISION_LOG.md` DEC-010's Turn 1 (the WLT-01-owned L3 application-side gate) was implemented and independently accepted: `WLT1_PUBLIC_SURFACE_ENABLED` (safe default `false` — the six routes are NOT registered at all when disabled: no handler, no IAM/CLT/FND client, no DB path exists) plus a conditionally-required `WLT1_PUBLIC_PERIMETER_TOKEN` (≥32 chars, boot fails closed if enabled without it), checked via an `onRequest` perimeter-provenance hook — confined to a dedicated Fastify plugin scope, proven inert on the 27 internal routes and health/readiness — strictly before body parsing and any IAM/CLT/FND call. Independently reproduced: 0/0/0 IAM/CLT/FND calls and zero row deltas (including `foundation.outbox_event`) on rejection; 404 response byte-identical (body, headers, status) across disabled/missing-provenance/wrong-provenance/genuinely-unknown-route; constant-time comparison stress-tested against 14 pathological inputs; full six-route business regression unaffected. | This finding tracked ONLY the WLT-01 application-side gate (DEC-010 Turn 1) — CLOSED, no further action. **Closing this finding does NOT approve internet exposure and does NOT close FND-FIND-001** — DEC-010's Turn 2 (trusted edge pre-authentication throttling + mandatory network isolation) remains separately BLOCKED, pending deployment-owner designation and numeric-policy governance; see FND-FIND-001. | None set | WLT-01 Public Client Surface independent Opus acceptance (origin label W5); architecture accepted at DEC-010; implementation `af52fe8` independently accepted — full record: `02_modules/WLT-01/acceptance/WLT-01_Public_Perimeter_Application_Gate_Opus_Acceptance_v1.0.md` | CLOSED |
| WLT-FIND-014 | WLT-01 | Public perimeter gate — config edge-case fail-closed behaviour | INFORMATIONAL | A hand-built `Wlt1Config` with `publicSurfaceEnabled: true` and `publicPerimeterToken: undefined` (the internal `as string` cast in `server.ts`, reachable only by constructing a config object directly rather than through `loadWlt1Config`) yields `500 INTERNAL_ERROR` rather than the generic `404` when the route is invoked. Unreachable through the real config loader — validation rejects this exact state at boot (`CONFIGURATION_INVALID`) — and it fails closed (never admits) even in this unreachable state. Same structural-typing residual class as the already-accepted M-D2R-1 finding. | No action required at this severity — the state cannot occur through any production configuration path and has no security bypass. A future hardening pass could add a defensive guard-side check for symmetry with the 404-equivalence property. | None set | WLT-01 Public Perimeter Application Gate independent Opus acceptance (origin label T1-INF-1) | OPEN |
| WLT-FIND-015 | WLT-01 | Public perimeter gate — test-assertion precision | INFORMATIONAL | Two `toContain` assertions in the enabled-state portion of `tests/unit/wlt1-public-perimeter.test.ts` are substrings of the corresponding internal route lines (`/internal/wlt1/wallet-destinations (POST)` / `…payout-destinations (POST)`) and would false-pass if the public routes were absent; the tests' own titles additionally claim "27 internal routes" while asserting only three by substring. `iamCallCount`/`cltCallCount` in the DB-gated suite have no dedicated positive-control assertion (only asserted `toBe(0)` on the rejection path). | No action required for current acceptance — fully compensated: `tests/unit/wlt1-app.test.ts` pins the complete enabled 33-line route tree via an exact `toEqual` (fails on any missing or extra route), `fndCallCount` carries its own positive control in the same suite, and independent Opus review directly confirmed the IAM/CLT counters increment correctly on a valid authenticated chain. A future WLT-01 test-hygiene pass should tighten the two substring assertions and add explicit positive controls for iamCallCount/cltCallCount. | None set | WLT-01 Public Perimeter Application Gate independent Opus acceptance (origin label T1-INF-2) | OPEN |
| WLT-FIND-011 | WLT-01 | Public client surface — environment template completeness | LOW | WLT-01's production environment configuration (30 WLT-specific variables, including the 5 this implementation added — `iamBaseUrl`/`iamIntrospectionServiceToken`/`fndBaseUrl`/`fndRateLimitConsumerToken`/`publicDestinationListMax`) is not represented in the canonical `platform/.env.example` template, which has never covered WLT-01 at all. A pre-existing gap, widened by this implementation, not newly introduced. | No action required for current acceptance — boot remains fail-closed on any missing required variable (no silent misconfiguration). A future WLT-01 turn should add a WLT-01 section to `.env.example`, mirroring FND-FIND-004's now-closed remediation pattern. | None set | WLT-01 Public Client Surface independent Opus acceptance (origin label W7) | OPEN |
| WLT-FIND-012 | WLT-01 | Public client surface — informational observations | INFORMATIONAL | Carried observational notes, none security-actionable: the pagination cursor is unsigned/malleable but cannot cross tenants (the SQL predicate always uses the server-derived `client_id`, never the cursor's own embedded claim); `WLT1_PUBLIC_DESTINATION_LIST_MAX` has no operator-configured upper bound; the `WLT1_IAM01_UNAVAILABLE`/`WLT1_CLT1_UNAVAILABLE` error codes name internal module topology to a public caller; the `WLT1_CLT1_UNAVAILABLE` public message text ("client-status service") is imprecise for the membership-resolution code path that also emits it. | No action required at this severity. | None set | WLT-01 Public Client Surface independent Opus acceptance (origin label W8) | OPEN |
| WLT-FIND-013 | WLT-01 | Public client surface — FND-unavailable observability | LOW | The corrected generic public mapping (WLT-FIND-007) is public-side correct, but internal diagnosability regressed: `checkPublicRateLimit` now throws `WLT1_SERVICE_UNAVAILABLE` with no `cause`/detail, and `lib/fnd-rate-limit-client.ts` contains no logging — an FND-01 rate-limit outage now logs identically to an unrelated WLT DB-pool or provider outage. No security, correctness, or public-contract impact. | No action required for current acceptance. A future turn should retain the distinct internal reason (a `cause`, or a debug log in the FND client) without changing the public wire code. | None set | WLT-01 Public Client Surface independent Opus narrow remediation re-review (origin label W9, newly identified during this review) | OPEN |

### Capability-control plane — MIG-005 canonical environment model (FND-01 / CFG-01 / WLT-01)

Raised by the `MIG-005` review (`03_implementation/tasks/MIG-005/04-review.md`, commit `5f78a0b`). None blocked `MIG-005`. `CFG-FIND-001` gated `MIG-004` and is now CLOSED by it. `CFG-FIND-002` was raised and registered at `MIG-004` plan approval (`03_implementation/tasks/MIG-004/01-plan.md` §10A); it did not block `MIG-004` acceptance (a compensating control exists) but blocks UAT/DEMO use of any environment-gated capability and any future `environment_scope` write path.

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| CFG-FIND-001 | CFG-01 | Feature evaluation — environment trust boundary | HIGH — **CLOSED at commit `5a4f872`** | `POST /internal/cfg1/features/evaluate` and `verify-decision` took `environment` from the caller's request body, an unauthenticated assertion under the shared interim internal-service token. `MIG-004` closed it: CFG-01 evaluates, logs, hashes and binds tokens to only its own bootstrap-validated `ENVIRONMENT` (via `canonicalEnvironment()`); a caller-supplied `environment` is reduced to a routing-consistency assertion — a mismatch denies (`environment_mismatch`) with a Critical audit and issues no token; verify-decision revokes on the same mismatch or on a cross-environment token (`CFG1_DECISION_BINDING_MISMATCH`). Independently proven on a real PostgreSQL database: a `prod`-configured CFG-01 cannot be made to evaluate as `dev`/`qa`/`uat`/`demo`; a token issued under one environment never verifies under another. | Closed — no further action. Independent `MIG-004` review + human acceptance: `03_implementation/tasks/MIG-004/04-review.md`, `06-acceptance.md`. | None set | `MIG-005` review (F03); closed by `MIG-004` (commit `5a4f872`, accepted) | CLOSED |
| CFG-FIND-002 | CFG-01 | Config integrity seal — `feature` scope not verified at decision time; `environment_scope` not covered by the seal hash | MEDIUM | `cfg1.config_integrity_seal` supports a `'feature'` scope and `resealScope` can write it, but `verifyDecisionTimeIntegrity` checks only `licence_profile` and `prohibited_registry` at decision time — never `feature`. `computeFeatureScopeHash`'s allow-list also does not cover `environment_scope` (deliberately left out of `MIG-004`, since extending it without a paired reseal migration would make every existing active `feature` seal stale the instant `environment_scope` is backfilled — confirmed unchanged by `MIG-004`'s own tests). An out-of-band write to `cfg1.feature` (`current_state`, and `environment_scope`) is therefore undetected by the seal mechanism at evaluation time, though `MIG-004`'s fresh verify-decision re-read of live `environment_scope` independently catches a tampered `environment_scope` the next time an issued token is verified — the residual gap is a fresh `evaluate()` call made before any such verify, and `current_state` tamper generally. Not HIGH: a working compensating control exists, unlike `CFG-FIND-001`. Not LOW/INFORMATIONAL: it is a real detection gap in a mechanism whose stated purpose is exactly this class of tamper, pre-dating `MIG-004`. | One coherent remediation: (1) add `environment_scope` to `computeFeatureScopeHash`'s allow-list; (2) a migration that reseals every existing active `feature`-scope seal to the new hash shape in the same change; (3) add `feature` to `verifyDecisionTimeIntegrity`'s checked scopes at decision time; (4) tests proving an out-of-band `current_state`/`environment_scope` write is caught on the very next `evaluate()`/`verify-decision()`, not only at next token verify; (5) correct treatment of every existing row under the new hash, proven by the reseal migration. | Before UAT or DEMO use of any environment-gated capability; before any real `environment_scope` mutation/write path is introduced | `MIG-004` plan approval (HD-3, `01-plan.md` §10A); reconfirmed untouched at `MIG-004` acceptance | OPEN |
| FND-FIND-012 | FND-01 | Readiness — licence-lock interface | LOW | `buildReadiness` makes `licence_lock_interface` blocking only when `environment === "prod"`. `staging`, the production mirror mapped to PRODUCTION by `MIG-005`, reports ready where production reports `not_ready`, contrary to Doc 00 §1.E rule 5 / `SYS-RULE-007`. Activates no capability; no staging deployment exists. | Base the blocking flag on `isProductionGated(environment)` (this site only; the other 17 `=== "prod"` sites were adjudicated intentional). | Before any staging deployment | `MIG-005` review (F01 #18) | OPEN |
| FND-FIND-013 | FND-01 / CFG-01 / WLT-01 | Canonical DEMO environment adoption | LOW — **CFG-01 portion CLOSED at commit `5a4f872`; row stays OPEN for the WLT-01 portion** | `demo` was valid at the foundation but rejected downstream: the CFG-01 evaluate schema (`routes/features.ts:51`), WLT-01 `POC_DOMAIN_ENVIRONMENTS` and the canonical-message builder, and the CHECK constraints of migrations 001 (`release_registry`) and 053 (`domain_environment`). `as PocDomainEnvironment` casts in both WLT-01 PoC routes remain type-unsound, contained by the builder's runtime check. **CFG-01 portion closed by `MIG-004`:** the evaluate schema now sources `@aix/foundation`'s `ENVIRONMENTS` directly (local list deleted), so `demo` is accepted; proven on a real database. **WLT-01 portion remains open and unchanged:** its own environment vocabulary, the Proof-of-Control message/domain values, and migrations 001/053 (never edited) — every path there still fails closed. | CFG-01 part: closed, no further action. WLT-01 part: a WLT-01 task, via **new** migrations (never edit 001/053) plus a WLT-01 decision on extending the frozen v1 message value domain; replace the casts with a narrowing guard. | WLT-01 portion: before any DEMO deployment | `MIG-005` review (F02); CFG-01 portion closed by `MIG-004` (commit `5a4f872`, accepted) | OPEN |
| WLT-FIND-016 | WLT-01 | Fiat verification posture flag | INFORMATIONAL / FUTURE_CONSUMER | `WLT1_FIAT_VERIFICATION_REQUIRED` is forced `true` only when `ENVIRONMENT=prod`, and may be `false` elsewhere. `SYS-RULE-007A` forbids disabling a control for non-production convenience. Pre-existing; inert because no runtime consumer exists. | When a runtime consumer is added, require `true` in all five environments (or remove the flag). | Trigger: first runtime consumer | `MIG-005` review (F01 #17) | FUTURE_CONSUMER |

### Environment

| Finding ID | Module | Control | Severity / Status | Finding | Required Action | Due / Trigger | Source / Acceptance | State |
|---|---|---|---|---|---|---|---|---|
| ENV-FIND-001 | Platform-wide | Shared test database grants | ENVIRONMENT | The shared `aix_platform_test` database contains unrelated grant drift affecting some non-WLT roles. A fresh canonical database proves the grant *files* themselves are correct — the drift is local environment state, not a code or migration defect. | No code/migration action required. Environment cleanup (resetting the shared test DB) is separate from module development and may be done at any time without blocking module work. | None set | WLT-01 acceptance review process | ENVIRONMENT |

---

If a finding above is later closed, update its **State** to `CLOSED` and cite the
commit/review that proves closure — do not delete the row; closed findings remain
part of the historical register.
