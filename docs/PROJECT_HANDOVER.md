# AIX Platform — Project Handover

Read this file **first** in any new session, together with [MODULE_STATUS.md](MODULE_STATUS.md). Do not scan the whole repository.

## Project context
AIX Money Broking + PSO (Payment System Operator) platform documentation workspace — Labuan FSA regulated. SDLC master documents + per-module blueprint packs. No git repo. User is product owner; Claude acts as principal fintech platform architect for review, and as engineer for build.

## Licence / product lock (non-negotiable)
- **Money Broking + PSO approved. Exchange application PENDING — all exchange features LOCKED.**
- No public order book · no matching engine · no client-to-client matching · no market making · no principal dealing · no AIX spread markup · AIX inventory = zero.
- Revenue model = **disclosed brokerage fee only**.
- Execution model = **agency / back-to-back through approved LP**.
- Third-party custody; client-money safeguarding (full-backing). Institutional/HNWI only; retail off by default.

## Completed master docs
Master SDLC pack **00 → 11 complete** (latest authoritative versions in [INDEX.md](INDEX.md)). 00/01 at v1.3; 02–11 at v1.2. All reviewed/final-verified.

## Accepted modules
| Module | Version | Status |
|---|---|---|
| FND-01 Platform Foundation | v1.2 | ✅ accepted |
| IAM-01 Authentication / MFA / Session | v1.2 | ✅ accepted |

## Implementation phase
- The full platform blueprint chain is review-complete and accepted through **PRT-01 v1.2**.
- **IMP-01 AIX Master Implementation Handover Pack v1.0** has been added under `implementation/`.
- Implementation is underway. FND-01, IAM-01, and IAM-02 (Phases 0-5) are accepted baselines.
- Current implementation module: **SEC-01 Audit Log / Security Monitoring** — Phases 0-2 accepted (Opus-reviewed, F1 closed); **Phase 3 accepted** after the **P3-F1 gap patch** (+ P3-L1, P3-L3) and an independent Opus re-review (`aix-platform/docs/SEC-01_Phase3_Security_Review_Opus_v0.2_reverify.md` → CLOSED, 325/325 tests); **Phase 4 accepted** after the **F-1 gap patch** and an independent Opus re-review (F-1 CLOSED, 401/401 tests); **Phase 5 now accepted** — security monitoring rule engine + alert lifecycle + Critical-closure approval via IAM-02's existing execute-verify endpoint + dead-letter replay (migrations 012-013). Two Sonnet self-review passes closed 4 real gaps (idempotency stuck-processing on Critical close; a rule-engine duplicate-alert race; the same race in the seal-verification hook; missing `client_id` decision-token binding), all fixed and regression-tested. Independent **Opus review → ACCEPT WITH MINOR CONDITIONS** (no Critical/High/Medium findings); the two minor conditions (LOW-1 dead-letter-write-failure observability, LOW-3 `decision_token` log redaction) closed in the same pass — **476/476 tests, ACCEPTED baseline**. P3-L2 + F-2…F-6 + SoD-rule-2-self-close + full IAM-02 `sod_rule`-table integration remain tracked non-blocking carry-forwards (F-5 now more load-bearing after Phase 5's added routes).
- **Current implementation module: CFG-01 Feature Flag / Licence Lock** — decision resolved (CFG-01 chosen over a further SEC-01 slice). **Phase 0 (scaffold) accepted**: new service `services/cfg1` (Fastify), own request-context plugin, own interim internal-identity guard (constant-time compare; traced to confirm it never trusts a client-supplied `actor_id`), own config loader (`CFG1_INTERNAL_SERVICE_TOKEN` fail-closed), boot-time `assertNoExchangeRuntime`, F3(c) import-boundary test, one route (`GET /internal/cfg1/health`) — no schema, migration, grants, or business logic this phase. Independent Opus review → ACCEPT, zero findings — 505/505 tests, ACCEPTED baseline.
- **CFG-01 Phase 1 (core registries + sealed baseline) now accepted**: migration `014_cfg1_core.cjs` creates `cfg1` schema + `role_cfg1_runtime` and 5 tables — `licence_profile`, `feature`, `prohibited_feature`, `feature_version`, `config_integrity_seal`. Licence-profile seed: `MB` approved/LFSA, `PSO` approved/LFSA, `EXCHANGE` pending/LFSA — Exchange enforcement is layered (licence status + every Exchange feature code separately hard-blocked in `prohibited_feature` + the boot-time `assertNoExchangeRuntime` guard), not status alone. A 30-code reconciled prohibited-feature registry (blueprint's original 19 + Doc00-derived additions: securities, investment advice, self-custody, lending, derivatives, margin, staking, yield, internal fallback pricing, internal inventory, synthetic liquidity) — corrected from a "31-code" miscount in the Phase 1 planning message (documented as a correction, not a scope reduction; no code was invented). A vendored Doc00 v1.3 baseline constant (`services/cfg1/src/lib/doc00-baseline.ts`, no runtime filesystem dependency on this docs repo). An interim sha256 integrity seal (CFG-01's own canonical-JSON/hash library, explicit allow-list fields excluding mutable columns, no fake KMS/PKI/digital signature — `signed_by`/`signature_ref`/`approval_id` all `NULL`). `GET /internal/cfg1/readiness` recomputes and compares both sealed scopes (minimal, non-leaking response; queried, not a boot-time hard-crash; explicitly does not replace Phase 2's real decision-time integrity check). `role_cfg1_runtime` is SELECT-only, verified directly under the real runtime role (INSERT/UPDATE/DELETE all denied; no `iam2`/`sec1`/`foundation` access). `feature`/`feature_version` intentionally empty. No runtime decision engine, no mutation workflow, no IAM-02/SEC-01 integration yet. A Sonnet self-review pass found one documentation-only defect (a stale header comment) and patched it — no functional defects. Independent **Opus review → ACCEPT**, zero Critical/High/Medium findings — **547/547 tests (505 baseline + 42 new), migrations 001→014 + all 5 grant files clean, `tsc -b` clean, ACCEPTED baseline**.
- **Carry-forwards (non-blocking) heading into Phase 2**: **F-1** — the seal-hash row-ordering uses locale-sensitive `localeCompare`; before Phase 2's decision-time checks depend on it, replace with locale-independent codepoint ordering in both the service library and the migration/seed logic (no live defect today — empirically confirmed `localeCompare` matches codepoint order for the current 30+3 codes — but a `LC_COLLATE` difference between environments could otherwise produce a false `hash_mismatch`). **F-2** — Phase 2's decision-time integrity check must verify DB-to-vendored-Doc00 agreement (recompute expected hashes from the vendored constant and check `doc00_source_version`), not only the DB-to-seal internal consistency Phase 1's readiness route performs. The interim sha256 seal remains detection-only (real signing/KMS deferred, must not be treated as solved). The later Exchange Activation Ceremony must remain a structural fail-closed placeholder — no fake LFSA evidence source, ever. The interim shared-token trust model carry-forward is now more load-bearing heading into Phase 2+.
- **CFG-01 Phase 2 (runtime decision engine) now accepted.** Migration `015_cfg1_decision_engine.cjs` adds `cfg1.feature_decision_log` (append-only decision evidence) and `cfg1.feature_decision_token` (bounded-reuse decision credential; `decision` CHECK'd to `allow` only, `status` CHECK'd to `active`/`revoked` only — no raw token column exists anywhere). New endpoints `POST /internal/cfg1/features/evaluate` and `POST /internal/cfg1/features/verify-decision`. **F-1 closed**: `codePointCompare` (locale-independent) replaces `localeCompare` everywhere a row ordering feeds a seal/baseline hash (`canonical.ts`, `doc00-baseline.ts`, `integrity-seal.ts`, migration 015's inline copy); migration 015 re-verifies the real Phase 1 seed data under the corrected comparator rather than assuming agreement — passed clean, confirming the earlier finding was latent, not live. **F-2 closed**: `verifyDecisionTimeIntegrity` adds a decision-time check that (1) re-derives the active seal's `doc00_baseline_hash`/`doc00_source_version` against the vendored, compiled-in Doc00 constant, then (2) directly compares live DB rows field-by-field against that same vendored constant — never mediated through the seal — closing the gap where an attacker who rewrites both the registry data and the seal's own recomputed hash would otherwise pass undetected. All 4 required attack scenarios (stale `doc00_baseline_hash` with agreeing `config_hash`; stale `doc00_source_version`; a prohibited code removed with the seal recomputed to match; `EXCHANGE` flipped to approved with the seal recomputed to match) are proven by dedicated tests. This check runs first, unconditionally, before any decision precedence step. Decision tokens are **bounded-reuse, not single-use** (5-minute TTL) — safety comes from re-checking full binding + fresh integrity + current registry version/hash on every `verify-decision` call, with any mismatch revoking the token immediately (a corrected retry with the same token then fails as `_REVOKED`). No raw token is ever stored (hash-only-at-rest) or logged (on the redaction list). No `Idempotency-Key` is required for either endpoint (a decision is a legitimate answer, mirroring IAM-02's `permission/check`). Audit integration uses only FND's `publishAudit`/outbox pattern (never direct SEC-01 HTTP), transaction-coupled with the decision-log write — any failure, including the outbox insert itself, fails the whole call closed as `CFG1_AUDIT_REQUIRED` with no orphaned decision-log row (proven by a forced outbox-grant-revoke integration test). `role_cfg1_runtime` grants extended least-privilege: `feature_decision_log` SELECT+INSERT (append-only), `feature_decision_token` SELECT+INSERT+UPDATE (no DELETE), `foundation.outbox_event` INSERT-only (CFG-01's first cross-schema grant) — Phase 1's 5 tables remain SELECT-only, no `iam2`/`sec1` access. `cfg1.feature`/`feature_version` remain empty in production seed; the allow path is reachable only via synthetic test-fixture rows. `caller_module` remains a declared, not cryptographically authenticated, identity under the interim shared-token guard — documented as more load-bearing now that it's attributed in decision logs and audit trails. No mutation workflow, kill-switch, IAM-02 registration, SoD rules, or Exchange Activation Ceremony this phase. A Sonnet self-review pass found two legitimately-stale test assertions (not functional defects) and updated them. Independent **Opus review → ACCEPT**, zero Critical/High/Medium findings — **596/596 tests (547 baseline + 49 new), migrations 001→015 + all 5 grant files clean, `tsc -b` clean**; the Opus reviewer independently re-ran the full pipeline from a fresh disposable Postgres, reproduced 596/596, and ran independent static sweeps (import-boundary, no-Exchange, `localeCompare`, raw-token storage/logging) — all clean. Two Low, non-blocking findings: **Low-1** the F-2 direct fact comparison currently covers only the fields the decision engine actually reads (licence status, prohibited-code presence) — extend it if a later phase makes `authority`/`applies_until`/`prohibition_reason` decision-load-bearing; **Low-2** audit events omit the optional `event_category` field (cosmetic, no live impact). **CFG-01 Phase 2 is an accepted implementation baseline.**
- **Carry-forwards (non-blocking) heading into Phase 3**: real service-identity model must replace the declared/spoofable `caller_module` before any Phase 3 mutation workflow trusts it as an identity; real signing/KMS/PKI remains deferred (interim sha256 seal stays detection-only, must not be treated as solved); the F-2 direct fact comparison should be extended if future phases add decision-load-bearing fields beyond licence status/prohibited-code presence; the bounded-reuse decision-token model should be revalidated once real production `feature` rows and a mutation path exist; the later Exchange Activation Ceremony must remain a structural fail-closed placeholder — no fake LFSA evidence source, ever; `assertNoExchangeRuntime` + the route-surface test must be kept in every future phase. (Process note, not a CFG-01 finding: SEC-01's own `sec1-db.test.ts` requires `SEC1_INGEST_TOKEN_*` env values to exactly match its hardcoded test literals — a mismatch produces confusing unrelated Phase-5 failures rather than a clear error; worth a one-line note in that test file.) **CFG-01 Phase 3A (governed feature/licence mutation workflow) now accepted**, after an initial DO NOT ACCEPT and a blocking-fix patch. Implements: `cfg1.feature_state_change` (blueprint-defined) + `cfg1.licence_profile_change` (documented blueprint extension — the blueprint never defined a licence-profile change table); `POST /internal/cfg1/feature-changes/{request,apply}` + `POST /internal/cfg1/licence-profile-changes/{request,apply}`; `resealScope()`/`computeFeatureScopeHash()` (reseal the mutated scope in the SAME transaction as the state write and audit publish, superseding the old active seal before inserting the new one — an ordering bug the self-review caught and fixed, since the reverse order violates the one-active-seal-per-scope partial unique index); decision-token invalidation extended with `licenceProfileVersion`/`featureConfigVersion` so a mutation invalidates every outstanding Phase 2 token bound to the mutated scope. **Approved decision #1/#2/#3 (identity)**: CFG-01 builds NO maker-checker tables of its own — mutation APPLY routes require a real IAM-02-issued decision token (bound to the approving human actor and the exact stored payload hash, verified via IAM-02's *existing* `execute-verify` endpoint) before any state change; `caller_module` + the shared internal-service-token alone can never authorise a mutation, reusing exactly the pattern SEC-01 Phase 5's Critical-alert-closure route established (no new IAM-02 code). 8 CFG-01 permissions additively registered in `iam2.permission` (no IAM-02 guard/grant change, no `role_permission` seed). `cfg1.prohibited_feature` remains structurally locked (SELECT-only, no mutation route, no `.manage` permission ever registered) — proven by direct grant-boundary regression tests.
- **Independent Opus review — initial verdict DO NOT ACCEPT.** One High finding **F-1**: the 6 sensitive mutation permissions (`feature.enable`/`.disable`, all four `licence_profile.*` actions) were registered `licence_locked=true` in the IAM-02 catalogue. IAM-02's guard treats `licence_locked=true` as an UNCONDITIONAL, non-overridable deny, checked before any role/approval logic runs — reserved for genuine business-capability locks (the Exchange-shaped features CFG-01's own prohibited-feature registry already blocks), not for "this permission concerns licence administration." Result: every real feature-apply and licence-profile call was denied outright, for every actor, always — the whole Phase 3A mutation workflow was fail-closed but permanently non-functional against the real guard. Invisible to the self-reported 646/646 result because every integration test stubbed IAM-02 to return `allow` directly; caught only because the independent review read `guard.ts`'s own source and traced the real decision path rather than trusting the test suite.
- **Fix**: `licence_locked=false` on all 8 CFG-01 permissions (they are admin/governance actions on CFG-01's own configuration surface, correctly gated by `requires_approval`/`requires_step_up`/the CFG-01 route-layer execute-verify binding — never by the licence-lock step). Building the real-IAM-02-guard regression test this fix required (see below) surfaced a **second, identically-shaped defect**: IAM-02's guard also returns `approval_required`/`step_up_required` UNCONDITIONALLY (before role-grant lookup) for any permission carrying `requires_approval`/`requires_step_up` — meaning even with `licence_locked=false`, CFG-01's baseline `checkPermission()` (which originally required a strict `decision === "allow"`) could never pass for the 6 sensitive permissions, for any actor, including a properly-granted one. Rather than following SEC-01's own alternate resolution (setting `requires_approval=false` on its own permission and enforcing the gate purely at the route layer), CFG-01 kept the catalogue flags as-is (so an operator reading `iam2.permission` directly can see which actions are approval/step-up-sensitive) and instead corrected `lib/iam2-client.ts`'s `checkPermission` to treat `approval_required`/`step_up_required` as a baseline PASS alongside `allow` — the real, unconditional authorisation gate was always the separate, mandatory `execute-verify` call, never this advisory baseline probe. Independently re-verified: no route can mutate on a baseline pass alone; `deny`/`licence_locked`/IAM-02-unavailable/malformed all still fail closed.
- New `tests/integration/cfg1-iam2-guard-real.test.ts` — the first CFG-01 test exercising a REAL, listening IAM-02 server (`app.listen()` on an ephemeral port) instead of a stub, calling CFG-01's own production `checkPermission()` client over real HTTP. Regression-proofed directly: run against a deliberately-reverted copy of the original buggy migration, it failed on 6 of 8 assertions; against the fix, all 8 pass. A stale header comment (describing an abandoned "mark the row failed in a recovery transaction" design) was also corrected to match the actual behaviour (failed apply attempts leave the change row `requested`/retriable — no separate unreliable write is attempted under an audit outage).
- **Independent Opus re-review → ACCEPT.** **657/657 tests (646 baseline + 11 new), migrations 001→017 + all 5 grant files clean, `tsc -b` clean.** Real IAM-02 guard coverage independently confirmed genuine (not a stub); direct catalogue inspection confirmed all 8 permissions `licence_locked=false`, zero `role_permission` rows, no forbidden codes (`prohibited_feature.manage`, `kill_switch.*`); static sweeps (kill-switch, `prohibited_feature` writes, Exchange routes, F3(c) import-boundary) all clean; both apply routes independently traced to confirm execute-verify remains mandatory before any mutation. **CFG-01 Phase 3A is an accepted implementation baseline.** No Phase 3B code, no kill-switch, no out-of-scope modules.
- **Carry-forwards (non-blocking) heading into Phase 3B**: Phase 3B's own `cfg1.kill_switch.*` permission registration must use the corrected pattern (`licence_locked=false`) and extend the real-IAM-02-guard test file to cover the new codes — do not rely on stubs for the positive path. Real service-identity model still needed (mutation *authorisation* is now IAM-02-approval-bound and cryptographically real; actor *attribution* on change rows is still only as strong as the interim shared-token guard). Real signing/KMS/PKI remains deferred. A licence-profile status mutation diverging from the vendored Doc00 baseline halts ALL feature decisions platform-wide via F-2 until `doc00-baseline.ts` is updated and redeployed — this is deliberate, conservative, and proven by dedicated tests, but needs a documented operator runbook before any real production use of the licence-mutation workflow. Role-level SoD (the blueprint's role-specific rules, e.g. "Compliance must approve licence-sensitive features") remains deferred pending real IAM-02 roles that don't yet exist in the provisional catalogue — maker≠approver is enforced for free by IAM-02's own unconditional approval check in the meantime.
- **CFG-01 Phase 3B (feature-scoped kill-switch workflow + decision-engine integration) now accepted.** Migration `018_cfg1_kill_switch.cjs`: `cfg1.kill_switch` (current state, one row per feature_code, partial unique index `cfg1_kill_switch_one_active_per_feature` enforcing at most one active switch per feature at the DB level), `cfg1.kill_switch_deactivation_request` (documented extension beyond the blueprint's single-table design — mirrors `feature_state_change`'s mutable request/apply shape, needed because the append-only event log cannot safely double as a "latest unresolved request" tracker), `cfg1.kill_switch_event` (append-only). Migration `019_iam2_register_cfg1_kill_switch_permissions.cjs`: `cfg1.kill_switch.activate` / `.deactivate_request` / `.deactivate` — three codes where the blueprint names one, because IAM-02's guard gates `requires_approval` as a static per-code property and activation/deactivation have structurally different approval requirements; all three `licence_locked=false` from the start (the Phase 3A-corrected pattern, no repeat of F-1). No `cfg1.kill_switch.read`, no read/list route, no global kill-switch, no `scope` jsonb — all deliberately out of scope. Feature-scoped only; no seal/reseal/Doc00-baseline participation (kill-switch is live operational state, read fresh on every decision, same posture as `cfg1.feature.current_state` itself).
- **Activation** (`POST /internal/cfg1/kill-switches/activate`) is a single step — permission-gated only, no execute-verify, because there is no second gate to fall back on; the route additionally asserts the baseline decision is a genuine `permission_granted` (not merely `approval_required`/`step_up_required`) as structural defence-in-depth. **Deactivation** is the same two-step request→apply, IAM-02-approval-bound pattern Phase 3A established (`cfg1.kill_switch.deactivate` requires real execute-verify before any mutation). **Partial SoD**: `requestKillSwitchDeactivation` rejects a proposer who equals the kill-switch's own `activated_by` — a cheap, real check CFG-01 already owns. **Residual SoD gap, explicitly documented, not silently ignored**: CFG-01 cannot verify that the eventual IAM-02 *approver* of a different proposer's request is not the original activator, because `execute-verify` binds and returns only the token's maker identity, never the approver's (which lives only in `iam2.approval_decision.approver_user_id`, a table CFG-01 has no grant to read) — closing this needs an IAM-02 change, out of scope for CFG-01.
- **Decision-engine integration**: `evaluateFeature()` gained a live, unversioned `isKillSwitchActiveForFeature()` check positioned after the prohibited-feature check and before the `cfg1.feature` row lookup — an active kill-switch pre-emptively blocks even a `feature_code` with no feature row yet. **Critical design correction, caught during planning**: `verify-decision` does NOT re-run `evaluateFeature()`, so a kill-switch check added only there would never protect outstanding decision tokens issued before activation. An explicit, separate live check was therefore added inside the verify-decision route and threaded into `verifyDecisionToken()` as a new parameter (deliberately not folded into the existing version-comparison mechanism, which would need a schema change on the already-accepted Phase 2 token table for no real benefit) — an allow token issued before activation is revoked and fails on its very next verify with `CFG1_TOKEN_REVOKED_BY_KILL_SWITCH`, proven end-to-end by a dedicated integration test.
- Every kill-switch mutation is transaction-coupled to FND's `publishAudit`/outbox pattern exactly like every prior CFG-01 mutation — an outbox failure rolls back the whole transaction, proven directly (no orphaned active switch, no orphaned deactivation). Grants extended with narrowly column-scoped `SELECT`/`INSERT`/`UPDATE` on the two mutable kill-switch tables and `INSERT`-only on the append-only event table; `cfg1.prohibited_feature` remains completely unchanged (SELECT-only, regression-proofed directly).
- **A Sonnet self-review pass found and fixed one real concurrency defect** (not caught by the first test run, which passed by timing luck): `activateKillSwitch`'s `FOR UPDATE` lookup does nothing when zero rows exist yet, so two genuinely simultaneous first-ever-activation attempts on the same `feature_code` could both reach `INSERT`, and the loser would hit a raw Postgres unique-violation that the route mis-translated into a misleading `CFG1_AUDIT_REQUIRED` (503) instead of the correct, clean `CFG1_KILL_SWITCH_ALREADY_ACTIVE` (409). **Fixed** with a transaction-scoped `pg_advisory_xact_lock(hashtext($1))` keyed by `feature_code`, taken before the lookup — the same idiom SEC-01 already established for the identical class of "first concurrent writer wins, others resolve cleanly" problem. A stale test assertion (a Phase 1 exchange-denial reason-code expectation) was also corrected.
- **Independent Opus review → ACCEPT.** Independently reproduced from a genuinely fresh disposable Postgres (migrations 001→019 run straight through, then all 5 grant files): **708/708 tests, 48 files, 0 failures**; `tsc -b` clean. Direct SQL catalogue inspection confirmed all 3 codes present with the exact documented flags, all `licence_locked=false`, zero `role_permission` rows, no `.read` row. Direct grant inspection confirmed column-scoped UPDATE grants exactly as documented, `INSERT`-only on the event table, no `DELETE`/`TRUNCATE` anywhere, `prohibited_feature` unchanged, no `iam2.*`/`sec1.*` cross-schema grants. `cfg1-iam2-guard-real.test.ts` independently re-run (14/14) — a real listening IAM-02 server, not a stub, confirms the positive path for all 3 new codes. Static sweeps (Exchange route/config, `prohibited_feature` write, kill-switch read/list, raw-token storage) all clean. **No Critical/High/Medium findings.** Two Low/informational, non-blocking items: **L1** the partial-SoD self-block is bounded by the same declared-identity trust model flagged since Phase 2 (not a Phase 3B regression); **L2** pre-existing test-harness fragility — `iam2-db.test.ts` and `cfg1-iam2-guard-real.test.ts` share a runtime-role name (forces single-fork test execution to avoid a `CREATE ROLE` race), and the Phase 1 "`cfg1.feature` intentionally empty" assertion is sensitive to shared-DB test ordering (passes 88/88 on a pristine DB) — both pre-existing test-infra items, not Phase 3B code. **CFG-01 Phase 3B is an accepted implementation baseline.** No code was written or changed during the review.
- **Carry-forwards (non-blocking) heading into the next CFG-01 phase**: approver-side SoD remains open until IAM-02's `execute-verify` exposes approver identity or a suitable cross-module SoD mechanism exists; service-to-service authentication / declared actor identity remains carry-forward (the partial SoD self-block and every prior actor-attribution caveat are bounded by this); real signing/KMS/PKI remains deferred; the F-2/licence-mutation operator runbook is still required before real licence-mutation use; role-level SoD remains deferred pending real IAM-02 roles; test-harness cleanup is tracked (namespace runtime-role names per test file or provision centrally; make the Phase 1 empty-table assertion resilient to shared-DB ordering). `assertNoExchangeRuntime` + the route-surface test must be kept in every future phase — no Exchange runtime, ever. CFG-01 paused as an accepted baseline through Phase 3B.
- **CLT-01 Feature-Flag-adjacent module chosen next: Client Onboarding / Client Profile.** **CLT-01 Phase 0 (scaffold) is now an accepted implementation baseline.** New service `services/clt1` (Fastify), structurally identical to CFG-01's own Phase 0: own request-context plugin, own interim internal-identity guard (constant-time compare, fail-closed), own config loader (`CLT1_INTERNAL_SERVICE_TOKEN` fail-closed — no CFG-01/IAM-02/SEC-01 base URL or token added yet, deliberately, since no Phase 0 code path reaches them), boot-time `assertNoExchangeRuntime`, F3(c) import-boundary test, exactly one route (`GET /internal/clt1/health`). **No `/internal/clt1/readiness` this phase** — deliberate, not an omission: CFG-01's own Phase 0 shipped health only and added readiness in Phase 1 alongside its first real schema to check; CLT-01 has no `clt1.*` table yet, so a readiness route today would have nothing CLT-01-specific to report. No schema, no migration, no grants, no business tables, no business routes, no CFG-01/IAM-02/SEC-01 integration, no public `/clt1/*` routes. Error catalogue (`lib/errors.ts`) ships a structurally-ready but intentionally empty `CLT1_ERROR_CODES` object — Phase 0's two failure modes (guard rejection, config failure) both reuse shared foundation codes (`SERVICE_IDENTITY_REQUIRED`, `CONFIGURATION_INVALID`), the same "only add codes this stage's routes can actually throw" discipline IAM-02/SEC-01/CFG-01 each applied at their own Phase 0/1. A Sonnet self-review pass found no functional defects.
- **Independent Opus review → ACCEPT.** Independently reproduced from a genuinely fresh disposable Postgres (migrations 001→019 interleaved with all 5 grant files, unchanged — no new CLT-01 migration): **740/740 tests, 54 files, 0 failures** (708 baseline + 32 new); `tsc -b` clean both incrementally and on a forced full rebuild. Route surface confirmed exactly `GET /internal/clt1/health` (readiness explicitly absent, proven by a 404 test); F3(c) import-boundary confirmed clean by both the dedicated test and independent static grep; zero occurrences of any forbidden Exchange/business-surface fragment anywhere in `services/clt1/src`; zero `infra/migrations`/`infra/grants` files added or touched. **No Critical/High/Medium findings.** Three Low/informational, non-blocking items: **L1** the route-surface test's forbidden-substring list (`matching`/`principal`/`spread`/`trading`/`execution`) is stricter than the real boot guard's hyphenated-fragment matching — reconcile if a legitimate future route (e.g. duplicate-detection `match_type`) needs one of these bare terms; **L2** the interim shared-token identity model is higher-stakes for CLT-01 specifically, since it will become the platform's PII system of record — flagged in `config.ts`'s own header comment, not a new problem; **L3** pre-existing no-DB `vitest run` canary-failure behaviour in unrelated integration test files, unrelated to CLT-01 code. **CLT-01 Phase 0 is an accepted implementation baseline.** No code was written or changed during the review.
- **Carry-forwards (non-blocking) heading into CLT-01 Phase 1**: `/internal/clt1/readiness` must land once the first `clt1.*` table exists (CFG-01's own Phase 0→1 precedent); the CFG-01 `cfg1.feature`-row operational dependency (most of CLT-01's planned CFG-01 gates need governed `cfg1.feature` rows created via CFG-01's own accepted Phase 3A mutation workflow before `evaluate()` can return anything but `unknown_fail_closed`) must be resolved or explicitly fixture-scoped before Phase 1's CFG-01 gate integration tests; the interim shared-token identity model (L2 above) should be revisited before any real client-profile route is exposed; the first CLT-01 IAM-02 permissions must use the `licence_locked=false` governance-permission pattern from the start (the CFG-01 Phase 3A F-1 lesson, must never be repeated) and Phase 1+ must include real-listening-IAM-02 guard tests (the `cfg1-iam2-guard-real.test.ts` precedent) once IAM-02 permissions are introduced — never rely on stubs alone for the positive path.
- **CLT-01 Phase 1 (application intake / classification / consent baseline) is now an accepted implementation baseline.** Migration `020_clt1_intake_baseline.cjs`: `clt1` schema + `role_clt1_runtime` + 4 tables:
  - `infra/migrations/020_clt1_intake_baseline.cjs`
  - `infra/grants/clt1_runtime_grants.sql`
  - `clt1.client_application` — the only table Phase 1 code mutates
  - `clt1.client_profile` — table only, zero Phase 1 rows written (final-approval-gated, out of scope)
  - `clt1.client_classification_evidence` — append-only, `provided`-status only
  - `clt1.consent_record` — append-only
  - internal-only application routes (`POST/PATCH/GET applications`, `classification-evidence`, `consents`, `submit`, `start-review`, `cancel`)
  - `GET /internal/clt1/readiness`
  - CFG-01 evaluate-only client (own copy, F3(c)) — no `verify-decision` call, no raw CFG decision token stored
  - `draft → submitted → under_review` lifecycle; `draft/submitted → cancelled`
  - class claim capture (institutional/hnwi/professional map to their own `onboarding.*` gate; retail/unknown both map to `onboarding.retail_default`, CFG-01's own permanent prohibition — no local special-casing)
  - classification evidence capture as `provided` only (no verify/reject route — no IAM-02 model yet to gate who may verify)
  - consent capture; consent required before submit
  - PII stored in `client_application` but never returned by `GET application` (`safeApplicationResponse()` single projection point)
  - audit/outbox transaction-coupled on every mutation; CFG-01 denials audited inside the transaction before the route throws
  - `role_clt1_runtime` least-privilege grants (column-scoped `UPDATE` on `client_application`; `SELECT`-only on `client_profile`; append-only on evidence/consent; `INSERT`-only on `foundation.outbox_event`)
  - no `cfg1`/`iam2`/`sec1` direct table grants — CFG-01 reached only over HTTP
  - no public `/clt1/*` routes
  - no final approval / approve / reject / hold
  - no IAM-02 integration yet
  - no KYC/AML/CDD
  - no Exchange/trading/settlement runtime
  - **806/806 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium findings**
- A Sonnet self-review pass fixed two bugs (a `config.ts` field-narrowing issue where a bad edit briefly produced invalid syntax, caught before commit; a TypeScript control-flow narrowing issue across three routes requiring an explicit unreachable-fallback throw so the compiler could narrow a discriminated-union return type) plus one header-comment correction (the "never throw inside `withTransaction`" claim was overstated — corrected to clarify that concurrency-guard throws are safe because they fire before any write in that attempt, only CFG-01-gate-denial audit writes require the structured-outcome pattern).
- **Independent Opus review → ACCEPT.** Independently reproduced from a genuinely fresh disposable Postgres (migrations 001→020, all 6 grant files): **806/806 tests, 57 files, 0 failures**, `tsc -b` clean (incremental and forced full rebuild). Grant boundary verified directly via `information_schema.role_table_grants`/`role_column_grants` (not just by reading the SQL) — column-scoped `UPDATE` on `client_application` confirmed to exclude `id`/`application_id`/`applicant_type`/`created_by`/`created_at_utc`; zero grant into `iam`/`iam2`/`sec1`/`cfg1`. Route surface confirmed exactly the 10 approved internal routes via direct source enumeration; confirmed only `POST /internal/cfg1/features/evaluate` is ever called, never `verify-decision`. Static sweeps (public-route strings, Exchange/trading fragments, cross-schema grants, `DELETE`/`TRUNCATE` grants, cross-service imports, decision-token handling) all clean. **No Critical/High/Medium findings.** Four Low/informational, non-blocking: **L1** `CLT1_SERVICE_UNAVAILABLE`'s catalogue message reads "CLT-01 database is not reachable" even when the actual cause is CFG-01 being unreachable (the `details` field is accurate; only the top-level message is imprecise) — consider a distinct `CLT1_CFG_GATE_UNAVAILABLE` code later; **L2** audit `actor_id` falls back to `application_id`/`service` for routes with no declared human-actor field (patch/submit/cancel/start-review-without-reviewer_id) — weak attribution, acceptable interim, tighten with IAM-02 identity; **L3** no FK from `client_classification_evidence.application_id`/`consent_record.application_id` to `client_application` — existence enforced only at the API layer, consistent with the codebase's append-only-log style but a hardening opportunity; **L4** readiness probes only `client_application` as the table-reachability proxy, not all four tables (low-risk since all four are created atomically in one migration). **CLT-01 Phase 1 is an accepted implementation baseline.** No code was written or changed during the review.
- **Carry-forwards (non-blocking) heading into CLT-01 Phase 2**: the CFG-01 `cfg1.feature`-row operational runbook must be executed (via CFG-01's own accepted Phase 3A governed mutation workflow) before any real onboarding use — institutional/hnwi/professional gates return `unknown_fail_closed` until then; the interim shared-token identity model is now higher-stakes since CLT-01 stores real (if never-returned) PII; `start-review` and every other mutation route's actor attribution needs real IAM-02 identity/permission gating before it can be trusted; L1 (`CLT1_SERVICE_UNAVAILABLE` message accuracy for the CFG-01-unreachable case), L2 (weak actor attribution), and L3 (evidence/consent FK hardening) are tracked minor cleanups; class-claim verification, final approval, CDD/KYC/AML, authorised users/mandates, duplicate detection, related-party graph, ongoing monitoring, data protection/retention, and evidence export all remain fully out of scope, each needing its own planning pass.
- **CLT-01 Phase 2 accepted (CDD outcome gate + final approval control) — CLT-01's first real IAM-02 integration:**
  - migration `021_clt1_cdd_final_approval.cjs` added CDD/final-approval schema
  - migration `022_iam2_register_clt1_permissions.cjs` registered 5 IAM-02 CLT-01 permissions
  - `clt1.cdd_outcome`
  - `clt1.handoff_status`
  - `clt1.application_decision_request`
  - CDD outcome receipt only, no screening engine
  - handoff tracking only, no outbound KYC/AML call
  - IAM-02 `checkPermission` for review/reject/hold/outcome-status read
  - IAM-02 execute-verify maker-checker flow for approval only
  - all CLT-01 IAM-02 permissions `licence_locked=false`
  - zero `role_permission` seed
  - approval creates `client_profile` only as `active_limited`
  - reject/hold create no `client_profile`
  - client status route returns non-PII only
  - approval re-runs the CFG-01 onboarding gate
  - no CFG-01 verify-decision call
  - no raw CFG/IAM token stored
  - audit/outbox transaction-coupled
  - no public `/clt1/*` routes
  - no KYC/AML/sanctions screening implementation
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - **889/889 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium findings**
- **Carry-forward (non-blocking) heading into CLT-01 Phase 3:**
  - **L1**: approval failure audit event name/doc mismatch — reconcile the event taxonomy or the implementation notes in the next CLT-01 touch
  - **L2**: consider `UNIQUE(application_id)` on `client_profile` in the next `clt1` migration
  - **L3**: remove the unused `ctx` parameter in `createHandoff`, or add `request_id`/`correlation_id` columns to `handoff_status` if desired
  - residual approver-side SoD gap — IAM-02 `execute-verify` does not expose approver identity
  - interim shared-secret internal-identity model remains higher-risk (CLT-01 is a PII system of record)
  - actual KYC/AML/sanctions/PEP screening engines remain deferred
  - duplicate detection, authorised users, mandates, related-party graph, monitoring, data protection/retention, evidence export remain deferred
  - `client_profile` active/suspend/close routes remain deferred
  - no trading/wallet/deposit/withdrawal/settlement capability yet
- **CLT-01 Phase 3 accepted (authorised users + client mandate baseline) — CLT-01's second real IAM-02 integration:**
  - migration `023_clt1_authorised_users_mandates.cjs` added authorised-user and mandate schema
  - migration `024_iam2_register_clt1_phase3_permissions.cjs` registered 8 IAM-02 CLT-01 Phase 3 permissions
  - `clt1.authorised_user`
  - `clt1.client_mandate`
  - `clt1.authorised_user_decision_request`
  - `clt1.client_mandate_decision_request`
  - `UNIQUE(application_id)` added on `client_profile`
  - one-active-mandate-per-client partial unique index
  - authorised-user add/remove uses request/apply + IAM-02 execute-verify
  - authorised-user suspend/reactivate are single-step permission-gated
  - mandate create/update uses request/apply + IAM-02 execute-verify
  - all new CLT-01 IAM-02 permissions `licence_locked=false`
  - zero `role_permission` seed
  - client status route now includes `mandate_configured` and `authorised_users_configured`
  - `client_profile` remains `active_limited`
  - no active client status
  - no public `/clt1/*` routes
  - no `authorised_party`
  - no identity verification
  - no duplicate detection
  - no KYC/AML/screening engine
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - `user_reference` stored but not returned in default list
  - mandate rules validated with strict schema
  - **949/949 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium findings**
- **Carry-forward (non-blocking) heading into CLT-01 Phase 4:**
  - **F1**: map the one-active-mandate unique violation to a 409 conflict code instead of 503 `CLT1_AUDIT_REQUIRED` at the next CLT-01 touch
  - **F2**: correct `CLT1_MANDATE_INVALID_STATE` documentation wording to "present-but-defensive" (the throw sites exist, just untriggerable this phase)
  - **F3**: optional wording cleanup for "no PII, including `user_reference`" in the implementation notes
  - Phase 2 **L1**: approval failure audit event-name/notes mismatch remains deferred
  - Phase 2 **L3**: unused `ctx` parameter in `createHandoff` remains deferred
  - interim shared-token internal-identity model remains higher risk (CLT-01 is a PII system of record)
  - no real IAM-01 client-user linkage yet
  - residual approver-side SoD gap remains because IAM-02 `execute-verify` does not expose approver identity
  - CFG-01 feature-row operational runbook still required before real onboarding gates
  - `authorised_party`, identity verification, duplicate detection, related-party graph, monitoring, data protection/retention, evidence export remain deferred
  - no wallet/deposit/withdrawal/trading/settlement capability yet
- **CLT-01 Phase 4 accepted (authorised party + screening linkage baseline):**
  - migration `025_clt1_authorised_parties.cjs` added `clt1.authorised_party` and `clt1.authorised_party_decision_request`
  - migration `026_iam2_register_clt1_phase4_permissions.cjs` registered 8 IAM-02 CLT-01 Phase 4 permissions
  - `authorised_party` is application-scoped
  - no `client_id` column on `authorised_party`
  - no `linked_user_id` column
  - `party_type` narrowed to `signatory`/`director`/`controller`/`ubo`
  - `party_reference` stored, redacted, and excluded from default responses
  - screening linkage is receipt only
  - no screening engine
  - no identity verification engine
  - no duplicate detection
  - no related-party graph
  - add/update/remove/activate use request/apply + IAM-02 execute-verify
  - restrict/reject/suspend are single-step permission-gated
  - screening-outcome receipt is internal-identity-only and has no IAM-02 permission
  - `authorised_parties_configured` added to client status
  - Phase 2 approval gate unchanged
  - `client_profile` remains `active_limited`
  - no public `/clt1/*` routes
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - **1017/1017 tests passing**
  - Independent Opus review → **ACCEPT WITH MINOR CONDITIONS** (one Medium, two Low), all three fixed in a follow-up patch
  - Independent targeted Opus re-review → **CONDITIONS CLOSED — ACCEPT CLT-01 Phase 4**
- **Fixed findings (Opus Phase 4 review):**
  - **M1 fixed**: `authorised_party` `update/apply` now preserves omitted `ownership_percentage`/`sec_audit_ref` using `COALESCE` semantics (previously an unconditional overwrite could silently null an omitted field)
  - **L1 fixed**: `ownership_percentage` precision validation (`validateOwnershipPercentagePrecision`) prevents a payload-hash mismatch caused by scientific-notation rendering of sub-hundredths values — TypeBox's own `multipleOf` keyword was tried first and found unreliable (floating-point imprecision rejected ordinary values), replaced with a pure round-trip check
  - **L2 fixed**: `registerSingleStepTransition` now uses bound SQL parameters instead of interpolating hardcoded status literals
- **Carry-forward (non-blocking) heading into CLT-01 Phase 5:**
  - Phase 3 **F1**: the one-active-mandate unique violation should later map to a 409 conflict code instead of 503 `CLT1_AUDIT_REQUIRED`
  - Phase 2 **L1**: approval failure audit event-name/notes mismatch remains deferred
  - Phase 2 **L3**: unused `ctx` parameter in `createHandoff` remains deferred
  - interim shared-token internal-identity model remains higher risk
  - no real IAM-01 client-user linkage yet
  - residual approver-side SoD gap remains because IAM-02 `execute-verify` does not expose approver identity
  - CFG-01 feature-row operational runbook still required before real onboarding gates
  - identity verification / actual KYC/AML/sanctions/PEP screening engines remain deferred
  - duplicate detection remains deferred
  - related-party graph remains deferred
  - UBO-threshold enforcement remains deferred
  - monitoring, retention, evidence export remain deferred
  - `client_profile` active/suspend/close remains deferred
  - mandate-check endpoint remains deferred
  - wallet/deposit/withdrawal/trading/settlement capability remains deferred
- **CLT-01 Phase 5 accepted (related-party edge baseline):**
  - migration `027_clt1_related_party_edges.cjs` added `clt1.related_party_edge` and `clt1.related_party_edge_decision_request`
  - migration `028_iam2_register_clt1_phase5_permissions.cjs` registered 4 IAM-02 CLT-01 Phase 5 permissions
  - related-party edges are manually declared only
  - node types are client/application/party only
  - no authorised_user or client_mandate node type
  - relationship_type uses blueprint enum:
    - ubo
    - director
    - signatory
    - shared_identity
    - shared_address
    - associated_account
  - status is active/inactive only
  - add/update/remove use request/apply + IAM-02 execute-verify
  - read is single-step permission-gated
  - all new CLT-01 IAM-02 permissions `licence_locked=false`
  - zero `role_permission` seed
  - no `owning_application_id` or `owning_client_id`
  - exact active-edge duplicate prevention implemented
  - duplicate active edge maps to 409 `CLT1_RELATED_PARTY_EDGE_DUPLICATE`, not `CLT1_AUDIT_REQUIRED`
  - client-scoped read route is bounded single-hop only
  - no recursion, BFS, DFS, or graph traversal
  - no `related_parties_configured` client-status boolean
  - no Phase 2 approval-gate change
  - no duplicate-detection engine
  - no UBO-threshold enforcement
  - no screening engine
  - no public `/clt1/*` routes
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - **1065/1065 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
- **Carry-forward (non-blocking) heading into CLT-01 Phase 6:**
  - Phase 3 **F1**: the one-active-mandate unique violation should later map to a 409 conflict code instead of 503 `CLT1_AUDIT_REQUIRED`
  - Phase 2 **L1**: approval failure audit event-name/notes mismatch remains deferred
  - Phase 2 **L3**: unused `ctx` parameter in `createHandoff` remains deferred
  - direction-canonicalization for symmetric related-party edges remains deferred
  - duplicate-detection engine remains deferred
  - graph traversal remains deferred
  - UBO-threshold enforcement remains deferred
  - CDD-gate wiring for duplicate/related-party findings remains deferred
  - interim shared-token internal identity remains higher risk
  - no real IAM-01 client-user linkage yet
  - residual approver-side SoD gap remains because IAM-02 `execute-verify` does not expose approver identity
  - CFG-01 feature-row operational runbook still required before real onboarding gates
  - identity/KYC/AML/sanctions/PEP screening engines remain deferred
  - monitoring, retention, evidence export remain deferred
  - `client_profile` active/suspend/close remains deferred
  - mandate-check endpoint remains deferred
  - wallet/deposit/withdrawal/trading/settlement capability remains deferred
- **CLT-01 Phase 6 accepted (duplicate candidate review baseline):**
  - migration `029_clt1_duplicate_candidates.cjs` added `clt1.duplicate_candidate` and `clt1.duplicate_candidate_decision_request`
  - migration `030_iam2_register_clt1_phase6_permissions.cjs` registered 5 IAM-02 CLT-01 Phase 6 permissions
  - duplicate candidates are manually recorded/reviewed only
  - node types are application/client/party only
  - no authorised_user or client_mandate node type
  - match_type uses blueprint enum:
    - name
    - id
    - email
    - phone
    - corporate_ref
  - status uses blueprint enum:
    - open
    - duplicate
    - not_duplicate
    - needs_more_info
  - needs_more_info is schema-present but unreachable
  - no needs_more_info route or permission
  - no `CLT1_DUPLICATE_REVIEW_REQUIRED` throw site
  - source_type supports manual/future_detector, but only manual is written
  - match_score exists for blueprint fidelity but is never written by Phase 6 code
  - create/update/confirm/dismiss use request/apply + IAM-02 execute-verify
  - read is single-step permission-gated
  - all new CLT-01 IAM-02 permissions `licence_locked=false`
  - zero `role_permission` seed
  - directional status='open'-scoped uniqueness implemented
  - duplicate open tuple maps to 409 `CLT1_DUPLICATE_CANDIDATE_ALREADY_OPEN`
  - resolved tuple may be re-declared after duplicate/not_duplicate status
  - blueprint SoD rule 4 implemented for confirm/dismiss
  - L1 fixed: application-vs-application SoD now checks both application-typed sides
  - application-scoped and client-scoped read routes implemented
  - no joined PII returned
  - no duplicate-detection engine
  - no scoring engine
  - no fuzzy matching
  - no graph traversal
  - no merge/consolidation
  - no Phase 2 approval-gate change
  - no public `/clt1/*` routes
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - **1132/1132 tests passing**
  - targeted Opus re-review confirmed **CONDITIONS CLOSED — ACCEPT CLT-01 Phase 6**
- **Carry-forward (non-blocking) heading into CLT-01 Phase 7:**
  - approver-side SoD gap remains because IAM-02 `execute-verify` does not expose approver identity
  - interim shared-token declared-identity trust model remains
  - symmetric-pair direction-canonicalization remains deferred
  - `CLT1_DUPLICATE_REVIEW_REQUIRED` / CDD approval-gate wiring remains deferred
  - duplicate-detection engine remains deferred
  - scoring/fuzzy matching remains deferred
  - merge/consolidation remains deferred
  - graph traversal remains deferred
  - UBO-threshold enforcement remains deferred
  - screening/identity-verification engines remain deferred
  - Phase 3 F1: `client_mandate` duplicate still maps to 503 instead of 409
  - Phase 2 L1: approval failure audit event-name/notes mismatch remains deferred
  - Phase 2 L3: unused `ctx` parameter in `createHandoff` remains deferred
  - `client_profile` active/suspend/close remains deferred
  - mandate-check endpoint remains deferred
  - wallet/deposit/withdrawal/trading/settlement capability remains deferred
- **CLT-01 Phase 7 accepted (final approval compliance gate wiring):**
  - no new migration
  - no new IAM-02 permission
  - no new grant
  - no new route
  - `duplicate_candidate` is now wired into final application approval
  - approval blocks when touching duplicate candidates have status:
    - open
    - duplicate
    - needs_more_info
  - approval allows when no touching candidates exist or all touching candidates are not_duplicate
  - gate checks application:application_id
  - gate checks party:any authorised_party_id under the application
  - no client node check before approval
  - no related_party_edge direct approval block
  - no authorised_party screening approval block
  - no approval override
  - no duplicate detection engine
  - no scoring engine
  - no fuzzy matching
  - no merge/consolidation
  - no graph traversal
  - no UBO-threshold enforcement
  - no screening engine
  - CLT1_DUPLICATE_REVIEW_REQUIRED now has a reachable throw site
  - approval gate is checked at approve/request
  - approval gate is checked at approve/apply pre-check
  - approval gate is checked again inside the locked transaction before client_profile insert
  - duplicate gate is dynamic and not included in approval payload_hash
  - clt1.cdd_gate_denied renamed to clt1.application_approval_denied
  - reason_code remains specific
  - Phase 2 L1 closed
  - client_profile.status remains active_limited
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - **1152/1152 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
- **Carry-forward (non-blocking) heading into CLT-01 Phase 8:**
  - authorised-party screening does not block final approval
  - related_party_edge does not directly block approval
  - party-node branch of the duplicate gate is defensively present but organically unreachable until a future phase allows party configuration during review/re-review
  - no approval override
  - symmetric-pair direction-canonicalization remains deferred
  - duplicate-detection engine remains deferred
  - scoring/fuzzy matching remains deferred
  - merge/consolidation remains deferred
  - graph traversal remains deferred
  - UBO-threshold enforcement remains deferred
  - screening/identity-verification engines remain deferred
  - IAM-01 client-user linkage remains deferred
  - client_profile active/suspend/close remains deferred
  - mandate-check endpoint remains deferred
  - approver-side SoD gap remains
  - interim shared-token trust model remains
  - Phase 3 F1: client_mandate duplicate still maps to 503 instead of 409
  - Phase 2 L3: unused ctx parameter in createHandoff remains deferred
- **CLT-01 Phase 8 accepted (client profile lifecycle baseline):**
  - migration 031 added clt1.client_profile_lifecycle_decision_request
  - migration 032 registered 3 IAM-02 CLT-01 Phase 8 permissions
  - controlled client_profile lifecycle implemented
  - reachable statuses:
    - active_limited
    - suspended
    - closed
  - active/restricted/pending remain present in CHECK but unreachable
  - allowed transitions:
    - active_limited → suspended
    - suspended → active_limited
    - active_limited → closed
    - suspended → closed
  - closed is terminal
  - reactivate restores active_limited only
  - no active trading-ready status
  - suspend/reactivate/close use request/apply + IAM-02 execute-verify
  - all 3 new permissions require approval
  - all new permissions licence_locked=false
  - zero role_permission seed
  - total CLT-01 permissions now 33
  - no clt1.client_profile.read permission
  - no lifecycle-history route
  - no new client_profile columns
  - reason/evidence_ref stored only on lifecycle decision-request row
  - client_profile UPDATE grant limited to status/version/updated_at_utc
  - GET /internal/clt1/clients/:client_id/status unchanged and naturally reports suspended/closed
  - active_limited-gated downstream routes naturally reject suspended/closed
  - no close cascade into authorised_user/client_mandate/authorised_party
  - no reopen-from-closed path
  - no wallet/deposit/withdrawal/trading/settlement/Exchange runtime
  - no KYC/AML/screening engine
  - no duplicate-detection/scoring/fuzzy/graph/UBO engine
  - no public /clt1/* routes
  - **1198/1198 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
- **Carry-forward (non-blocking) heading into CLT-01 Phase 9:**
  - no lifecycle-history read route / clt1.client_profile.read permission
  - no status_reason/evidence_ref column on client_profile
  - no close cascade into child rows
  - no reopen-from-closed path
  - no approval override
  - authorised-party screening does not block final approval
  - related_party_edge does not directly block final approval
  - symmetric-pair direction-canonicalization remains deferred
  - duplicate-detection/scoring/fuzzy/merge/graph/UBO engines remain deferred
  - screening/identity-verification engines remain deferred
  - IAM-01 client-user linkage remains deferred
  - mandate-check endpoint remains deferred
  - approver-side SoD gap remains
  - interim shared-token identity model remains
  - Phase 3 F1: client_mandate duplicate still maps to 503 instead of 409
  - Phase 2 L3: unused ctx parameter in createHandoff remains deferred
  - wallet/deposit/withdrawal/trading/settlement/Exchange capability remains deferred
  - **Next: CLT-01 Phase 9 planning.**
- **CLT-01 Phase 9 accepted (module closure & final hardening baseline):**
  - closure and hardening only, no new business capability
  - no new migration
  - no new IAM-02 permission
  - no new grant
  - no new route
  - Phase 3 F1 closed: duplicate active mandate now maps to CLT1_MANDATE_ALREADY_ACTIVE 409, not 503 CLT1_AUDIT_REQUIRED
  - Phase 2 L3 closed: unused ctx parameter removed from createHandoff and both call sites
  - route surface finalized and exact-set tested via app.printRoutes()
  - permission inventory finalized: 33 clt1.* permissions (19 maker-checker + 14 single-step), all licence_locked=false, zero role_permission seed
  - grant inventory finalized: zero DELETE/TRUNCATE, zero cross-schema grant, exact column-scoped UPDATE lists confirmed
  - audit-event inventory finalized: full documented event set emitted, clt1.cdd_gate_denied permanently absent, no PII in any clt1.* payload
  - PII/safe-response sweep finalized: one route per family confirmed clean
  - no-Exchange/no-wallet/no-trading posture verified: client_profile.status never active/trading_enabled/wallet_enabled
  - no public /clt1/* routes
  - migrations remain 001→032, no migration 033
  - **1223/1223 tests passing**
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
  - TEST_DATABASE_URL explicitly set and independently verified (DB-gated test bodies confirmed genuinely executing, not silently skipping)
  - no code changed during the review
- **CLT-01 complete:**
  - accepted through Phase 9
  - all phases 0-9 independently reviewed and accepted
  - final test baseline 1223/1223
  - migrations remain 001→032
  - no migration 033
  - total CLT-01 IAM-02 permissions remain 33
  - no public /clt1/* routes
  - route surface finalized and exact-set tested
  - permission inventory finalized
  - grant inventory finalized
  - audit-event inventory finalized
  - PII/safe-response sweep finalized
  - no-Exchange/no-wallet/no-trading posture verified
  - Phase 3 F1 closed: duplicate active mandate now maps to CLT1_MANDATE_ALREADY_ACTIVE 409
  - Phase 2 L3 closed: unused ctx parameter removed from createHandoff
  - CLT-01 delivers baseline:
    - application intake
    - classification/consent
    - CDD outcome receipt
    - KYC/AML handoff status
    - maker-checker final approval
    - active_limited client_profile creation
    - authorised users
    - client mandates
    - authorised parties
    - screening-linkage receipt
    - related-party edges
    - duplicate-candidate review
    - duplicate-candidate approval gate
    - client_profile lifecycle suspend/reactivate/close
    - module closure/hardening
- **Carry-forward after CLT-01:**
  - lifecycle-history read route / clt1.client_profile.read remains future CLT enhancement
  - status_reason/evidence_ref columns on client_profile remain future CLT enhancement
  - close cascade remains future CLT enhancement
  - reopen-from-closed remains future CLT enhancement
  - symmetric-pair direction-canonicalization remains future CLT enhancement
  - approval override remains deliberate platform posture
  - approver-side SoD gap remains platform-wide limitation
  - interim shared-token identity model remains platform-wide limitation
  - screening/identity-verification engines remain future module dependency
  - duplicate-detection/scoring/fuzzy/graph/UBO engines remain future module dependency
  - IAM-01 client-user linkage remains future module dependency
  - mandate-check endpoint remains future module dependency
  - wallet/deposit/withdrawal/trading/settlement/Exchange remain future modules / locked scope
  - consider future audit-taxonomy pass for distinguishing conflict vs server-fault failure events
- **Recommended next runtime module candidate: AML-01, with KYC-01 as close alternative.** Reason: CLT-01 already has receipt-only seams for CDD outcomes, KYC/AML handoff status, authorised-party screening linkage, and approval gates — AML-01 is high-leverage because it can make screening and sanctions/PEP/adverse-media controls load-bearing.
- **CLT-01 — Accepted implementation baseline COMPLETE (Phases 0-9).**
- **CLT-01 — KYC-01 Phase 4A prerequisite (application-keyed KYC roster contract) also accepted**, added in a later session on top of the complete Phases 0-9 baseline, once KYC-01 Phase 4 planning identified the need — one new route, no migration/schema/grant/permission change, Opus-reviewed ACCEPT WITH LOW FINDINGS. Full detail in the KYC-01 section below (search "Phase 4A prerequisite").
- **Next: runtime module planning — AML-01 recommended, KYC-01 as close alternative. Do not start next-module coding until planning is confirmed.**
- **AML-01 Phase 0 accepted (service scaffold / boot guard / internal route baseline):**
  - services/aml1 scaffold created
  - own config loader created
  - own request-context plugin created
  - own internal-identity guard created
  - own Fastify buildApp created
  - boot-time no-Exchange guard active
  - exactly one route:
    GET /internal/aml1/health
  - health route is unauthenticated liveness
  - AML1_ERROR_CODES intentionally empty
  - no AML1_SERVICE_UNAVAILABLE yet
  - no readiness route
  - no schema
  - no migration
  - no grant
  - no role_aml1_runtime
  - no screening logic
  - no match/disposition logic
  - no CLT-01 callback
  - no CLT-01 HTTP client
  - no IAM-02 integration
  - no CFG-01 integration
  - no SEC-01 integration
  - no vendor integration
  - no external network call
  - no watchlist data
  - no public route
  - no wallet/deposit/withdrawal/trading/settlement/Exchange capability
  - import boundary forbids clt1/iam/iam2/sec1/cfg1/fnd internals
  - full suite 1261/1261 passing
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
- **Carry-forward (non-blocking) into AML-01 Phase 1:**
  - populate AML1_ERROR_CODES only when real reachable throw sites exist
  - add AML1_SERVICE_UNAVAILABLE only when first DB-querying route exists
  - consciously update Phase 0 forbidden-fragment tests when legitimate screening/match routes are added
  - create aml1 schema and role_aml1_runtime in Phase 1
  - keep deterministic stub adaptor as a strict boundary
  - no real vendor/watchlist/network calls
  - mandatory deterministic HIT fixtures in Phase 1
  - declared-identity provenance must be explicit
  - interim shared-token identity remains platform-wide carry-forward
  - no IAM-02 permissions until human disposition/maker-checker phase
  - no CLT-01 callback until later phase
- **AML-01 — accepted implementation baseline through Phase 0.**
- **Next: AML-01 Phase 1 planning. Do not start AML-01 Phase 1 coding until planning is confirmed.**
- **AML-01 Phase 1 accepted (point-in-time screening record & deterministic stub adaptor):**
  - aml1 schema created
  - role_aml1_runtime created (least-privilege)
  - migration 033_aml1_core.cjs
  - aml1_runtime_grants.sql applies clean after migration 033
  - four tables created:
    - aml1.screening_request
    - aml1.screening_subject_snapshot
    - aml1.screening_result
    - aml1.screening_match
  - routes added:
    - GET /internal/aml1/health
    - GET /internal/aml1/readiness
    - POST /internal/aml1/screening-requests
    - GET /internal/aml1/screening-requests/:screening_request_id
  - screening lifecycle runs synchronously in one transaction
  - deterministic synthetic screening fixtures:
    - sanctions hit
    - PEP hit
    - adverse-media hit
    - provider-down
    - clear case
  - safe response projection is PII-free
  - audit/outbox is transaction-coupled, source_module = AML-01
  - runtime grants are least-privilege (no DELETE/TRUNCATE, no cross-schema grant, UPDATE column-scoped to screening_request status/completed_at_utc/version only)
  - five reachable error codes added: AML1_SERVICE_UNAVAILABLE / AML1_SCREENING_REQUEST_NOT_FOUND / AML1_SCREENING_SUBJECT_INVALID / AML1_SCREENING_PROVIDER_UNAVAILABLE / AML1_AUDIT_REQUIRED
  - AML1_SCREENING_REQUEST_INVALID_STATE deliberately not added (no reachable throw site this phase)
  - date_of_birth validation gap found and fixed during self-review (malformed/impossible dates now rejected as 422 AML1_SCREENING_SUBJECT_INVALID before any DB write, not 503 AML1_AUDIT_REQUIRED)
  - no CLT-01 callback yet
  - no CLT-01 HTTP client yet
  - no IAM-02 permissions yet
  - no human disposition yet
  - no real vendor/watchlist/network integration
  - no wallet/transaction/trading/Exchange scope
  - **1317/1317 tests passing (74 files)**
  - `tsc -b --force` clean
  - migration 033 and aml1_runtime_grants.sql independently reproduced clean on a fresh disposable Postgres
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
- **Carry-forward (non-blocking) into AML-01 Phase 2:**
  - CLT-01 wiring: AML-01-owned CLT-01 HTTP client posting screening outcomes into CLT-01's existing receipt endpoints
  - human match disposition
  - first AML-01 IAM-02 permissions (licence_locked=false pattern, real-listening-IAM-02 guard tests)
  - first sensitive-read path (full match detail, IAM-02-gated)
  - AML1_SCREENING_REQUEST_INVALID_STATE may be introduced only when a real throw site exists
  - interim shared-token identity model remains platform-wide carry-forward
  - fetchSafeResponseOrThrow's 3-sequential-query pattern remains an acceptable, non-blocking future micro-optimization
- **AML-01 — accepted implementation baseline through Phase 1.**
- **AML-01 Phase 2A accepted (CLT-01 outcome delivery):**
  - migration 034_aml1_clt_outcome_delivery.cjs
  - aml1.screening_request gains nullable subject_parent_ref (INSERT-time only, not in the UPDATE grant)
  - aml1.clt_outcome_delivery table created
  - AML-01-owned CLT-01 HTTP client (lib/clt1-client.ts), F3(c)-clean, never imports services/clt1/**
  - CLT-01 outcome mapping library (lib/clt1-outcome-mapping.ts): dismissed is match-level only, never a result-level status
  - effective-status derivation: zero matches → clear; any confirmed_hit → confirmed_hit; any potential_match → potential_match; all dismissed → clear_after_review
  - application-level mapping: clear/clear_after_review → pass, potential_match → pending, confirmed_hit → hit; two deliveries per screen (aml_sanctions, pep_adverse_media)
  - authorised-party mapping: clear/clear_after_review → clear, potential_match → review_required, confirmed_hit → hit; never writes identity_verification_status
  - routes added:
    - POST /internal/aml1/screening-requests/:screening_request_id/clt-outcome
    - POST /internal/aml1/clt-outcome-deliveries/:delivery_id/retry
    - GET /internal/aml1/clt-outcome-deliveries/:delivery_id
  - two-phase delivery model: TX1 pending-insert + attempted audit, commit; HTTP call outside any transaction, timeout-bounded; TX2 terminal update + succeeded/failed audit, commit
  - crash between TX1 and TX2 leaves a retriable pending row
  - at-least-once delivery accepted; duplicate CLT-01 receipt is safe but noisy; CLT-01 was not modified to add idempotency
  - AML1_CLT_DELIVERY_FAILED made genuinely reachable in a micro-stabilization pass: both delivery/retry routes throw it (502) only AFTER TX2 has already durably committed the failed-delivery evidence; the row remains queryable via GET and retriable regardless
  - runtime grants extended: clt_outcome_delivery gets SELECT/INSERT + column-scoped UPDATE (status/attempt_count/failure_reason_code/response_ref/delivered_at_utc/version only); zero DELETE/TRUNCATE; zero grant into clt1 or any other module's schema; subject_parent_ref confirmed NOT in the screening_request UPDATE grant
  - real CLT-01 integration test provisions its own private, uniquely-named throwaway database per run (full migration chain + all 7 grant files, dropped in afterAll) — resolves a cross-file test-parallelism collision without touching any CLT-01 production or test code
  - real CLT-01 test proves the genuine under_review precondition and real rollup convergence (including an at-least-once duplicate-delivery case), not a stub-only positive path
  - no IAM-02, no human disposition, no sensitive-read route
  - no Exchange/wallet/deposit/withdrawal/trading/settlement scope
  - **1388/1388 tests passing (77 files)**
  - `tsc -b --force` clean
  - migration 001→034 + all 7 grant files independently reproduced clean on a fresh disposable Postgres, twice
  - full suite passed twice under default file-level parallelism (no --no-file-parallelism needed)
  - Independent **Opus review → ACCEPT WITH LOW FINDINGS** (zero Critical/High/Medium)
- **Carry-forward (non-blocking) into AML-01 Phase 2B:**
  - Low-1: delivered_status has no CHECK constraint, unlike sibling enum-like columns (not live-reachable — mapping library is a total function over a closed TypeScript union; column carries no UPDATE grant)
  - Low-2: failure_reason_code should be length-clamped before writing to varchar(64) (no live risk observed — longest real CLT-01 code is 40 chars — but fails only as a misleading AML1_AUDIT_REQUIRED rather than a clean rejection if ever exceeded)
  - Info-1: partial-failure response discards the succeeded delivery's ID; the failed one is recoverable only from free-text details[].issue, not a structured field
  - Info-2: clt_outcome_delivery's delivery_id index duplicates its own UNIQUE-created index (mirrors migration 033's own accepted precedent)
  - Info-3: real CLT-01 test leaves one cluster-global LOGIN role uncleaned, consistent with every other integration test file's existing convention
  - no CLT-01 callback beyond the two existing receipt endpoints
  - no human match disposition / maker-checker yet
  - no AML-01 IAM-02 permissions yet — first ones must be licence_locked=false from day one (the CFG-01 F-1 lesson), with real-listening-IAM-02 guard tests, never stub-only
  - no sensitive-read path for full match detail yet
  - screening_match still carries zero UPDATE grant — Phase 2B's disposition workflow needs its first deliberate column-scoped grant there, keeping matched_name/match_detail/score immutable
  - post-disposition re-delivery should reuse the existing Phase 2A delivery routes unchanged
  - interim shared-token identity model remains a platform-wide carry-forward
- **AML-01 — accepted implementation baseline through Phase 2A.**
- **AML-01 Phase 2B accepted (human match disposition + IAM-02 + sensitive read):**
  - migration 035_aml1_match_disposition.cjs
  - migration 036_iam2_register_aml1_permissions.cjs
  - aml1.screening_match gains its first-ever UPDATE grant, column-scoped to exactly match_status/reviewed_by/reviewed_at_utc/approval_id/version/updated_at_utc
  - matched_name/match_detail/score/category/list_source/screening_result_id/screening_match_id/created_at_utc remain permanently immutable
  - new table aml1.match_disposition_decision_request (request/apply binding table, mirrors CLT-01's duplicate_candidate_decision_request shape)
  - AML-01-owned IAM-02 client (lib/iam2-client.ts), F3(c)-clean, never imports services/iam2/**, 5s AbortSignal.timeout-bounded
  - 4 IAM-02 permissions registered:
    - aml1.screening.read (requires_approval=false)
    - aml1.screening.sensitive_read (requires_approval=false)
    - aml1.match.confirm (requires_approval=true)
    - aml1.match.dismiss (requires_approval=true)
  - all 4 permissions licence_locked=false from day one (the CFG-01 F-1 lesson applied proactively)
  - zero role_permission seed
  - verified against a real listening IAM-02 guard, not only a stub
  - routes added:
    - GET /internal/aml1/screening-requests/:screening_request_id/matches (PII-free match inventory — returns screening_match_id/category/match_status/reviewed_at_utc only)
    - GET /internal/aml1/matches/:screening_match_id/sensitive-detail (the ONLY AML-01 route ever returning matched_name/match_detail/score/list_source)
    - POST /internal/aml1/matches/:screening_match_id/confirm/request
    - POST /internal/aml1/matches/:screening_match_id/confirm/apply
    - POST /internal/aml1/matches/:screening_match_id/dismiss/request
    - POST /internal/aml1/matches/:screening_match_id/dismiss/apply
  - sensitive-detail write-before-return, same-transaction, fail-closed aml1.sensitive_match_detail_read audit
  - disposition follows the same request/apply + IAM-02 execute-verify pattern as CLT-01's own maker-checker tables
  - payload_hash snapshotted at request time, recomputed from the STORED row at apply time
  - fingerprint(token) stored, never the raw decision token
  - potential_match -> confirmed_hit/dismissed are both terminal, no re-open path
  - strict SoD: disposition requester must not equal the original screening request's own requested_by (AML1_SELF_DISPOSITION_BLOCKED)
  - combined with IAM-02's own unconditional requester != approver rule, a normal workflow needs three distinct identities: screener, disposition requester, approver
  - apply's concurrency guard uses SELECT...FOR UPDATE on both the decision row and the match row
  - post-disposition re-delivery is NOT automatic — apply returns redelivery_required:true + screening_request_id
  - caller must explicitly invoke the EXISTING Phase 2A POST .../clt-outcome route, unchanged
  - dismissed match re-delivers as clear_after_review/pass; confirmed_hit re-delivers as confirmed_hit/hit
  - closes Phase 2A Low-1: delivered_status CHECK constraint added
  - closes Phase 2A Low-2: failure_reason_code clamped to 64 chars before writing
  - exactly 8 new error codes, all with reachable throw sites
  - exactly 4 new transaction-coupled audit events: aml1.match_disposition_requested/_confirmed/_dismissed, aml1.sensitive_match_detail_read
  - no PII in any Phase 2B audit metadata
  - no real vendor/list/monitoring integration, no wallet/Travel Rule/deposit/withdrawal/trading/settlement/Exchange scope
  - **1464/1464 tests passing (80 files)**
  - `tsc -b --force` clean
  - migrations 001->036 + all 7 grant files independently reproduced clean on a fresh disposable Postgres, twice
  - full suite passed twice under default file-level parallelism
  - Independent **Opus review → ACCEPT WITH LOW FINDINGS** (zero Critical/High/Medium)
- **Carry-forward (non-blocking) into AML-01 Phase 3:**
  - Low-1: IAM-02 unreachability during execute-verify is reported as AML1_APPROVAL_REQUIRED/403 instead of AML1_IAM2_UNAVAILABLE/503 (fail-closed either way, no security impact — a diagnostics-accuracy nit)
  - Low-2: sensitive-detail checks match existence before the IAM-02 permission check, creating a low-risk existence oracle and materialising PII in-process before permission is established (opaque UUIDs, enumeration impractical)
  - Low-3: no regression test proving the disposition reason free text stays out of disposition audit metadata (holds by inspection, not pinned by a test)
  - Phase 2A Info-1: partial-failure response drops succeeded delivery IDs (unchanged, still deferred)
  - Phase 2A Info-2: redundant delivery_id index (unchanged, still deferred)
  - Phase 2A Info-3: real CLT-01 test leaves one cluster-global role uncleaned (unchanged, still deferred)
  - approver-side SoD remains an inherited platform-wide gap — IAM-02's execute-verify does not expose approver identity
  - the interim shared-token identity model remains a platform-wide carry-forward, now materially more consequential — Phase 2B is the first AML-01 surface returning sanctions/PEP/adverse-media PII and the first that mutates a compliance decision
  - AML-01 still screens on a deterministic stub adaptor — no closure review is appropriate until a real vendor/list adaptor boundary exists
- **AML-01 — accepted implementation baseline through Phase 2B.**
- **AML-01 Phase 3A accepted (micro-hardening — closes all three Phase 2B Low findings):**
  - Low-1 closed: IAM-02 execute-verify unavailability during confirm/dismiss apply now maps to AML1_IAM2_UNAVAILABLE/503, not AML1_APPROVAL_REQUIRED/403
  - same reason==="iam2_unavailable" ternary already used at every checkPermission site, applied to verifyDecisionToken's result
  - a genuine execute-verify rejection (tampered token, payload-hash mismatch) still correctly returns AML1_APPROVAL_REQUIRED/403
  - no mutation occurs in either failure case; decision row and screening_match both unaffected
  - Low-2 closed: sensitive-detail now calls IAM-02 checkPermission BEFORE fetching the screening_match row
  - unpermissioned caller requesting an unknown match now gets 403 AML1_PERMISSION_DENIED, not 404 — existence oracle closed
  - permissioned caller requesting an unknown match still gets 404 AML1_SCREENING_MATCH_NOT_FOUND
  - PII row is never materialised in-process before permission is established
  - write-before-return audit-before-return and fail-closed AML1_AUDIT_REQUIRED on audit failure unchanged
  - response projection unchanged: exactly matched_name/match_detail/score/list_source/category/match_status
  - Low-3 closed: new regression test proves disposition reason free text is stored as evidence in match_disposition_decision_request.reason but never appears in any aml1.match_disposition_* audit payload_ref
  - no migration added; highest migration remains 036
  - no schema change, no grant change, no route change, no permission change
  - route tree remains exactly 13 AML-01 routes
  - aml1.* IAM-02 permission count remains exactly 4
  - aml1 schema table count unchanged
  - role_aml1_runtime grant fingerprint confirmed byte-identical to Phase 2B baseline at the information_schema level
  - IAM-02 client itself not modified — only its two AML-01 consumer call sites
  - **1468/1468 tests passing (80 files)**
  - `tsc -b --force` clean
  - migrations 001->036 unchanged (no migration 037) + all 7 grant files independently reproduced clean on two independent fresh disposable Postgres databases
  - full suite passed twice under default file-level parallelism
  - Independent **Opus review → ACCEPT, zero Critical/High/Medium/Low findings**
- **Carry-forward (non-blocking) into AML-01 Phase 3B:**
  - Phase 2A Info-1: partial-failure response drops succeeded delivery IDs (unchanged, still deferred)
  - Phase 2A Info-2: redundant delivery_id index (unchanged, still deferred)
  - Phase 2A Info-3: real CLT-01 test leaves one cluster-global role uncleaned (unchanged, still deferred)
  - approver-side SoD remains an inherited platform-wide gap — IAM-02's execute-verify does not expose approver identity
  - the interim shared-token identity model remains a platform-wide carry-forward, increasingly consequential as AML-01's PII/compliance-mutation surface expands
  - NEW platform-wide carry-forward (identified by the Phase 3A Opus review): CLT-01 and CFG-01 still collapse ALL execute-verify failures — including genuine IAM-02 unavailability — into their own single _APPROVAL_REQUIRED-style code, at 10 combined call sites; AML-01 Phase 3A fixed this locally but the same latent gap remains open in both sibling modules
  - Phase 3B architectural prerequisite: the Phase 1 screening lifecycle (routes/screening.ts) must be restructured so a future provider call runs OUTSIDE the DB transaction (mirroring Phase 2A's own two-phase delivery model) before any real vendor/list adaptor boundary can land
  - AML-01 still screens on a deterministic stub adaptor — no closure review is appropriate until a real vendor/list adaptor boundary exists
- **AML-01 — accepted implementation baseline through Phase 3A.**
- **AML-01 Phase 3B accepted (two-phase screening lifecycle + provider adaptor boundary):**
  - migration 037_aml1_provider_attempt.cjs
  - migration 038_iam2_register_aml1_provider_permission.cjs
  - closes the Phase 3A architectural prerequisite: POST .../screening-requests is now two-phase
  - TX1: INSERT screening_request (requested) + screening_subject_snapshot (incl. subject_nature) + screening_provider_attempt (pending); publish aml1.screening_request_created; commit
  - provider call happens OUTSIDE any DB transaction, 5s timeout-bounded via Promise.race
  - TX2 success: attempt -> succeeded (provider_reference_id/provider_list_version/response_payload_hash/latency_ms/checked_at_utc set) + screening_result + screening_match rows + request -> completed; publishes aml1.screening_completed
  - TX2 failure (unavailable or invalid response): attempt -> failed (failure_reason_code clamped to 64 chars) + request -> failed; publishes aml1.screening_failed; no result/match row ever written
  - HTTP response contract unchanged, remains synchronous — no async/polling
  - structurally verified: provider call sits strictly between TX1's close and TX2's open (line-position check) plus a witness-provider integration test that queried the DB on a separate connection mid-call and observed TX1's committed requested/pending state
  - no retry/resume route added this phase (crash between TX1/TX2 leaves a resumable requested+pending shape, proven structurally, not functionally resumed)
  - new AML-01-owned provider adaptor boundary: lib/providers/types.ts (ScreeningProvider interface, ProviderScreeningOutcome union: screened/unavailable/invalid_response), lib/providers/stub-provider.ts (deterministic stub moved here byte-identical to its Phase 1-3A fixtures, plus 3 new fixtures: malformed response, unknown category, null score), lib/providers/registry.ts (screenViaProvider — the ONE invocation point, 5s timeout, throw/reject/timeout all map to unavailable, no always-clear fallback ever)
  - routes/screening.ts imports the registry only, never the stub directly and never a vendor SDK
  - AML1_SCREENING_PROVIDER config: defaults to the stub in dev/test, validated against the registry's known-id set, fail-closed on unknown id
  - production boot guard: ENVIRONMENT=prod + provider=stub fails closed as CONFIGURATION_INVALID at loadAml1Config (the real production entrypoint, index.ts, is fully covered)
  - subject_nature (individual/entity) is now a REQUIRED top-level body field, caller-supplied, never derived by AML-01, stored on screening_subject_snapshot
  - payload minimization: registration_number sent only for entity, date_of_birth/nationality only for individual; subject_ref/screening_request_id/requested_by/provenance never sent; raw provider payload never stored/returned/audited/logged — only fingerprint() hashes retained
  - screening_match.score is now NULLABLE — NULL means the provider supplied no confidence score, never a misleading 0.000 sentinel; handled end-to-end by sensitive-detail and disposition
  - normalization fails closed: unknown provider category -> AML1_VENDOR_RESPONSE_INVALID (502), never bucketed into adverse_media, never silently dropped
  - a provider can only ever produce clear/potential_match — confirmed_hit remains exclusively human-disposition's own output
  - new route: GET /internal/aml1/provider/status — returns active_provider_id/adaptor_version/environment/stub_provider only, no credentials/tokens/secrets, no live vendor health call
  - new IAM-02 permission: aml1.provider.read (licence_locked=false, requires_approval=false, zero role_permission seed) — AML-01's 5th permission
  - AML1_SCREENING_PROVIDER_UNAVAILABLE (503) reused unchanged; AML1_VENDOR_RESPONSE_INVALID (502) is the only new error code
  - no CLT-01 source touched; existing Phase 2A delivery route unchanged, still works for completed provider-generated results; failed requests remain non-deliverable
  - no Phase 3C code (no rescreening/monitoring/risk_signal), no real vendor SDK/client/credentials
  - route tree grows from 13 to 14; AML-01 IAM-02 permission count grows from 4 to 5
  - **1524/1524 tests passing (82 files)**
  - `tsc -b --force` clean
  - migrations 001->038 + all 7 grant files independently reproduced clean on two independent fresh disposable Postgres databases
  - down-migrations 038->037 independently verified — including a direct reproduction seeding a NULL-score row and confirming the rollback fails ATOMICALLY (no partial schema state, no data loss)
  - full suite passed twice under default file-level parallelism
  - Independent **Opus review → ACCEPT WITH LOW FINDINGS** (zero Critical/High/Medium)
- **Carry-forward (non-blocking) into AML-01 Phase 3C:**
  - Low-1: migration 037's down-migration restores screening_match.score SET NOT NULL unconditionally; if populated NULL scores exist, rollback past 037 fails — confirmed atomic/safe (no partial state, no data loss); operator note: rollback past migration 037 in a populated environment requires manual remediation of NULL scores (or a forward-fix plan) first
  - I-1: the production stub boot guard lives in loadAml1Config rather than buildApp; the real production entrypoint is fully covered, a buildApp-level second layer may be considered later as belt-and-braces
  - I-2: no new provider request/response log-redaction paths were added — deliberate, since the provider payload is never attached to any Fastify request/reply object Pino would serialize, so there is no req.*/res.* path to redact
  - I-3: AML1_SCREENING_PROVIDER_UNAVAILABLE (503) and AML1_VENDOR_RESPONSE_INVALID (502) use deliberately different HTTP statuses (retry-later vs upstream-untrustworthy) — re-confirm the semantics once a real vendor lands
  - Phase 2A Info-1/Info-2/Info-3 (unchanged, still deferred)
  - approver-side SoD remains an inherited platform-wide gap
  - the interim shared-token identity model remains a platform-wide carry-forward
  - CLT-01/CFG-01 still collapse execute-verify unavailability into approval-required-style errors (platform-wide, unchanged)
  - AML-01 still has no real vendor onboarded — no closure review is appropriate until one is live
- **AML-01 — accepted implementation baseline through Phase 3B.**
- **AML-01 Phase 3C accepted (re-screening triggers + route-triggered monitoring runs + risk-signal emission):**
  - migration 039_aml1_rescreen_monitoring.cjs
  - migration 040_iam2_register_aml1_phase3c_permissions.cjs
  - screening_request.rescreen_of_request_id (nullable) points a re-screen at its source; original request/result/match rows are never mutated
  - screening_request.trigger_reason (nullable) restricted to manual / periodic_due / list_version_changed only — kyc_profile_changed / transaction_triggered / remediation_check deliberately deferred
  - partial unique index prevents more than one in-flight (status='requested') screening request per subject_type + subject_ref — loop/concurrency-prevention backstop
  - aml1.monitoring_run table (route-triggered batches only, no scheduler/cron/queue)
  - aml1.risk_signal table (confirmed_hit / potential_match_unresolved / rescreen_overdue)
  - shared screening-execution core (lib/screening-execution.ts) extracted from the Phase 3B create route, split into beginScreening/completeScreening — reused unchanged by re-screen and by monitoring's own claim/complete split
  - re-screening helper + route: POST /internal/aml1/screening-requests/:screening_request_id/rescreen — copies declared-identity PII from the source snapshot, caller cannot resupply it; provenance/subject_nature carried forward
  - monitoring helper + routes: POST /internal/aml1/monitoring-runs, GET /internal/aml1/monitoring-runs/:run_id
  - risk-signal helper + routes: GET /internal/aml1/risk-signals, POST /internal/aml1/risk-signals/:signal_id/acknowledge
  - four new IAM-02 permissions: aml1.rescreen.request, aml1.monitoring.run, aml1.risk_signal.read, aml1.risk_signal.acknowledge — all licence_locked=false, zero role_permission seed
  - concurrency safety: pg_advisory_xact_lock spans candidate SELECTION *and* per-candidate CLAIMING, not just the SELECT — an earlier design that locked only the SELECT failed its own concurrency test and was redesigned before acceptance; provider calls run unlocked, after claiming
  - confirmed_hit disposition emits a critical, open risk signal atomically inside the existing confirm-apply transaction; duplicate open signals are suppressed by dedup
  - AML-01 emits risk signals only — no freeze/block/debit/settle/trade/downstream-module mutation, no grant into any downstream module's schema
  - no CLT-01 source touched, no new CLT-01 delivery target
  - route tree 14 → 19; AML-01 IAM-02 permission count 5 → 9
  - **1581/1581 tests passing (82 files)**
  - `tsc -b --force` clean
  - migrations 001→040 + all 7 grant files independently reproduced clean on two independent fresh disposable Postgres databases; down-migrations 040→039 verified clean, up again reproduces an identical schema
  - full suite passed twice on two independent fresh databases (a same-database re-run instead surfaces the known, pre-existing, unrelated IAM-01/IAM-02/CFG-01 bootstrap/seed-state re-run artifact — zero AML-01 failures, not a Phase 3C regression)
  - Independent **Opus review → ACCEPT WITH FINDINGS** (1 Medium, 2 Low, 3 Informational; zero Critical/High)
- **MEDIUM-1 — mandatory AML-01 Phase 3D hardening item, not an ordinary carry-forward:** a crash between the claim (TX1) and complete (TX2) steps of a re-screen/monitoring attempt can leave `screening_request.status='requested'` stuck. Migration 039's own partial unique index then permanently blocks any further re-screen of that subject (manual or monitoring); monitoring candidate selection excludes the subject because its latest row is no longer `completed`; no `rescreen_overdue` signal is ever emitted because the subject is never selected as a candidate. Empirically reproduced by the independent Opus reviewer (a seeded stuck row was shown to be invisible to candidate selection, blocked from manual re-screen by both the application guard and the DB constraint, and absent from `rescreen_overdue` signals). Recovery today requires direct DBA intervention. **Must be closed — stuck-request detection, resume/cancel/fail handling, and an operator runbook/detection query, or equivalent — before Phase 4 / real vendor / production use**; real-vendor latency and outages will only widen the crash window.
- **Carry-forward (non-blocking) into AML-01 Phase 3D:**
  - LOW-1: the code comment justifying the `createMonitoringRun`-before-claim-loop reorder in `routes/monitoring.ts` states an incorrect rationale (the reorder itself is beneficial, for a different reason than stated) — comment-only correction
  - LOW-2: `AML1_MONITORING_RUN_INVALID_STATE` is a real, tested throw site only at the library level (`finalizeMonitoringRun`) — not organically reachable via the HTTP route in normal operation; document as present-but-defensive (mirrors CLT-01 Phase 3's own F2 wording for an identical shape)
  - Info-1: match-keyed duplicate suppression is effectively unreachable for confirmed_hit (a match can only ever be confirmed once) and for potential_match_unresolved (each re-screen inserts new screening_match rows, so repeated signals across re-screens are expected — new evidence each time, not a leak)
  - Info-2: selectListVersionChangedCandidates has no attempt-level tiebreaker — deterministic today (one attempt per request), revisit if a future retry/resume phase introduces multiple attempts per request
  - Info-3: the re-screen path carries provenance forward without re-running the WRITABLE_SCREENING_PROVENANCE check — safe today since only declared_identity is ever written, but is the one code path that would propagate kyc_verified_identity forward if a row ever acquired it; revisit once KYC-01 lands
  - Phase 3B Low-1 (migration 037 down-migration blocked by populated NULL scores), I-1 (stub boot guard lives in loadAml1Config not buildApp), I-2 (no new provider log-redaction paths needed), I-3 (503-vs-502 provider-failure semantics to reconfirm once a real vendor lands) — all unchanged
  - Phase 2A Info-1/Info-2/Info-3 (unchanged, still deferred)
  - approver-side SoD remains an inherited platform-wide gap — IAM-02's execute-verify does not expose approver identity
  - the interim shared-token identity model remains a platform-wide carry-forward
  - CLT-01/CFG-01 still collapse execute-verify unavailability into approval-required-style errors (platform-wide, unchanged)
  - AML-01 still has no real vendor onboarded — no closure review is appropriate until one is live
- **AML-01 — accepted implementation baseline through Phase 3C.**
- **AML-01 Phase 3D accepted (stuck requested-screening detection / orphan-claim hardening — closes Phase 3C's own MEDIUM-1):**
  - migration 041_iam2_register_aml1_phase3d_permissions.cjs — **permissions-only, no aml1 schema DDL**
  - TX2 concurrency guard (lib/screening-execution.ts): `completeScreening` now opens with `SELECT status FROM screening_request ... FOR UPDATE` as its FIRST statement, before any screening_result/screening_match INSERT — aborts cleanly (`{kind:"aborted"}`) if the row is no longer `requested`; same row lock the recovery transaction takes, so the two race-safely serialize in either direction
  - new lib/stuck-screening.ts + routes/stuck-screening.ts
  - GET /internal/aml1/screening-requests/stuck — IAM-02-gated (aml1.screening.stuck_read), bounded (max 200), PII-free safe projection, min_age_seconds can only RAISE the effective threshold, never lower it below the configured floor
  - POST /internal/aml1/screening-requests/:screening_request_id/recover — IAM-02-gated (aml1.screening.stuck_recover), single-step, no maker-checker; asserts a GENUINE permission_granted baseline — approval_required/step_up_required do NOT authorise recovery
  - reason_code is a closed enum (process_crash_orphan / provider_call_abandoned / deployment_interruption / manual_operator_recovery) — no free text ever accepted or stored
  - recovery marks screening_request AND its latest screening_provider_attempt both `failed` (failure_reason_code = "stuck_recovery_<reason_code>", reusing existing Phase 1/3B grants — zero new grant needed); never deletes evidence; never auto-starts a re-screen (recovery and re-screen stay separate, independently auditable actions)
  - monitoring gains a stuck-detection VISIBILITY pass (both trigger reasons), inside the existing locked transaction — reuses rescreen_overdue (no schema change) + new aml1.stuck_screening_detected event; never counted in candidates_selected/rescreens_created/failures; never claims/mutates/auto-recovers a stuck subject
  - new config AML1_STUCK_SCREENING_THRESHOLD_SECONDS — default 900s, HARD FLOOR 300s (below the floor fails startup closed as CONFIGURATION_INVALID), no per-request override
  - two new IAM-02 permissions: aml1.screening.stuck_read, aml1.screening.stuck_recover — both licence_locked=false, zero role_permission seed
  - three new error codes: AML1_STUCK_SCREENING_NOT_FOUND / _INVALID_STATE / _TOO_FRESH
  - two new audit events: aml1.stuck_screening_detected, aml1.stuck_screening_recovered — no PII, no free text (closed reason_code enum makes a free-text leak structurally impossible)
  - standalone operator runbook added: docs/implementation/AML-01_PHASE_3D_STUCK_SCREENING_RUNBOOK.md
  - closes Phase 3C's own LOW-1 (routes/monitoring.ts comment corrected) and LOW-2 (AML1_MONITORING_RUN_INVALID_STATE documented as present-but-defensive / library-level reachable)
  - grants **unchanged** — independently confirmed byte-identical to the Phase 3C baseline via information_schema (308 privilege rows, zero drift)
  - route tree 19 → 21; AML-01 IAM-02 permission count 9 → 11; error code count 22 → 25
  - **1620/1620 tests passing (82 files)**
  - `tsc -b --force` clean
  - migrations 001→041 + all 7 grant files independently reproduced clean on a fresh disposable Postgres; migration 041's own down-migration independently verified to restore the catalogue to exactly 9 aml1.* permissions with zero stuck_* rows remaining
  - the highest-risk item — the TX2 concurrency guard — was independently re-proven under GENUINE concurrency (a real blocking provider call racing a real recovery commit, and a real held-row-lock scenario racing recovery against a winning terminal update), not by re-running the implementer's own hand-edited-state test
  - the known same-DB re-run artifact (iam-db/iam2-db/cfg1-db test-isolation issues, unrelated to AML-01) was independently reproduced and reconfirmed — zero AML-01 failures on either a fresh or a reused database
  - Independent **Opus review → ACCEPT WITH LOW FINDINGS** (2 Low, 5 Informational; zero Critical/High/Medium)
- **MEDIUM-1 (Phase 3C) is now CLOSED:** a stuck subject is detected, signalled, and recoverable via a single-step, IAM-02-gated operator action; recovery unblocks a future manual re-screen; a late-landing provider TX2 cannot resurrect a recovered request nor write orphaned evidence; recovery never deletes evidence, never produces a clear/pass outcome, is never deliverable to CLT-01, and never mutates any downstream module.
- **Carry-forward (non-blocking) into whatever comes next:**
  - LOW-1: the operator runbook originally implied a `periodic_due` monitoring cycle could trigger the required follow-up re-screen — independently DISPROVED by direct test (a just-recovered subject's latest status is `failed`, not `completed`, so no AML-01 detection surface ever selects it) — corrected in this same documentation-sync pass
  - LOW-2: the recovery route's staleness gate uses the application clock against the DB-stored `created_at_utc`, while detection uses the DB clock — accepted as Low (requires skew exceeding the 300s hard floor, cannot produce a clear/pass outcome, TX2 guard prevents evidence corruption regardless); future hardening option: a DB-clock predicate for this one gate
  - Info-1: a post-recovery detection blind spot remains by design (a recovered request's `failed` status is selected by no detection surface) — mitigated, not eliminated, by the lingering open `rescreen_overdue` signal and the runbook's mandatory manual-follow-up instruction
  - Info-2: an `aborted` TX2 outcome counts as a `failures` increment in monitoring's Phase B loop — effectively unreachable in production (recovery requires ≥300s, a monitoring claim is seconds old)
  - Info-3: the latest-attempt tie-break is nondeterministic if two attempts share a timestamp — unreachable today (one attempt per request); a future retry/resume phase must add a deterministic tie-break
  - Info-4: one integration test's own comment is self-contradictory/brittle — no runtime issue
  - Info-5: the runbook's manual-follow-up instruction is correct post-fix but not yet covered by an automated test
  - Phase 3C Info-1/Info-2/Info-3 (unchanged); Phase 3B Low-1/I-1/I-2/I-3 (unchanged); Phase 2A Info-1/Info-2/Info-3 (unchanged, still deferred)
  - approver-side SoD remains an inherited platform-wide gap — IAM-02's execute-verify does not expose approver identity
  - the interim shared-token identity model remains a platform-wide carry-forward
  - CLT-01/CFG-01 still collapse execute-verify unavailability into approval-required-style errors (platform-wide, unchanged)
  - the same-DB re-run test artifact in iam-db/iam2-db/cfg1-db remains unrelated to AML-01 (confirmed twice now)
  - AML-01 still has no real vendor onboarded — no closure review is appropriate until one is live
- **AML-01 — accepted implementation baseline through Phase 3D.**
- **AML-01 pauses after Phase 3D — no further AML-01 hardening phase is planned at this time.** Real vendor onboarding remains Phase 4, still blocked on procurement, DPA, vendor credentials, real provider selection, production secret management, and vendor governance/outsourcing review. AML-01 closure review remains unavailable until a real provider is live AND Phase 4 is accepted AND the real-vendor path has itself been independently reviewed.
- **KYC-01 KYC/KYB Verification selected as the next runtime module** (the close alternative named alongside AML-01 at the prior decision point; CLT-01 already exposes a receipt-only CDD-outcome/handoff-status seam this module makes load-bearing). **KYC-01 Phase 0 (scaffold) accepted**: new service `services/kyc1` (Fastify), structurally identical to every prior module's own Phase 0 — own request-context plugin, own interim internal-identity guard (`KYC1_INTERNAL_SERVICE_TOKEN`, fail-closed), own config loader, boot-time `assertNoExchangeRuntime`, F3(c) import-boundary test, one route (`GET /internal/kyc1/health`). No schema, migration, grants, or business logic this phase.
- **KYC-01 Phase 1 (core CDD baseline) accepted:**
  - migration `042_kyc1_core.cjs` — `kyc1` schema + `role_kyc1_runtime` + 4 tables
  - `infra/grants/kyc1_runtime_grants.sql`
  - `kyc1.kyc_case` (partial unique index — at most one active case per anchor)
  - `kyc1.document_checklist_item` (deterministic default checklist per case type — a documented Phase 1 placeholder, not a final taxonomy)
  - `kyc1.verification_result` (append-only manual/registry findings; `vendor` source type reserved, unimplemented)
  - `kyc1.cdd_outcome` (append-versioned, never updated in place)
  - deterministic pure-function CDD outcome engine (`computeCddOutcome`) — no human override, no maker-checker, no AML/vendor/biometric input this phase
  - opaque evidence-reference handling — never raw document content, never base64/`data:` inline bytes
  - 10 routes total (health/readiness/handoffs/cases-read×2/evidence/checklist/verification-results/compute-outcome/outcome-read)
  - 9 error codes; 10 audit events, transaction-coupled via FND's `publishAudit`/outbox
  - PII-minimal safe-response projections throughout (no raw row spreading anywhere)
  - zero IAM-02 `kyc1.*` permissions this phase, zero `role_permission` seed
  - no CLT-01 outcome-delivery call, no UBO/EDD/proofing/vendor/periodic-review tables, no real document store, no Phase 2/3 code
  - **1755/1755 tests passing**
  - Independent **Opus review → ACCEPT WITH FINDINGS** (1 Medium — MED-1 — plus Low/Informational; zero Critical/High)
- **MED-1 micro-patch accepted (closes the Phase 0+1 review's sole Medium finding):** `computeCddOutcome` was non-deterministic under concurrent verification-result submission — `latestResultOfType` sorted only by `receivedAtUtc`, the comparator never returned `0`, SQL retrieval order also lacked a tie-break, and because `pass` is terminal this phase, a nondeterministically-wrong `pass` was not correctable through the API. Fix: SQL retrieval reordered to `received_at_utc DESC, verification_result_id DESC`; `computeCddOutcome`'s `OutcomeVerificationResultInput` gained `verificationResultId`; a new `compareByRecencyThenId` comparator sorts by `receivedAtUtc` DESC then `verificationResultId` DESC, returning `0` only on true full-key equality. **A second, deeper bug was self-caught by Sonnet during the same patch, before any report**: the first fix attempt used `!==` to detect a tie, which is always `true` for two distinct `Date` objects at the identical instant (`node-postgres` returns `timestamptz` as `Date`, not the `string` the row interfaces declare) — silently never reaching the tie-break branch against real DB data. Caught by the new DB-integration tests (invisible to pure-string unit tests), fixed by using only `<`/`>` throughout, pinned with a dedicated `Date`-object regression test. No business rule changed; `pass` remains terminal by design; no manual override/IAM-02/CLT-01 delivery added, per explicit scope. Files changed: `lib/outcome-engine.ts`, `lib/kyc-case.ts`, `routes/outcome.ts`, plus the two test files. No route added, no migration added (no `042` was superseded and no `043` was created), no grant changed. **135/135 KYC-01 tests, 1755/1755 full suite, `tsc -b --force` clean.**
- **Independent Opus review of the MED-1 patch → MED-1 CLOSED.** Independently re-derived from source and re-proven empirically (not rubber-stamped): read the three changed files directly, rebuilt a genuinely fresh disposable Postgres, re-ran migrations 001→042 + all 8 grant files, reproduced **1755/1755** and `tsc -b --force` clean, and ran a standalone probe generating 2,000 random tied/untied verification-result sets evaluated across **every permutation** of array order — **0 order-dependence violations**. The `pg`-`Date`-object hazard was independently reproduced directly against the scratch DB and confirmed resolved in both directions. **Scoping caveat recorded, not a defect**: the fix closes the non-determinism completely, but the sticky-wrong-`pass` half of the original finding is closed only in the weaker sense that the winner is now *reproducible*, not *correct-by-construction* — the tie-break key (`verificationResultId`) is a random UUID, so a genuine same-instant conflicting tie still resolves by chance, and `pass` remains uncorrectable through the API by explicit instruction. Tracked as INFO-6, a Phase 2 gate, not a missed fix.
- **Carry-forward (non-blocking) heading into KYC-01 Phase 2:**
  - LOW-1: `validateEvidenceRef`'s base64 heuristic is imprecise (MIME-with-newlines false-negative; long legitimate slash-delimited keys false-positive) — bounded impact, `data:`/length checks still apply independently
  - LOW-2: concurrent evidence POST for a not-yet-existing `document_type` can surface as `KYC1_AUDIT_REQUIRED`/503 instead of 409 — fails closed, no corruption
  - LOW-3: mixed `Date`/`string` `receivedAtUtc` inputs collapse recency comparison to a tie — unreachable today (the sole production caller always supplies `pg`-returned `Date` values)
  - LOW-4: the SQL-retrieval-order DB test asserts its own literal query, not the production `fetchVerificationResults` query — defence-in-depth only, the engine re-sorts internally regardless
  - INFO-1: `timestamptz` columns are typed `string` in row interfaces at runtime, `pg` returns `Date` — now a demonstrated correctness hazard (LOW-3's root cause), must be considered before Phase 2
  - INFO-2: a new handoff is permitted for an anchor whose prior case already reached pass/fail — Phase 2 must define which outcome is authoritative before CLT-01 delivery
  - INFO-3: the AML-01 micro-patch precedent this session followed depends on every AML-01 migration filename containing `aml1` — verified true today
  - INFO-4: `document_type` is an unconstrained `varchar`, accepted as a Phase 1 placeholder pending the blueprint's own final taxonomy decision
  - INFO-5: `evidence_refs.verification_result_ids` persisted order flipped to newest-first as a direct, harmless consequence of the MED-1 fetch-order change
  - INFO-6: the tie-break key is stable but non-monotonic/random; the terminal-`pass` correction path remains unresolved — must be addressed before Phase 2 CLT-01 outcome delivery
  - **Phase 2 gates, must be explicitly decided before Phase 2 coding starts**: (1) authoritative-outcome/supersession rule; (2) a correction path for a terminal `pass`; (3) the `timestamptz` typing mismatch, ideally resolved repo-wide; (4) whether the MED-1 scoping caveat needs a dedicated Phase 1B/2A hardening slice before Phase 2 or can proceed alongside it; (5) CLT-01 handoff/outcome delivery mechanics themselves — undecided, no code exists yet
  - interim shared-token internal-identity model remains a platform-wide carry-forward
  - approver-side SoD remains an inherited platform-wide gap
- **KYC-01 — accepted implementation baseline through Phase 1.**
- **KYC-01 Phase 2A (authoritative outcome + publication model, no CLT-01 delivery) accepted.** Deliberately split from Phase 2B so aggregation/publication semantics could be independently reviewed before any CLT-01 delivery call exists:
  - timestamp normalization at the KYC-01 DB boundary (`normalizeTimestamp`, applied at every fetcher and inline route query — closes Phase 1's INFO-1/LOW-3; KYC-01-scoped only, not repo-wide)
  - `lib/authoritative-outcome.ts`'s `computeAuthoritativeOutcome` — application-scoped (never case-scoped) worst-wins aggregation across every KYC-01 anchor an application holds: `fail` beats `remediation_required` beats `pass`; a missing/uncomputed PRIMARY (`individual`/`entity`) anchor makes the whole aggregate `pending` (not publishable); an uncomputed PARTY anchor folds in as `remediation_required` rather than blocking; within an anchor the latest case WITH A COMPUTED OUTCOME wins (`createdAtUtc` DESC, `caseId` DESC tie-break — correctly treats `remediation_required` as having an outcome, not gated on `status='completed'`)
  - `detectTiedConflictingEvidence` — refuses a would-be `pass` publication (`KYC1_OUTCOME_EVIDENCE_CONFLICT`, 422, no row created) when contributing evidence contains a tied conflicting pass/fail pair — the direct D4/INFO-6 remedy
  - migration `043_kyc1_outcome_publication.cjs` — one new table, `kyc1.outcome_publication`; `aggregate_status` CHECK excludes `pending` (structurally never persisted); `status` CHECK carries `pending`/`succeeded`/`failed`/`superseded`, a documented D6 forward-compatibility exception for the CHECK values only
  - `POST /internal/kyc1/applications/:application_id/publish-outcome` — application-scoped, internal-identity-only, no IAM-02; supersedes any prior active publication before inserting a new `pending` row
  - `GET /internal/kyc1/outcome-publications/:publication_id` — safe projection, `payload_hash` excluded
  - `payload_hash` server-computed and deterministic, never exposed in any response
  - audit events: `kyc1.outcome_publication_requested`/`_superseded`/`_refused` (10→13)
  - 3 new error codes: `KYC1_OUTCOME_NOT_PUBLISHABLE`/`_EVIDENCE_CONFLICT`/`_PUBLICATION_NOT_FOUND` (9→12) — a fourth, `KYC1_OUTCOME_PUBLICATION_INVALID_STATE`, was pre-registered then REMOVED by a pre-review micro-patch for having no Phase 2A throw site
  - no CLT-01 client, no CLT-01 HTTP call, no retry route, no IAM-02, no maker-checker
  - route count 10→12, table count 4→5
  - **186/186 KYC-01 tests, 1806/1806 full suite (91 files), `tsc -b --force` clean, migrations 001→043 + all 8 grant files clean, 043 down/up round-trip clean, no migration 044**
  - Independent **Opus review → ACCEPT WITH LOW FINDINGS** (3 Low, 3 new Informational; zero Critical/High/Medium) — every headline number independently reproduced from a fresh disposable Postgres, including live grant-boundary proof under the real `role_kyc1_runtime` (every immutable `outcome_publication` column individually confirmed UPDATE-denied) and empirical reproduction of both Low findings below via concurrent/adversarial HTTP probes, not just static reading
- **Carry-forward (non-blocking) heading into KYC-01 Phase 2B:**
  - LOW-5: concurrent `publish-outcome` calls can surface as 503 `KYC1_AUDIT_REQUIRED` instead of a 409-class conflict (empirically reproduced: 4/10 concurrent calls in a 5-trial probe) — state integrity holds (partial unique index prevents two active rows), fails closed, same shape as the still-open Phase 1 LOW-2/CLT-01 Phase 3 F1 — fix with the CFG-01 Phase 3B `pg_advisory_xact_lock` idiom before Phase 2B retry/delivery
  - LOW-6: `detectTiedConflictingEvidence` groups across ALL contributing cases, not within one case — empirically reproduced (two internally-consistent cases sharing one timestamp/opposing status wrongly refuse a publication); fail-safe (over-refuses, never wrongly publishes) — fix by adding `caseId` to the grouping key before Phase 2B
  - LOW-7: a corrective same-anchor case is invisible to the aggregate while its own outcome is uncomputed — empirically reproduced (a stale terminal `pass` remains publishable during the correction window) — must be an explicit Phase 2B gate (block vs. downgrade), not a silent carry-forward
  - INFO-7: `fetchCasesForApplication` has no `LIMIT` — bounded in practice by realistic party/case counts
  - INFO-8: `contributingOutcomeIds` casts `currentOutcomeId as string`, relying on the current_outcome_status/current_outcome_id write-together invariant — defensive-only
  - INFO-9: migration 043's down/up round-trip drops `outcome_publication`'s grants, requiring `kyc1_runtime_grants.sql` to be re-applied afterward — documented platform convention, operator-note only
  - Phase 1 LOW-1 (evidence-ref base64 heuristic imprecision), LOW-2 (concurrent evidence POST 503-vs-409), LOW-4 (SQL-order test gap) — unchanged
  - INFO-3 (AML-01 migration naming-convention dependency), INFO-4 (unconstrained `document_type` placeholder), INFO-5 (evidence_refs newest-first order) — unchanged
  - D1 limitation unchanged: KYC-01 publishes over its own case set only and cannot prove CLT-01's authorised-party roster is complete
  - INFO-2 (multiple completed cases / authoritative outcome) is now **substantially addressed** by Phase 2A's aggregation/supersession rule — only the CLT-01-roster-completeness half (D1) remains open
  - INFO-6 (non-monotonic tie-break) is now **materially improved** by the evidence-conflict refusal — the random tie-break key itself is unchanged, but the ambiguous `pass` it could produce is now refused at publication
  - repo-wide `timestamptz` typing outside KYC-01 remains open as a separate platform-hardening item
  - **Phase 2B gates, must be explicitly decided before Phase 2B coding starts:** (1) LOW-7's correction window — decide whether an in-flight corrective case must block or downgrade publication; (2) LOW-5's concurrent-publish conflict signalling — fix via advisory lock or equivalent before retry/delivery; (3) LOW-6's evidence-conflict grouping — include `caseId` in the grouping key; (4) CLT-01 delivery design — two-phase delivery with the HTTP call outside the DB transaction, mirroring AML-01 Phase 2A; (5) CLT-01 lifecycle handling — a non-`under_review` application must be recorded as a failed delivery, never a false success; (6) retry semantics — `pending`/`failed` publications may retry, `succeeded`/`superseded` must reject; (7) no IAM-02/maker-checker unless separately justified
- **KYC-01 — accepted implementation baseline through Phase 2A.**
- **KYC-01 Phase 2B (CLT-01 outcome delivery + LOW-5/6/7 pre-delivery closures) accepted.** Mandatory ordering followed exactly as planned — all three Phase 2A Low findings closed and independently green BEFORE any CLT-01 delivery code was written:
  - **LOW-6 closed**: `detectTiedConflictingEvidence`'s grouping key gained `caseId` (now `caseId::resultType::receivedAtUtc`) — a tie is detected only within one case's own evidence; cross-case same-timestamp/opposing-status pairs across two internally-consistent cases no longer falsely block publication, while a genuine same-case tie still blocks
  - **LOW-7 closed**: within an anchor the latest case now wins outright, not "latest case with an outcome" — an in-flight corrective primary case (no outcome yet) blocks publication (`KYC1_OUTCOME_NOT_PUBLISHABLE`); a corrective party case downgrades the aggregate to `remediation_required`; a corrective case that DOES have an outcome still supersedes normally (unchanged from Phase 2A)
  - **LOW-5 closed**: `pg_advisory_xact_lock(hashtext('kyc1.outcome_publication:'+application_id))` as the first statement in `publish-outcome`'s transaction and in both of `deliver`'s (TX1/TX2) — never held across the CLT-01 HTTP call; concurrent publish now serializes instead of racing the partial unique index into a misleading `KYC1_AUDIT_REQUIRED`/503
  - KYC-owned CLT-01 client: `services/kyc1/src/lib/clt1-client.ts` — F3(c)-clean (no import from AML-01's own `clt1-client.ts`), no CLT-01 source change, explicit status map (`pass`→`pass`/`fail`→`fail`/`remediation_required`→`remediation_required`, never a pass-through), `CLT1_BASE_URL`/`CLT1_INTERNAL_SERVICE_TOKEN` required and fail-closed, 5s timeout-bounded, no token/raw-request/raw-response logging, `response_ref` stores only CLT-01's own `outcome_id`, `failure_reason_code` clamped to 64 chars
  - `POST /internal/kyc1/outcome-publications/:publication_id/deliver` (13th route, 12→13) — handles BOTH first delivery (`pending`) and retry (`failed`) through one route (approved D3, no separate `/retry`); `publish-outcome` remains create-only, its accepted Phase 2A contract unchanged
  - two-phase delivery lifecycle mirroring AML-01's own accepted pattern: TX1 (advisory lock → re-validate `pending`/`failed` → recompute-and-compare the live aggregate against the publication's own stored snapshot → audit `attempted` → commit, releasing the lock) → HTTP outside any transaction → TX2 (re-lock → re-check still active → terminal write → audit)
  - a stale snapshot (live aggregate ≠ stored — e.g. a corrective case opened between publish and deliver) is rejected with `KYC1_OUTCOME_PUBLICATION_STALE` (409) BEFORE any CLT-01 HTTP call, reusing the EXISTING `kyc1.outcome_publication_refused` audit event (`reason_code: "stale"`) rather than a new event type
  - 4 new error codes (12→16): `KYC1_OUTCOME_PUBLICATION_INVALID_STATE`, `KYC1_OUTCOME_PUBLICATION_STALE`, `KYC1_CLT_DELIVERY_FAILED`, `KYC1_CLT_UNAVAILABLE` — each confirmed to have exactly one reachable throw site
  - 3 new audit events (13→16): `kyc1.outcome_delivery_attempted`/`_succeeded`/`_failed` — transaction-coupled, no PII/`evidence_ref`/`payload_hash`/raw CLT-01 request-or-response body/token in any metadata
  - no IAM-02, no maker-checker, **no migration, no grant/schema change** — Phase 2A's own D6 forward-compatibility decision on the `outcome_publication.status` CHECK (declaring `succeeded`/`failed` a phase early) paid off exactly as intended
  - **236/236 KYC-01 tests, 1856/1856 full suite (92 files), `tsc -b --force` clean, migrations 001→043 + all 8 grant files clean, no migration 044**
- **Independent Opus review of Phase 2B → ACCEPT WITH LOW FINDINGS, conditional on closing one Medium (MED-2) first.** Every claim independently reproduced on a fresh disposable Postgres via adversarial probes that INJECTED the actual races (concurrent HTTP calls, a superseding publish fired from inside a fetch stub mid-delivery), not static reading.
  - **MED-2**: the documented `superseded_during_delivery` mitigation was itself non-functional. TX2 wrote `status='failed'` over a row already `status='superseded'` when a fresh publish superseded the in-flight delivery mid-HTTP-call — re-entering the row into `idx_kyc1_outcome_publication_one_active_per_application` (`UNIQUE(application_id) WHERE status<>'superseded'`) alongside the newer publication, raising a raw `23505` that the generic handler mapped to `KYC1_AUDIT_REQUIRED` (503) with the WHOLE transaction rolled back — losing the attempt evidence entirely, even when CLT-01 had genuinely accepted the delivery (only an `attempted` audit event survived, never a `failed` one).
  - **Micro-patched and independently re-reviewed → MED-2 CLOSED.** The terminal write became three-way (`succeeded`/`failed`/`superseded` — the superseded branch now writes `status='superseded'`, i.e. leaves it exactly where it was, never re-entering the index), tracked via a real discriminated result (`DeliverOutcome`) returned directly from the transaction rather than inferred back out of the final `status` value (which could no longer distinguish the two cases once the write stopped conflating them). Re-verified via the SAME adversarial reproduction across all three CLT-01 response variants (accept/reject/unreachable — the third added independently during re-review, not in the patch's own tests): `status` stays `superseded`, `attempt_count`/`failure_reason_code=superseded_during_delivery`/the `failed` audit event are all durably recorded in the SAME transaction, the caller receives `KYC1_OUTCOME_PUBLICATION_STALE` (409) never `KYC1_AUDIT_REQUIRED`, exactly one active publication survives every trial, `response_ref` stays `null` (safe-minimal — CLT-01's own `outcome_id` is never attached to a non-authoritative row), and the superseding publication remains independently deliverable afterward (verified to succeed on its own subsequent `/deliver` call).
  - **LOW-8** (`delivered_at_utc` set on failure the same as success, no distinct "last attempted at" semantics — cosmetic); **INFO-10** (concurrent `/deliver` on the SAME publication can produce two real CLT-01 writes — accepted at-least-once, AML-01 symmetry, safe-but-noisy given CLT-01's append+last-write-wins rollup); **INFO-11** (a LOW-6 test's own trailing comment is partially self-contradictory — the assertion itself is correct); **INFO-12** (if CLT-01 accepts during the supersession race, its own `cdd_outcome` row is not linked back via `response_ref` — accepted safe-minimal trade-off, the attempt is still recorded via `attempt_count`/`failure_reason_code`/the failed audit); **INFO-13** (the CLT-unavailable race variant was independently verified during review but not covered by the patch's own tests — optional future test); **INFO-14** (a benign local cast on `DeliverOutcome`, constructed inside the same function, never on external data) — all carried forward, non-blocking.
  - **D1 restated as higher-stakes, not new**: KYC-01 publishes over its own case set only and cannot prove CLT-01's authorised-party roster is complete — and a `pass` now genuinely reaches CLT-01's approval gate, so this limitation is no longer purely theoretical.
  - **Ordering residual (named, queryable, accepted)**: if a superseding publication is itself never delivered, CLT-01's rollup retains the prior value — mitigated by the publication read route being directly queryable, not eliminated.
- **Carry-forward (non-blocking) heading into KYC-01 Phase 3:** LOW-8, INFO-10, INFO-11, INFO-12, INFO-13, INFO-14 (all above); Phase 1 LOW-1 (evidence-ref base64 heuristic imprecision), LOW-2 (concurrent evidence POST 503-vs-409), LOW-4 (SQL-order test gap); INFO-3 (AML-01 migration naming-convention dependency), INFO-4 (unconstrained `document_type` placeholder), INFO-5 (evidence_refs newest-first order), INFO-7 (`fetchCasesForApplication` no `LIMIT`), INFO-8 (`contributingOutcomeIds` cast invariant), INFO-9 (grants must be re-applied after a 043 down/up round-trip); **D1** and the **ordering residual** (both above, now higher-stakes); repo-wide `timestamptz` typing outside KYC-01 remains a separate platform-hardening item.
- **Phase 3 planning inputs**: IAM-02 / manual review / maker-checker (none exist yet for KYC-01 — first real integration point, same shape CLT-01/AML-01 each hit at their own first IAM-02 phase); the **D1** authorised-party-roster-completeness limitation; the **ordering residual**; the full carry-forward register above.
- **KYC-01 — accepted implementation baseline through Phase 2B.**
- **KYC-01 Phase 3A (IAM-02 integration + sensitive evidence-read) accepted.** KYC-01's first IAM-02 integration point:
  - KYC-owned IAM-02 client: `services/kyc1/src/lib/iam2-client.ts` — F3(c)-clean (no import from AML-01/CFG-01/IAM-02 source), `checkPermission` only — no `verifyDecisionToken`/execute-verify client this phase (no Phase 3A permission carries `requires_approval=true`, so there is no execute-verify throw site to bind against; adding it unreached would itself be decorative)
  - `IAM02_BASE_URL`/`IAM02_INTERNAL_SERVICE_TOKEN` required, fail-closed at config load; 5s timeout-bounded; no token/raw-request/raw-response logging
  - baseline decision handling: `allow`/`approval_required`/`step_up_required` → pass; `deny`/`licence_locked` → `KYC1_PERMISSION_DENIED`; network error/timeout/non-2xx/malformed JSON/`success:false`/missing-decision → `KYC1_IAM2_UNAVAILABLE` — the CFG-01 Phase 3A F-1 lesson (treating `approval_required` as a denial would make the baseline check permanently unsatisfiable) applied from day one, not discovered by review
  - Migration `044_iam2_register_kyc1_phase3a_permissions.cjs` — permission-registration only, no table/`ALTER`/grant statement. Registers exactly one `iam2.permission` row: `perm_kyc1_evidence_sensitive_read` / `kyc1.evidence.sensitive_read`, resource `document_checklist_item`, action `sensitive_read`, sensitivity `sensitive`, `licence_locked=false`, `prohibited=false`, `requires_step_up=false`, `requires_approval=false`, `status=active`, `owner_module=KYC-01`. No `role_permission` seed.
  - checklist projection narrowed: `lib/kyc-case.ts`'s shared `safeChecklistItemResponse` had `evidence_ref`/`evidence_hash` REMOVED. This narrowing is intentionally BROADER than a single-route instruction — it applies to all three of that function's call sites (`GET /internal/kyc1/cases/:case_id/checklist`, `POST /internal/kyc1/cases/:case_id/evidence`'s own confirmation response, `POST /internal/kyc1/handoffs`'s embedded checklist array) — because narrowing only the checklist-read route while leaving the evidence-add echo untouched would have made the "sensitive route is the sole disclosure point" invariant unsatisfiable. Routine projection retains `checklist_item_id`/`case_id`/`document_type`/`required`/`status`/`expiry_date`/`verification_result_id`/`created_at_utc`/`updated_at_utc`.
  - new route `GET /internal/kyc1/checklist-items/:checklist_item_id/sensitive-detail?actor_id=<actor_id>` (14th route, 13→14) — internal-identity-gated, `actor_id` required, IAM-02 `kyc1.evidence.sensitive_read`-gated, single-record only (no list/search)
  - PERMISSION-BEFORE-EXISTENCE: the IAM-02 check runs before any `document_checklist_item` row is fetched — a denied caller and a denied-caller-against-a-nonexistent-item both resolve to the identical `KYC1_PERMISSION_DENIED`/403, never a 404; only once permission genuinely passes does an unknown id resolve to the EXISTING `KYC1_CHECKLIST_ITEM_NOT_FOUND` (no new `KYC1_SENSITIVE_EVIDENCE_NOT_FOUND` code)
  - AUDIT-BEFORE-RETURN, fail-closed: `kyc1.sensitive_evidence_read` (severity `high`, metadata `{checklist_item_id, case_id, document_type}` only — never `evidence_ref`/`evidence_hash`/`payload_hash`/PII/tokens/raw IAM-02 body) commits before the response is built; if the audit write fails, the route fails `KYC1_AUDIT_REQUIRED`/503 with no sensitive data returned
  - response returns an evidence REFERENCE, never document content — exactly `checklist_item_id`/`case_id`/`document_type`/`status`/`evidence_ref`/`evidence_hash`/`expiry_date`, no base64/data-URI/vendor payload
  - 2 new error codes (16→18): `KYC1_PERMISSION_DENIED`/403, `KYC1_IAM2_UNAVAILABLE`/503, each with exactly one reachable throw site
  - the existing 13 routes remain internal-service-token-only — NOT retro-gated with IAM-02 (mirrors the AML-01 precedent of gating only human-actor routes, leaving the service-to-service pipeline untouched)
  - no manual override, no maker-checker, no execute-verify, no `manual_override_request` table, no migration 045/046, **no grant/schema change**
  - **276/276 KYC-01 tests, 1896/1896 full suite (93 files), `tsc -b --force` clean, migrations 001→044 + all 8 grant files clean, no migration 045**
- **Independent Opus review of Phase 3A → ACCEPT WITH LOW FINDINGS (1 Low, 5 Informational; zero Critical/High/Medium).** Every claim independently reproduced on a fresh disposable Postgres via adversarial probes rather than static reading: DB-query-order instrumentation proved zero `document_checklist_item` queries occur before a denied permission decision (stronger than the implementer's own status-code-only test); the audit-failure fail-closed path was re-tested under a genuine least-privilege runtime role after an initial superuser-connected probe produced a false leak signal (retracted once corrected); an exhaustive canary-value sweep across all 14 routes confirmed exactly one route ever returns `evidence_ref`/`evidence_hash`; and the migration 044 down path — previously code-inspected only — was LIVE-TESTED (kyc1 permissions 1→0, total permissions 93→92 with zero collateral to any other module's permissions, kyc1 tables unchanged at 5, migration head reverted to 043, up/down/up idempotency confirmed) and is now **CLOSED**.
  - **LOW-9 (new)**: the sensitive-read `actor_id` is caller-asserted via a query parameter and is used for BOTH the IAM-02 authorization decision and the high-severity audit record — any holder of the shared `KYC1_INTERNAL_SERVICE_TOKEN` can assert an arbitrary actor identity. This follows the existing AML-01/platform-wide interim-shared-secret-identity carry-forward exactly and is NOT a Phase 3A regression, but is flagged now because Phase 3B's override attribution and strict SoD (requester ≠ approver, requester ≠ evidence provider) will depend on this identity being genuine — do not silently assume cryptographically authenticated human identity when designing Phase 3B.
  - **INFO-15 (new)**: the audit-write-fails fail-closed test is only meaningful when the integration harness connects as a non-superuser role — a superuser bypasses `REVOKE` and would silently make the test a no-op that always passes. Needs a test-site comment/harness assertion documenting the least-privilege-role requirement.
  - **INFO-16 (new)**: an authorised-but-not-found sensitive-read attempt emits no audit event (the not-found throw precedes the audit) — matches AML-01's own `matchNotFound()` ordering, accepted.
  - **INFO-17 (new)**: the pre-existing global "no deferred-phase event type is ever emitted" sweep was narrowed to a single lifecycle's own events — necessary and correct (the global form false-positived once `kyc1.sensitive_evidence_read` became genuinely reachable elsewhere in the shared test database), but it incidentally weakened the guard against the still-forbidden `kyc1.manual_review_*` event types from "never anywhere in the outbox" to "never in this one lifecycle." Phase 3B should restore a global sweep scoped to only the event types that remain forbidden.
  - **INFO-18 (new)**: `infra/grants/kyc1_runtime_grants.sql`'s header comment is stale — still claims KYC-01 has no CLT-01/IAM-02 HTTP dependency, which became untrue at Phase 2B and is now doubly untrue. Correctly NOT edited during Phase 3A (grant file changes were out of scope) — fix opportunistically when Phase 3B legitimately edits that file for its own reasons.
  - **INFO-19 (new)**: the sensitive-read audit's `try/catch` block omits the house-style `if (err instanceof Kyc1Error) throw err;` re-throw guard present in every other KYC-01 route's equivalent block. Safe today (only `publishAudit` runs inside the block, which cannot itself throw a `Kyc1Error`), carried forward as defensive-consistency hardening rather than a functional defect.
- **Carry-forward (non-blocking) heading into KYC-01 Phase 3B:** **LOW-9**, **INFO-15**–**INFO-19** (all above, new); **LOW-8**, **INFO-10**, **INFO-11**, **INFO-12**, **INFO-13**, **INFO-14** (Phase 2B, unchanged); Phase 1 **LOW-1** (evidence-ref base64 heuristic imprecision), **LOW-2** (concurrent evidence POST 503-vs-409), **LOW-4** (SQL-order test gap); **INFO-3** (AML-01 migration naming-convention dependency), **INFO-4** (unconstrained `document_type` placeholder), **INFO-5** (evidence_refs newest-first order), **INFO-7** (`fetchCasesForApplication` no `LIMIT`), **INFO-8** (`contributingOutcomeIds` cast invariant), **INFO-9** (grants must be re-applied after a migration down/up round-trip — now also true of migration 044); **D1** and the **ordering residual** (both unchanged, still open); repo-wide `timestamptz` typing outside KYC-01 remains a separate platform-hardening item.
- **Phase 3B planning inputs (accepted scope, not yet implemented):** manual outcome override ONLY (no verification_result/document_checklist_item/publication-eligibility/evidence-conflict/stale-refusal override — all forbidden); maker-checker via IAM-02 execute-verify (KYC-01's `iam2-client.ts` gains `verifyDecisionToken`); append-only `cdd_outcome` override (never mutates evidence, always inserts a new versioned row); migration 045 (`manual_override_request` table) + migration 046 (permission registration) — two migrations, matching the table→permission convention every prior module uses; strict SoD (requester ≠ approver via IAM-02, requester ≠ any manual verifier on the case via a KYC-01-owned self-block check); recompute-lock on overridden cases (reuses the existing `KYC1_CASE_INVALID_STATE`, no new code); explicit republish + redelivery required — no automatic CLT-01 publication or delivery from an override; **LOW-9** (caller-asserted actor attribution — now load-bearing for override SoD, not just decorative); **D1** (authorised-party-roster-completeness limitation, still open); the `MED-2`-style partial-unique-index status-transition hazard named in the accepted Phase 3 planning report — `manual_override_request.status` writes need the same scrutiny that caught MED-2 in `outcome_publication.status`; **INFO-17**'s global-sweep restoration; **INFO-18**'s stale grant-header fix (opportunistic, when the grant file is next legitimately touched).
- **KYC-01 — accepted implementation baseline through Phase 3A.**
- **KYC-01 Phase 3B (maker-checker manual outcome override) accepted.** Case-level `cdd_outcome` is the ONLY permitted override target; `verification_result`/`document_checklist_item`/publication-eligibility/evidence-conflict/stale-publication refusals remain explicitly forbidden override targets:
  - Migration `045_kyc1_manual_override_request.cjs` — new table `kyc1.manual_override_request` (append-only maker-checker request record). Live lifecycle: `requested`/`applied`/`failed` (the last added by the MED-3 fix, below); `cancelled` schema-present but unreachable. Partial unique index enforces at most one open `requested` row per case. No ALTER to any Phase 1/2 table.
  - Migration `046_iam2_register_kyc1_phase3b_permissions.cjs` — KYC-01's SECOND IAM-02 permission: `perm_kyc1_outcome_override` / `kyc1.outcome.override`, resource `cdd_outcome`, action `override`, sensitivity `sensitive`, `licence_locked=false`, `requires_step_up=false`, `requires_approval=true`, `status=active`, `owner_module=KYC-01`. No `role_permission` seed.
  - `lib/iam2-client.ts` gained `verifyDecisionToken` (execute-verify) alongside the existing `checkPermission`. Baseline `allow`/`approval_required`/`step_up_required` → pass; `deny`/`licence_locked` → `KYC1_PERMISSION_DENIED`. Execute-verify unavailable/network/timeout/non-2xx/malformed → `KYC1_IAM2_UNAVAILABLE`; a genuine denial (invalid/expired/replayed/hash-mismatched token) → `KYC1_APPROVAL_REQUIRED`. Raw decision token never logged or persisted — only `fingerprint(decision_token)` stored, and only on a successful apply.
  - Two new routes (14→16): `POST /internal/kyc1/cases/:case_id/outcome-override/request` (`requested_by`/`target_outcome_status` ∈ {pass, fail, remediation_required}/`reason_code` ∈ {system_derived_outcome_incorrect, manual_evidence_review, documented_compliance_exception} — bounded enum, no free text) and `POST .../outcome-override/apply` (`override_id`/`approval_id`/`decision_token` — no actor identity accepted from the caller; the route uses the STORED `requested_by`).
  - Self-override SoD: the requester is blocked (`KYC1_SELF_OVERRIDE_BLOCKED`/409) when `requested_by` equals the `source_id` of a manual verification result on the SAME case; refusal audited (`kyc1.manual_override_refused`, `reason_code: self_override_blocked`) with no requester/source_id value in metadata. Vacuous for a registry-only case (SoD then rests on IAM-02's own requester≠approver rule alone) — a named limitation, not silently accepted.
  - Append-only apply: inserts a NEW `cdd_outcome` row (`outcome_reason` ∈ {manual_override_pass, manual_override_fail, manual_override_remediation_required}), never mutates `verification_result`/`document_checklist_item`/any prior `cdd_outcome` row; updates the case's current-outcome pointer through the EXISTING column-scoped grant (no widening).
  - Recompute-lock: a case whose latest outcome is a manual override is terminal against `compute-outcome` (reuses `KYC1_CASE_INVALID_STATE`, no new code); correction requires a new case for the same anchor.
  - Publication/delivery interaction: apply never publishes, never delivers, never calls CLT-01 — zero automatic side effects. An existing active publication goes stale through the UNCHANGED Phase 2B live-snapshot comparison; override-to-pass still cannot bypass the evidence-conflict refusal at publish. Explicit republish + redeliver required (`republish_required`/`redelivery_required: true` on every apply response).
  - 4 new error codes (18→22): `KYC1_APPROVAL_REQUIRED`/403, `KYC1_OVERRIDE_NOT_FOUND`/404, `KYC1_OVERRIDE_INVALID_STATE`/409, `KYC1_SELF_OVERRIDE_BLOCKED`/409 — `KYC1_CASE_INVALID_STATE` (case/business-state problems) vs `KYC1_OVERRIDE_INVALID_STATE` (override-row lifecycle problems) is a deliberate semantic split, not two names for the same condition.
  - 3 new audit events (17→20): `kyc1.manual_override_requested`/`_applied`/`_refused` — the last reachable for both `self_override_blocked` and (post-MED-3) `case_state_changed`. Metadata excludes decision tokens/hashes/`payload_hash`/`requested_by` value/`source_id`/evidence references/PII/free text throughout.
  - **INFO-17 CLOSED**: the global forbidden-event sweep is restored — a genuinely global (unscoped) sweep confirms `kyc1.manual_review_*`/`kyc1.manual_override_denied`/`kyc1.outcome_published`/UBO/EDD/vendor/proofing event prefixes never appear anywhere in the outbox, permitting only the three legitimate override events.
  - **INFO-18 CLOSED**: `kyc1_runtime_grants.sql`'s header now correctly acknowledges KYC-01's CLT-01/IAM-02 HTTP dependencies while preserving the no-cross-schema-SQL-grant invariant — the GRANT statements themselves are byte-identical to before (comment-only fix).
  - **386/386 KYC-01 tests, 2006/2006 full suite (94 files), `tsc -b --force` clean, migrations 001→046 + all 8 grant files clean, migrations 045/046 down/up round-trip clean, same-DB rerun clean, no migration 047.**
- **Independent Opus review of Phase 3B → ACCEPT WITH LOW FINDINGS, conditional on closing one Medium (MED-3) first.** Every claim independently reproduced on a fresh disposable Postgres via adversarial probes, not static reading.
  - **MED-3**: an override approval was bound only to `{override_id, case_id, target_type, target_outcome_status, reason_code, requested_by}` — NEVER to the case outcome that existed when the override was requested. Reproduced exploit: a case at `remediation_required` has an override-to-`pass` requested and approved; genuinely adverse evidence then arrives and the case recomputes to `fail`; the STALE approval is applied anyway — `manual_override_pass` masks the `fail` finding, and the recompute-lock then makes the adverse finding unreassertable except by opening a new case.
  - **Micro-patched and independently re-reviewed → MED-3 CLOSED.** Migration 045 gained two immutable NOT NULL columns — `approved_against_outcome_id` (a genuine FK into `kyc1.cdd_outcome(outcome_id)`) and `approved_against_outcome_status` — captured from the REQUEST route's own locked re-read (never the unlocked preflight read) and included in the canonical payload hash bound into the IAM-02 approval. The APPLY route's final transaction compares the case's CURRENT `current_outcome_id`/`current_outcome_status` against this stored snapshot BY ID, not merely by status, so a same-status-different-version drift (e.g. a corrective re-verification landing a new row with the identical status) is also caught. On any mismatch: no `cdd_outcome` insert, no case-pointer update, no `manual_override_applied` event; the override row transitions `requested`→`failed` (a NEW reachable status value, vacating the `one_open_per_case` partial index exactly like `applied` already does — no MED-2-style re-entry hazard), atomically with a `kyc1.manual_override_refused` audit (`reason_code: case_state_changed`); the route returns `KYC1_CASE_INVALID_STATE`/409; a replacement request may be created immediately for the case's new state. If the refusal audit itself fails, the WHOLE transaction rolls back (the row stays `requested`, never silently `failed` without its audit) and the caller receives the existing `KYC1_AUDIT_REQUIRED`. Opus independently replayed the ORIGINAL exploit verbatim against the patched code and confirmed it is genuinely blocked (case-outcome history stops at the adverse `fail` row, no `pass` masking, no recompute-lock introduced on the failed path), plus a same-status/different-outcome-ID variant proving ID-binding specifically, plus a forced-audit-failure probe proving atomicity.
  - **LOW-10 (new)**: the request route's self-override check runs BEFORE the IAM-02 permission check (as specified by the accepted Phase 3B design) — an unpermissioned shared-token caller can distinguish "requested_by matches a manual evidence provider on this case" from "it doesn't," a one-bit manual-verifier-attribution oracle; non-blocking, implemented as designed, not a deviation.
  - **LOW-11 (new)**: case-existence/state validation also precedes the IAM-02 permission check — an unpermissioned caller can distinguish missing-case/invalid-state/permission-denied; materiality is low since equivalent case/outcome data is already available from KYC-01's own intentionally-ungated internal routes.
  - **INFO-20 (new)**: the self-override refusal audit is best-effort — a self-override attempt can go unaudited if the outbox write fails, though no state is ever mutated by that refusal.
  - **INFO-21 (new)**: `cancelled` remains schema-present and unreachable; no decorative error code was added for it.
  - **INFO-22 (new)**: the implementation report's own 13-vs-11 file-count discrepancy was confirmed to be bookkeeping only (13 total — 5 created, 8 modified; the 11-file sweep had excluded migrations 045/046 by using migration 046 itself as the `-newer` cutoff) — no missing or extra change.
  - **INFO-23 (new)**: on a MED-3 case-state-drift refusal, IAM-02 may already have successfully verified and consumed the decision token, yet KYC-01 records `approval_id`/`decision_token_hash` as `null` on the failed row — KYC-01 alone cannot reconstruct "was an approval issued and spent" for a refused override; that evidence lives solely on IAM-02's side. Accepted safe-minimal behaviour (recording an approval linkage on a row that was never applied would misleadingly imply the approval authorised something); a replacement request requires a fresh approval cycle regardless.
  - **INFO-24 (new)**: the apply route's defence-in-depth `caseIsOverridable` re-check is logically redundant whenever the exact approved-against ID/status snapshot still matches — harmless, retained for defensive consistency; the ID/status snapshot comparison is the actual MED-3 control.
- **Carry-forward (non-blocking) heading into KYC-01 Phase 4:** **LOW-10**, **LOW-11**, **INFO-20**–**INFO-24** (all above, new); **LOW-9**, **INFO-15**, **INFO-16**, **INFO-19** (Phase 3A, unchanged — INFO-17/INFO-18 now CLOSED, above); **LOW-8**, **INFO-10**, **INFO-11**, **INFO-12**, **INFO-13**, **INFO-14** (Phase 2B, unchanged); Phase 1 **LOW-1**/**LOW-2**/**LOW-4**, **INFO-3**/**INFO-4**/**INFO-5**/**INFO-7**/**INFO-8**/**INFO-9** (now also true of migrations 045/046's own down/up round-trip); **D1** and the **ordering residual** (both unchanged, still open); repo-wide `timestamptz` typing outside KYC-01 remains a separate platform-hardening item.
- **Phase 4 planning inputs**: **D1** (authorised-party-roster-completeness limitation) is now the LEADING planning input — materially higher-risk than at Phase 2B/3A, since a human-approved override-to-`pass` is now implemented, can be explicitly published and delivered to CLT-01, and incomplete authorised-party coverage could mean approval was granted over an incomplete KYC case set; the registry-only SoD limitation; the ungated publish/deliver residual (D6); the full carry-forward register above. **Phase 3C is explicitly skipped** — Phase 3 is complete through Phase 3B; the next step is KYC-01 Phase 4 planning (no Phase 4 implementation has started).
- **KYC-01 — accepted implementation baseline through Phase 3B** (phase label unchanged by the CLT-01-side prerequisite below).
- **KYC-01 Phase 4A prerequisite — CLT-01 application-keyed KYC roster contract — now an accepted CLT-01 baseline.** Opus's Phase 4 planning had identified a structural blocker: CLT-01's only existing authorised-party roster read (`GET /internal/clt1/clients/:client_id/authorised-parties`) is client-keyed and gated on `client_profile.status='active_limited'`, which is created only at final approval — chronologically unavailable to KYC-01, which publishes/delivers while the application is still `under_review`, before any `client_profile`/`client_id` exists. **CLT-01 Phase 4A** closes this with exactly one new internal route, added on top of the already-complete CLT-01 Phases 0-9 baseline — **no migration, no schema/grant change, no IAM-02 permission, no `role_permission` seed, no outbox event, no KYC-01 source change.**
  - Route: `GET /internal/clt1/applications/:application_id/kyc-roster` — keyed directly by `client_application.application_id`, no `client_id`/`client_profile`/`fetchActiveClientOrThrow` dependency; internal-service-token-only (no IAM-02 call, no `actor_id`, no caller-asserted human identity — the empty querystring schema structurally rejects both); read-only (no INSERT/UPDATE/DELETE, no transaction, no audit/outbox event — a non-mutating, PII-free machine read).
  - Response: `{application_id, application_status, primary_subject_type, authorised_parties, party_count, roster_hash}`; each party `{authorised_party_id, party_type, authority_status, version}` — no `party_reference`, no PII, no `applicant_type` (mapped once to `primary_subject_type`: `individual`→`individual`, `corporate`/`institutional`→`entity`). No application-status filtering — CLT-01 reports fact, KYC-01 Phase 4B decides eligibility.
  - Complete roster, unfiltered by `authority_status`; complete-or-error, no pagination/truncation; hard cap 500 parties (`LIMIT 501` to detect overflow without materialising an unbounded set).
  - `roster_hash`: CLT-owned, `@aix/foundation` `fingerprint()` over the roster canonically sorted by `authorised_party_id` ASC via a locale-independent `codePointCompare` (the CFG-01 F-1 lesson applied up front — proven, not assumed, by an Opus adversarial collation test) — deliberately a content digest, not a version counter, so the "same version, different contents" drift class is structurally impossible and no migration touches any of the four already-accepted `authorised_party` maker-checker apply paths.
  - **Roster-cap error micro-patch** (same-day follow-up): registered a dedicated `CLT1_KYC_ROSTER_TOO_LARGE` (409, "KYC roster exceeds the maximum supported size.", bounded non-PII details `reason_code=roster_too_large maximum_party_count=500 observed_at_least=501`) rather than continuing to reuse the ill-fitting `CLT1_APPLICATION_INVALID_STATE`. CLT-01 error catalogue 42→43; route tree 67→68 lines (+1, the sole new node); `clt1.*` IAM-02 permissions unchanged at 33.
  - **Independent Opus review → ACCEPT WITH LOW FINDINGS** (zero Critical/High/Medium), reproduced via 76 independently-authored adversarial probes on a fresh disposable Postgres — including an independent hash recomputation from raw DB rows matching the route byte-for-byte, and a decisive collation probe (mixed-case IDs sort `B,a,c` under the route vs `a,B,c` under `en_US`, proving the ordering is application-code-driven, not DB-collation-driven). **LOW-1**: the application-row read and the party-roster read are two independent (non-transactional) READ COMMITTED statements, so they are not snapshot-consistent — classified Low, not Medium, because the tear direction is structurally safe: the party read runs second, so a concurrently-added party is never missed; the only achievable tear (stale `application_status` paired with current parties) fails closed at Phase 4B's comparison, never open. Seven Informational findings, all non-blocking (a misleadingly-titled test whose real coverage exists elsewhere; a no-op test statement; `codePointCompare` naming vs true code-point semantics, immaterial for ASCII IDs; SQL-typed-not-runtime-validated enum fields, safe under existing CHECK constraints; no Fastify response schema, consistent with CLT-01 convention; Fastify auto-HEAD executing the full handler like every other CLT-01 GET; an existence oracle already available from the Phase 1 application-read route). Opus independently reproduced **CLT-01 focused unit 57/57, CLT-01 integration 357/357, KYC-01 regression 386/386 (unchanged), full suite 2084/2084 (95 files)**, migrations 001→046 clean (no 047), all 8 grant files clean, and separately isolated the known same-DB 22 IAM-01/IAM-02/CFG-01 bootstrap-ordering failures as causally unrelated to Phase 4A by running only those three files against a fresh database with zero CLT-01/KYC-01 test involvement and reproducing the identical 22-failure set on the second run. No code was written or changed during the review.
  - Residual risks (classified, non-blocking): the shared internal-token model is now compliance-load-bearing but exposes no PII and no caller-asserted human identity; `roster_hash` is drift-detection only, not signed/KMS-attested, and does not defend against an actor with authorised `clt1` database write access; the digest proves content equality, not absence of intermediate churn between two reads; `party_type='ubo'` proves coverage of UBOs CLT-01 already knows about, not beneficial-owner-discovery completeness or ownership-threshold enforcement (`ubo_identification_threshold` remains `to_be_defined`).
  - **Phase 4B readiness — Opus-confirmed**: the contract supplies every element KYC-01 Phase 4B needs (application identity, lifecycle state, primary-subject type, complete roster, stable `authorised_party_id` matching key, party type/authority-status/version, deterministic hash, total count, complete-or-error semantics), with no missing field or blocking ambiguity. **D1 remains open** until KYC-01 Phase 4B itself implements publish-time roster-completeness validation, a roster-bound publication snapshot, and delivery-time roster revalidation. **KYC-01 Phase 4B planning/coding may now begin.**
- **CLT-01 Phase 4A.1 (atomic KYC roster binding, coordinated prerequisite) — now accepted.** Closes a confirmed, empirically-reproduced TOCTOU between the Phase 4A roster read and the existing KYC/KYB outcome receipt: migration `047_clt1_atomic_kyc_roster_binding.cjs` adds two nullable, lowercase-`sha256`-format-constrained columns — `clt1.client_application.kyc_roster_hash` (the digest the latest accepted `kyc_kyb` outcome was validated against, rechecked at final approval) and `clt1.cdd_outcome.kyc_roster_hash` (immutable, append-only, set once at INSERT). `expected_roster_hash` is now mandatory on every `kyc_kyb` receipt; a shared application-scoped `pg_advisory_xact_lock` (`acquireKycRosterLock`, 8 source call sites covering 10 runtime transaction paths) is the FIRST statement of every roster-mutating transaction and of both the outcome-receipt and approval-recheck transactions — independently verified via a static lock-position guard with a genuine negative control (a synthetic lock-after-`FOR UPDATE` block is proven to fail the guard) plus three behavioural independent-connection lock-block tests. CLT-01 recomputes the current roster hash under the lock and compares it atomically; a mismatch refuses as `CLT1_KYC_ROSTER_STALE`/409 BEFORE any `cdd_outcome` INSERT or rollup UPDATE, with a committed `clt1.kyc_outcome_refused` audit event. Final approval independently rechecks the SAME binding under the SAME lock — a NULL binding is a hard refusal, never a bypass; no `client_profile` is ever created on a stale/missing binding. A test-only MED-1 micro-patch corrected the lock-position guard's own overclaimed scope description without weakening the guard.
- **KYC-01 Phase 4B (authoritative roster completeness, publication binding, atomic delivery integration) — now accepted.** Migration `048_kyc1_publication_roster_binding.cjs` adds five nullable, all-or-none-bound columns to `kyc1.outcome_publication` (`roster_hash`, `required_party_count`, `evaluated_party_count`, `contributing_party_ids`, `roster_fetched_at_utc`) — every pre-048 row is genuinely all-NULL, and all five are INSERT-once, independently confirmed immutable under the real runtime role. New KYC-owned roster client (`lib/roster-client.ts`) wraps CLT-01's Phase 4A route with exhaustive shape validation and a single fail-closed discriminant. `computeRosterBoundOutcome` reuses the EXISTING anchor-resolution algorithm (`selectAuthoritativeCasePerAnchor`) — no second authority algorithm; `computeAuthoritativeOutcome` itself is untouched (proven byte-for-byte), though no live route calls it any longer. Required-party set is exactly `authority_status` ∈ {pending, active, restricted, suspended}; matching is exact codepoint string equality only. A required party with no case, or no resolved outcome, ALWAYS blocks publication outright — never downgraded to `remediation_required` (a deliberate tightening from Phase 2A's own party-anchor folding). Extra KYC-01 cases are excluded from the aggregate, never blocking. Publish fetches/validates the roster and confirms `under_review` OUTSIDE any transaction before the SAME LOW-5 advisory lock as its first statement. Deliver re-fetches the roster outside TX1 and compares (in priority order, each independently reachable) the required-party set, roster hash, freshly-recomputed live aggregate, contributing IDs, and aggregate status against the stored evidence — `expected_roster_hash` is sourced EXCLUSIVELY from the locked publication row. CLT-01's own `CLT1_KYC_ROSTER_STALE`/409 (Phase 4A.1) maps to `KYC1_OUTCOME_PUBLICATION_STALE`, never `KYC1_CLT_DELIVERY_FAILED`/`KYC1_CLT_UNAVAILABLE`. A legacy (pre-048) publication can never be delivered — the only remedy is a fresh publish. Zero new KYC-01 error codes (still 22) and zero new audit event types (still 20). Manual overrides (Phase 3B) interact correctly — an override on one anchor cannot substitute for a different missing required party, and MED-3's approved-against binding is unaffected.
- **Independent Opus review of the combined CLT-01 Phase 4A.1 + KYC-01 Phase 4B implementation → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High/Medium; 2 Low — dead production code (`computeAuthoritativeOutcome` no longer called by any route, retained only as the regression oracle) and a stale opening header sentence in `routes/outcome-publication.ts`; 4 Informational — collapsed roster-fetch-failure reason codes, an inaccurate migration-header attribution for `contributing_party_ids` dedup, incomplete `party_missing` diagnostics, and a process recommendation to pin canonical SEC-01 test env values). No fixes required before acceptance. Canonical combined full suite **2195/2195 (96/96 test files)** on a fresh disposable Postgres, `npx tsc -b --force` clean, migrations 001→048 clean, migration 047 intact and unmodified by Phase 4B, migration 048 down/up round-trip clean, all 8 grant files clean, same-DB targeted KYC+CLT rerun **606/606**, migration head **048**, no migration 049.
- **SEC-01 test-environment correction (record accurately, supersedes any earlier "pre-existing failure" characterization):** an implementation session's own `sec1-db.test.ts` run showed 66 failures in the Phase 5 security-monitoring-rule-engine section. Independently investigated, not accepted on report: root cause was the session running migration 008 with TRANSPOSED `SEC1_INGEST_TOKEN_*` env values against the values `sec1-db.test.ts` itself hardcodes and documents in its own header comment — migration 008 seeds the expected token hashes at migration time, so every authenticated ingest call returned 401. This was NOT a Phase 4A.1/4B regression and NOT an inherent pre-existing SEC-01 code defect — classification: **unrelated harness/environment invocation issue, independently proven** (reproduced the 66 failures in isolation with the transposed values; with the canonical values below, `sec1-db.test.ts` passed 139/139 and the full canonical suite passed 2195/2195). **Canonical values** (pin these — do not re-derive): `SEC1_INGEST_TOKEN_FND01=test-fnd01-ingest-token-it`, `SEC1_INGEST_TOKEN_IAM01=test-iam01-ingest-token-it`, `SEC1_INGEST_TOKEN_IAM02=test-iam02-ingest-token-it`. Canonical command: `npm test`. Canonical result: `Test Files 96 passed (96)` / `Tests 2195 passed (2195)`.
- **D1 (authorised-party-roster-completeness) remains open.** Technical roster-binding consistency is enforced end to end. Operational authorised-party completeness remains open pending Phase 4A.2 pre-approval authorised-party capture. Structurally confirmed: all 11 CLT-01 authorised-party routes require an approved `active_limited` `client_profile`, which is created only at final approval — so every supported pre-approval workflow currently produces an empty roster, and every non-empty-roster test in Phase 4B seeds via raw SQL/test fixtures, explicitly labeled as proving technical consistency only, not organic-workflow completeness.
- **KYC-01 — accepted implementation baseline through Phase 4B.**
- **CLT-01 Phase 4A.2A (pre-approval authorised-party capture) — now accepted, after one reject/remediate/re-review cycle.** Objective: make the KYC roster organically reachable before final approval, closing the structural gap where every pre-approval workflow produced an empty roster. Architecture: extracted the four existing maker-checker mutation functions (`requestPartyAdd`/`applyPartyAdd`, `requestPartyUpdate`/`applyPartyUpdate`, `requestPartyRemoval`/`applyPartyRemoval`, `recordPartyScreeningOutcome`) plus list/lookup helpers into a shared service (`services/clt1/src/lib/authorised-party-service.ts`), called identically by two route families — the pre-existing client-keyed post-approval routes (`services/clt1/src/routes/authorised-parties.ts`, refactored, behavior-preserving) and eight new application-keyed pre-approval routes (`services/clt1/src/routes/application-authorised-parties.ts`: GET list, add/request, add/apply, update/request, update/apply, remove/request, remove/apply, screening-outcome). No application-keyed routes were added for activate/restrict/reject/suspend — those remain post-approval-only, client-keyed. Lifecycle: pre-approval capture allowed only from `draft`/`submitted`/`under_review`; screening outcomes only from `under_review`; post-approval maintenance only from `approved`, enforced via two new `Clt1Action` entries (`authorised-party-capture`, `authorised-party-screening`) in `lib/applications.ts`. Party model, PII handling, maker-checker separation-of-duties, and the shared `pg_advisory_xact_lock` roster-binding discipline (now 8 source call sites / 14 runtime paths, re-inventoried from source, not hardcoded) are unchanged from Phase 4A/4A.1. The pre-existing held-state lifecycle gap (no resume/unhold transition exists anywhere) is unchanged and remains explicitly out of scope.
- **Independent Opus review of the INITIAL Phase 4A.2A implementation → REJECT.** Two empirically-proven critical findings, both reproduced with disposable-Postgres adversarial probes: **CRITICAL-1** — the shared mutation functions validated application lifecycle only as a route preflight, never re-checked inside the mutation transaction; `reject`/`hold` take no roster advisory lock, so a party could be inserted into an already-`rejected` application, HTTP 200, no refusal. **CRITICAL-2** — the same preflight-only gap allowed a party to be inserted into an already-`approved` application (with a live `client_profile`) after approval had already committed and Phase 4A.1's own roster recheck had already passed, leaving the accepted `kyc_roster_hash` diverged from the current roster, HTTP 200, no refusal. No code was changed during this review.
- **CLT-01 Phase 4A.2A lifecycle TOCTOU remediation — accepted.** Added `lockAndAssertApplicationLifecycle(txClient, applicationId, allowedApplicationStatuses)` to `authorised-party-service.ts`: `SELECT status FROM clt1.client_application WHERE application_id = $1 FOR UPDATE`, called inside every one of the four shared mutation functions, after the existing roster advisory lock, before any business write or audit publication, returning a discriminated refusal result (not a throw) so the original accurate error code (`CLT1_APPLICATION_INVALID_STATE`) is preserved rather than collapsing into `CLT1_AUDIT_REQUIRED`. Three new trusted, compile-time-only policy constants gate which statuses each route family may pass in: `PRE_APPROVAL_PARTY_STATUSES = ["draft","submitted","under_review"]`, `PRE_APPROVAL_SCREENING_STATUSES = ["under_review"]`, `POST_APPROVAL_PARTY_STATUSES = ["approved"]` — selected by trusted route registration code, never derived from caller input. Touched exactly 4 files (`authorised-party-service.ts`, both route files for call-site wiring, `tests/integration/clt1-db.test.ts`); no migration, grant, permission, error code, or audit event type changed. The prior nondeterministic `Promise.all` race test was replaced with 9 deterministic tests using two genuine test barriers: an independent-connection advisory-lock-hold barrier (proving reject/hold win when they commit first) and a `setTimeout`-based delay embedded in a route-scoped IAM-02 stub (proving approval commits first without relying on unguaranteed lock re-acquisition order), covering all four mutation functions individually.
- **Independent Opus re-review of the remediation → ACCEPT REMEDIATED PHASE 4A.2A — WITH NON-BLOCKING FINDINGS** (0 Critical/High/Medium). Re-ran the ORIGINAL, unmodified CRITICAL-1/CRITICAL-2 exploit probes against the remediated code plus 6 additional independently-authored probes (client-keyed update/remove/screening refusal, full post-approval maintenance lifecycle, a 6x concurrent add-vs-approve race, organic capture regression) — both original exploits now refuse with HTTP 409 `CLT1_APPLICATION_INVALID_STATE` and zero corruption; the concurrent race produced zero both-success outcomes and zero deadlocks across all runs, confirming no third unsafe ordering is reachable. Deterministic test subset re-run 5x consecutively, stable every time. Independently reproduced `npx tsc -b --force` clean, canonical full suite **2241/2241 (96/96 test files)**, CLT-01 integration 415/415, KYC-01 regression 237/237, AML regression 191/191, same-DB targeted rerun 652/652, migration head **048**, no migration 049. **2 Low** (carried forward, unchanged framing: the lock-guard's honestly-scoped documentation limitation; application-keyed list ordering lacks an explicit tie-break) and **3 Informational** (the pre-existing held-state dead end, now explicitly flagged for deliberate future review; the inherited client-keyed `client_profile.status` preflight-only asymmetry, also flagged for future review; a harmless transaction-commits-on-refusal subtlety) — none classified as acceptance blockers.
- **CLT-01 Phase 4A.2B (coordinated KYC anchor-sync integration) — now accepted.** KYC-01 gains `POST /internal/kyc1/applications/:application_id/roster-sync` — internal KYC service token only, no IAM-02, no caller-supplied actor identity — which fetches the authoritative CLT-01 roster, requires `application_status='under_review'`, and creates exactly one KYC `authorised_party` case for every required party that has no KYC case at all; any party with ANY existing case (any status) is skipped, never re-created. Case creation now runs through one shared implementation (`lib/kyc-case-creation.ts`) used by both the pre-existing handoff route and roster-sync — generates the case id, inserts `kyc_case` + the default checklist, emits `kyc1.case_created`, never opens/commits its own transaction, makes no HTTP call. Audit separation: handoff alone still emits `kyc1.handoff_received`; roster-sync never does, emitting only `kyc1.case_created` per created anchor plus one bounded, PII-free `kyc1.roster_sync_processed` summary event per call. The required-authority-status set (`pending`/`active`/`restricted`/`suspended`) and the application-scoped advisory lock (`kyc1.outcome_publication:<application_id>`, unchanged namespace) are now shared exports consumed identically by outcome publication and roster-sync, so the same lock serialises roster-sync, publication, and delivery for one application. Completed-case and corrective-case retries are safe (creates nothing new, changes no authoritative selection, never makes a publishable application unpublishable); concurrent roster-sync calls serialise cleanly through the shared lock (one creator, one no-op, no duplicates, no `40P01`); a handoff-versus-roster-sync race is bounded by the pre-existing partial unique index (`KYC1_CASE_ALREADY_EXISTS` on the raced path, atomic rollback, safe convergent retry) — the handoff route does not need the roster-sync lock. Roster-sync creates authorised-party cases only, never a primary case; `primary_anchor_present` is informational only, and publication remains the sole authority for `primary_missing`/`primary_type_mismatch`/`anchor_ambiguous`. All Phase 4B controls preserved unchanged (missing anchor/outcome still blocks, exact ID matching, extra-case non-blocking, worst-wins, immutable publication evidence, delivery revalidation, CLT-01's own atomic stale rejection, final-approval roster-hash recheck). One new error `KYC1_APPLICATION_INVALID_STATE` (409, thrown only when the validated roster reports a non-`under_review` application) — KYC error catalogue now **23**. One new audit event `kyc1.roster_sync_processed` — KYC audit-event inventory now **21**. KYC route handlers now **17**; tables unchanged **6**; IAM-02 `kyc1.*` permissions unchanged **2**; no `role_permission` seed; grants unchanged; CLT-01 runtime source unchanged (error catalogue 44, IAM-02 permissions 33, tables 18, routes 69, all unchanged).
- **The organic, no-raw-SQL D1-closing end-to-end test passes.** Real CLT-01 app + real KYC-01 app in one process (combined-role fixture, inject-fetch adapter — proven not to bypass any real auth/validation/route logic): create application → submit → start review → capture an authorised party through the accepted CLT-01 Phase 4A.2A maker-checker routes → create the primary KYC case through the existing handoff route → invoke KYC roster-sync (creating the authorised-party KYC case with `party_id` exactly equal to the CLT-generated `authorised_party_id`) → complete evidence/checks/outcomes for both cases → create the required AML handoff marker → deliver `aml_sanctions`/`pep_adverse_media`/`risk_rating` → publish the roster-bound KYC outcome → deliver it atomically to CLT-01 → request and apply final approval → `client_profile` created — zero raw SQL `authorised_party`/`kyc_case`/`outcome_publication`/`client_profile` inserts, no hand-written roster hash, no fake caller-supplied party id, no maker-checker bypass; database SELECTs used only for final assertions.
- **Independent Opus review of the initial implementation: runtime found correct, one blocking test-infrastructure finding (MEDIUM-1, not a runtime defect).** The exact 21-event KYC audit-event inventory assertion was declared BEFORE the Phase 4A.2B block that first organically emits `kyc1.roster_sync_processed`, so on a genuinely fresh database the event did not exist yet when the assertion ran (2279/2280, 96/97 files) — a warm/pre-populated database masked this. **Remediated and re-verified → MEDIUM-1 closed.** The exact-equality assertion (unweakened, same 21-member set) moved to a new, final `describe` block executing after every KYC event-emitting test including the whole Phase 4A.2B block — no event manually seeded, no runtime source changed. Independently re-verified on TWO separately-provisioned fresh databases, canonical `npm test` **2280/2280 across 97/97 files** on each, proving the fix is deterministic, not residual-state-dependent. **Accepted canonical baseline**: `npx tsc -b --force` clean; KYC-01 integration **273/273**; KYC-01 unit **38/38**; organic E2E **1/1**; CLT-01 regression **415/415**; AML-01 regression **191/191**; same-DB targeted rerun **887/887**; migrations 001→048 clean; all 8 grants clean; migration head **048**; no migration 049. **2 Low** (non-blocking, both independently proven correct despite lacking a dedicated implementation-suite test): roster-sync's own audit-failure rollback (`KYC1_AUDIT_REQUIRED`, full rollback, clean retry) and the handoff-versus-roster-sync `23505` race (atomic rollback, safe convergent retry) — coverage housekeeping only; `primary_anchor_present` may report `true` when both an individual and entity primary case exist — informational only, publication still correctly refuses `anchor_ambiguous`. **4 Informational**: the held-state dead end (separate, unrelated CLT-01 carry-forward); the handoff route's exact SQL statement position changed while relative audit ordering is preserved (comment wording imprecise only); the combined-role E2E fixture proves the full workflow but not cross-schema grant isolation on its own (the dedicated grant-boundary tests under the real KYC-01 role remain the authority for that); `entity_type="roster_sync"` is the first KYC audit `entity_type` with no corresponding table, safely permitted by SEC-01's free-form field. **Known, pre-existing, unrelated same-database limitation** (not a Phase 4A.2B finding): a SECOND consecutive full-suite run against an already-used database reproduces the documented one-time IAM-01/IAM-02/CFG-01 bootstrap-idempotency signature (`iam-db`/`iam2-db`/`cfg1-db`) — KYC-01/CLT-01/AML-01 remain fully green in that run; canonical acceptance rests on the two independently fresh-database runs above.
- **D1 (authorised-party-roster-completeness) — closed. Technical roster-binding consistency is enforced. Pre-approval authorised-party capture is operational. Deterministic KYC anchor creation is operational. One complete organic CLT-to-KYC workflow — application creation, submission, review, authorised-party capture, KYC roster sync, KYC evidence and outcome, roster-bound publication, atomic delivery, and final approval — is proven end to end with no raw-SQL authorised-party or KYC-case seeding.**
- **KYC-01 — accepted implementation baseline through Phase 4B plus accepted Phase 4A.2B deterministic authorised-party anchor sync.** KYC-01 reaches the planned accepted completion point for this workstream; still recorded as partial pending independent confirmation that no later KYC-01 phases remain on its own module roadmap.
- **CLT-01 — accepted core implementation (Phases 0-9) plus accepted Phase 4A KYC roster prerequisite, accepted Phase 4A.1 atomic KYC roster binding, accepted Phase 4A.2A pre-approval authorised-party capture, and accepted coordinated Phase 4A.2B KYC anchor-sync integration.** CLT-01 remains a completed module.
- **D1 is closed. Phase 4A.2B is accepted. The next implementation task must be selected from the remaining approved module roadmap.**
- **WLT-01 selected as the next approved-roadmap module.** The documented build order is `WLT-01 → LED-01 → DEP-01 → WDR-01 → TRD-01 → E2E-01 → REC-01 → INC-01 → PRT-01` (correcting an earlier assumed ledger-first ordering). WLT-01 v1.2 is the controlling blueprint — the v1.1→v1.2 delta is documentation-only (no substantive route/table/state-machine/permission/error/audit-event/test/integration-contract change); no additional blueprint review is required before Phase 1 planning.
- **WLT-01 Phase 0 (service scaffold, boot guard, internal route baseline) implemented and accepted.** New service `services/wlt1` (`@aix/service-wlt1`) — own request-context plugin, own interim internal-service identity guard (`WLT1_INTERNAL_SERVICE_TOKEN`, `timingSafeEqual`, fail-closed), own config loader, boot-time `assertNoExchangeRuntime` (wired before `listen`), F3(c) import-boundary test, one production route (`GET /internal/wlt1/health`, unauthenticated, `{status:"alive", module:"WLT-01"}`). No `wlt1` schema, no `role_wlt1_runtime`, no migration 049, no WLT grant file, no IAM-02 permission, no business route, no public route, no external HTTP/provider call.
- **Independent Opus review of Phase 0 → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High; 1 Medium; 0 Low; 6 Informational), every claim independently reproduced: `tsc -b --force` clean; WLT tests 45/45 (40 WLT-authored + 5 re-executed shared import-boundary scanner tests); full canonical suite **2325/2325 across 102/102 files** reproduced on two separately created genuinely fresh Postgres databases (one running the canonical suite as its very first test action); direct `information_schema`/`pg_roles`/`iam2.permission`/`iam2.role_permission` queries confirmed zero WLT-01 database footprint; `package-lock.json`'s only change is the new workspace-member registration (no dependency version changed); the boot guard was empirically proven to throw on an injected prohibited route. **MED-1 — the inherited shared import-boundary scanner does not detect dynamic `import()`, template-literal dynamic `import()`, `require()`, or `@aix/service-*` sibling package aliases** — current WLT-01 source contains none of these, so this is non-blocking for Phase 0, but it is a **mandatory Phase 1 entry condition** (must close before WLT-01 business or sibling-integration code is added — Phase 1+ will eventually add five sibling HTTP clients, the largest sibling-integration surface of any module built so far). Full detail and the six Informational findings: `aix-platform/docs/implementation/WLT-01_IMPLEMENTATION_NOTES.md`.
- **WLT-01 is partial through the accepted Phase 0 service scaffold baseline.** Phase 1 (schema `wlt1`, `role_wlt1_runtime`, migration 049, destination registration/read, address canonicalisation and integrity, chain-coverage registry, readiness route) has not begun. MED-1 must be closed as the first Phase 1 implementation commit. Open decisions before Phase 1 coding: **D-CFG** (`cfg1.feature` has zero positive WLT entries; unknown feature codes fail closed; no bypass permitted), **D-CHAINS** (initial supported-chain coverage), **D-COOLOFF** (cooling-off duration source). **AML-01 Phase 3E** (synchronous stub-backed pre-transaction gate) must land before **WLT-01 Phase 4A**; WLT Phase 4 remains split into 4A (evaluate-use/token issuance/read-only verify) and 4B (verify-and-consume/release/LED-01 contract tests); **LED-01 remains blocked until WLT-01 Phase 4B acceptance**.
- **WLT-01 Phase 1A (MED-1 import-boundary control remediation) implemented and accepted.** MED-1 required closing 8 proven false negatives (dynamic `import()`, `require()`/`require.resolve()`, sibling package aliases/subpaths, `export … from`/`export * from`, `ImportEqualsDeclaration`) and 2 proven false positives (commented-out imports, import-like text in ordinary strings). The inherited regex scanner is retired; `tests/helpers/module-import-boundary.ts` — a new side-effect-free helper using `ts.createSourceFile` + structural AST traversal — replaces it. Dynamic-specifier policy: a string or no-substitution-template argument resolves statically; anything else (interpolated template, identifier, call expression) fails closed as `unresolvable_dynamic_specifier`. Sibling package-alias names are derived from each service's own `package.json` (`@aix/service-fnd`, `@aix/service-iam`, `@aix/service-iam2`, `@aix/service-sec1`, `@aix/service-cfg1`, `@aix/service-clt1`, `@aix/service-kyc1`, `@aix/service-aml1`, `@aix/service-wlt1`), not hard-coded. All 8 consumer suites (IAM-01, IAM-02, SEC-01, CFG-01, CLT-01, KYC-01, AML-01, WLT-01) now import the shared helper directly; IAM-01's former 5 tests no longer re-register inside the other 7.
- **Independent Opus review of Phase 1A → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High/Medium; 2 Low; 4 Informational), every claim independently reproduced: `tsc -b --force` clean; helper unit tests 32/32; all 8 module boundary suites individually and together (41/41 across 9 files); WLT-01 unit baseline 40/40; non-DB unit baseline 1054/1054 across 89 files; **canonical full suite 2319/2319 across 103/103 files reproduced on two separately created genuinely fresh Postgres databases** (the second running the canonical suite as its very first test action); migration head remains 048, no migration 049. The reviewer additionally scanned all 9 existing service source trees against **all** siblings under the enhanced rule and confirmed every one clean — no dynamic sibling import, no `require`/`require.resolve` sibling import, no sibling re-export, no sibling package alias, no foundation-internal import exists anywhere in current service source. **Test-count reconciliation**: 2325→2319 tests, 102→103 files — the decrease is entirely removed redundant test execution (IAM-01's 5 tests no longer replayed in 7 other suites), not reduced coverage; authored boundary-control coverage rose 9→41 tests. **MED-1 — closed.**
- **LOW-1** (mandatory first Phase 1B commit): the shared scanner does not yet enumerate `.mts`/`.cts` source files — no such file currently exists under any `services/*/src` tree, so this is non-blocking for Phase 1A but must close before any WLT-01 Phase 1B business source is added. **LOW-2**: `tests/unit/sec1-external-anchor-boundary.test.ts` retains a private copy of the retired regex scanner (proves a SEC-01 same-module property, not sibling coupling; its assertion remains true) — scheduled for a later, separate pass. Four Informational findings recorded non-blocking (test-title precision; narrow sibling-forbidden sets on some suites, all independently confirmed clean regardless; no dedicated FND-01 boundary suite; same-module package-alias policy undocumented, no current occurrence).
- **WLT-01 Phase 1B (schema `wlt1`, `role_wlt1_runtime`, migration 049, wallet-destination registration/read, Ethereum/TRON canonicalisation, chain-coverage baseline) implemented and accepted.** Gate A (LOW-1 remediation, `.mts`/`.cts` scanner extension) closed and independently verified first (49/49 across 9/9 boundary suites) before any Gate B business file was touched. Gate B: exactly 4 tables (`destination`, `wallet_destination`, `address_integrity_check`, `chain_coverage`), `role_wlt1_runtime` plus one WLT grant file, `GET /internal/wlt1/readiness`, `POST /internal/wlt1/wallet-destinations`, `GET /internal/wlt1/wallet-destinations/:destination_id`, CLT-01 client-status validation resolved fully before the WLT-01 transaction opens, deterministic Ethereum mainnet EIP-55 canonicalisation (genuine Keccak-256, never SHA3-256) and TRON mainnet Base58Check canonicalisation (double SHA-256) via `@noble/hashes@1.8.0`/`@scure/base@1.2.6` (exact pins, no wallet/RPC/signing SDK introduced), deterministic address-integrity evidence including durable refusal evidence, deny-by-default chain-coverage registry seeded with exactly `ethereum/mainnet` + `tron/mainnet` (address-format coverage only, not asset approval — ETH/TRX/USDT approval remains a separate CFG-01-licence-controlled decision), safe masked API projection, natural-key duplicate prevention, request idempotency, client-scoped advisory locking, and audit-coupled atomic registration. Exactly 3 audit event types, 10 reachable error codes, 0 IAM-02 permissions. Phase 1B is wallet-only — no fiat, no screening, no whitelist, no public route.
- **Independent Opus review of Phase 1B → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High; 1 Medium; 5 Low; 7 Informational), every claim independently reproduced via adversarial empirical probes on genuinely fresh disposable Postgres databases: a deterministic advisory-lock barrier (independent connection holds the lock; the registration request stays unsettled while held; releasing it completes the request); a statement/HTTP-ordered instrumentation log proving the CLT-01 fetch occurs at transaction depth 0, before `BEGIN`, with zero HTTP calls at any non-zero transaction depth; live RLS isolation probes on `foundation.idempotency_record` (RLS enabled and forced; WLT-01 sees/completes only its own rows; another module's rows are invisible and unmodifiable; flipping `source_module` is rejected; an unset `aix.module` context fails closed); a full live privilege matrix (every allowed operation succeeded, every denied operation returned `42501`); an independently written EIP-55 reference cross-checked against all 8 canonical spec vectors; an independently written Base58Check reference cross-checked against two real published TRON mainnet addresses; a full refusal-path inventory exercised against the real database across all eleven refusal scenarios. `npx tsc -b --force` clean; Gate A boundary suites 49/49 across 9/9 files; WLT unit 48/48 across 5 files; WLT integration 64/64; foundation/IAM/IAM-02/SEC-01 idempotency regressions 237/237 across 4 files; CLT-01 status-route regressions 415/415; **first independently fresh canonical suite 2399/2399 across 104/104 files; second independently fresh canonical suite (canonical suite as its first test action) 2399/2399 across 104/104 files** — neither reused an already-tested database; migrations 001→049 clean; all 9 grant files clean; migration 049 down/up/re-up clean; migration head 049; no migration 050. `tests/integration/kyc1-db.test.ts` and `tests/integration/clt1-db.test.ts` were updated only to recognize `049_wlt1_core` specifically as a later unrelated migration (do not accept an arbitrary migration 049 or 050+; CLT/KYC ownership assertions remain intact). No runtime source, test assertion logic, migration, grant, package file, or TypeScript configuration was changed during the review.
- **P1B-MED-1** (mandatory Phase 2 entry condition, not closed this sync): the database chain-coverage registry and the in-process address dispatcher are currently aligned, but no automated test enforces that alignment — the implementation comment claims a protecting test exists; it does not. Current pairs are identical and current runtime behaviour is correct; no present unsupported/unhandled pair exists — non-blocking for Phase 1B, but must close (a real bidirectional sync test between `wlt1.chain_coverage` and the dispatcher's known pairs) before any Phase 2 chain/network coverage or dispatcher change.
- **P1B-LOW-1 through P1B-LOW-5** (all non-blocking): LOW-1 the committed concurrency test is `Promise.all` timing-luck (runtime locking independently proven correct via a real barrier; add a deterministic barrier test). LOW-2 the route-level `23505`→`WLT1_DESTINATION_DUPLICATE` mapping is correct but untested at the route level. LOW-3 TRON `invalid_network_prefix`/`invalid_payload_length` are reachable and correct (proven with concrete witnesses) but the named tests are intercepted earlier by the T-prefix guard. LOW-4 pre-transaction refusals create no idempotency record, so repeated same-key blocked/invalid requests produce repeated refusal evidence/audit rows (no destination is ever created; evaluate bounded deduplication in Phase 2). LOW-5 four redundant unique/explicit index pairs exist (harmless, precedented by KYC-01's migration 042); the exact index inventory test currently covers only two of the four tables. Seven Informational findings (P1B-INF-1..7): masking edge-case revisit needed before shorter address formats; alias classification precision; unrestored `clt1FetchImpl` in some tests; table-wide idempotency UPDATE grant (platform-wide hardening, shared by 4 other roles); shared actor idempotency namespace across upstream callers; relocated CLT-01 migration-head drift guard (coverage-neutral); unused test code. **Test-infrastructure carry-forward** (platform-wide, not WLT-01-specific): the pre-existing `foundation.outbox_event` REVOKE/GRANT catalogue-race pattern (cfg1/aml1/clt1/kyc1/wlt1) — both official parallel fresh-database runs were green; a shared advisory lock around ACL mutation is recommended platform-wide.
- **WLT-01 Phase 2A (chain-coverage/dispatcher control and deterministic hardening) implemented and accepted; P1B-MED-1 closed.** Typed `CHAIN_CANONICALISER_REGISTRY` chain canonicaliser registry (`chain`/`network`/`address_format`/`canonicalisation_version`/`canonicalise()` bound per entry) replaces the prior hard-coded supported-pair branches as the genuine runtime dispatch authority; registry-derived, read-only `CHAIN_DISPATCHER_INVENTORY` (never a second hand-maintained list); pure bidirectional `compareChainCoverageToDispatcher` comparison helper; an authoritative integration test comparing real `wlt1.chain_coverage` (`coverage_status='supported' AND activation_status='active'`) against the real runtime dispatcher inventory in both directions across `chain`/`network`/`address_format`/`canonicalisation_version`, plus four synthetic one-sided/format/version drift meta-tests; the prior misleading sync-test comment corrected. Also delivered: a deterministic `pg_locks`-proven registration advisory-lock barrier test and a two-competitor barrier test (closing P1B-LOW-1); a real route-level natural-key `23505` race test using a competing uncommitted transaction plus a direct negative-coverage test of the duplicate-constraint predicate (closing P1B-LOW-2); independently-verified TRON `invalid_network_prefix`/`invalid_payload_length` direct branch vectors and honestly-renamed `t_prefix_required` tests (closing P1B-LOW-3); a complete index inventory across all four current `wlt1` tables (closing P1B-LOW-5); `config.clt1FetchImpl` restoration hygiene and dead test-code removal; a test-only outbox-ACL advisory-lock helper (`tests/helpers/outbox-acl-lock.ts`) adopted at WLT-01's own two ACL-mutation sites. No Phase 2 screening business functionality, no migration 050, no schema/grant/permission/route change. Baseline moved 2399/2399 (104 files) → **2412/2412 (104/104 files)**; WLT-01 integration tests 64→77 (+13, zero coverage removed).

- **Independent Opus review of Phase 2A → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High/Medium; 1 Low; 6 Informational), every claim independently reproduced on two separately created, genuinely fresh Postgres databases: `npx tsc -b --force` clean; WLT unit 48/48; WLT integration 77/77; import-boundary 49/49 across 9/9 files; CLT-01 regression 415/415; foundation/IAM/IAM-02/SEC-01 idempotency regressions 237/237 across 4 files; **both fresh canonical runs 2412/2412 across 104/104 files**, one with the canonical suite as its literal first test action. Three temporary, fully-reverted negative-control probes (hash-verified byte-identical restoration) produced the decisive evidence: (1) hard-coded dispatch branches inserted ahead of the registry left all 77 tests green, including the test titled "REGISTRY IS LOAD-BEARING" — **P2A-LOW-1**, that specific test's proof is weaker than its title claims, though production dispatch is confirmed registry-driven by source inspection; (2) dropping `tron` from the registry while runtime dispatch stayed hard-coded-unchanged caused the AUTHORITATIVE sync test alone to fail with exactly `coverage_without_dispatcher: tron/mainnet` — independently proving the actual P1B-MED-1 drift control works; (3) disabling `takeRegistrationLock` made both new deterministic lock-barrier tests fail at their poll timeout while the old un-renamed `Promise.all` test still passed, confirming the new tests are genuinely non-vacuous. Six Informational findings (P2A-INF-1..6): duplicate-key Map collapse in the comparison helper, unreachable under the real unique index; module-private registry not runtime-frozen while the derived inventory is; a blocked-INSERT test matcher that could be more tightly scoped; a fetch-stub restoration convention judged safe by isolated-subset reruns; a committed harmless competitor fixture row in the 23505 race test; no VCS in `aix-platform`, so absolute historical byte-equivalence cannot be proven from version control. Unrelated-`23505` negative coverage assessed **ACCEPTABLE NON-BLOCKING LIMITATION** (forcing it end-to-end through the live route was correctly judged unsafe; the duplicate-constraint predicate is instead directly exercised against 7 cases). None of the findings reopen P1B-MED-1 or block Phase 2B. Migration-050 rule recorded: it must not read a runtime environment variable to backfill `wlt1.chain_coverage.provider_id` — use an explicit, reviewed, deterministic provider identifier tied to the accepted stub baseline, decided and frozen before implementation.

- **WLT-01 — partial through accepted Phase 0, accepted Phase 1A import-boundary control remediation, accepted Phase 1B wallet-destination core baseline, and accepted Phase 2A chain-coverage/dispatcher control and deterministic hardening.** P1B-MED-1 is closed.

- **WLT-01 Phase 2B (screening-persistence/provider-foundation baseline) implemented and accepted.** `infra/migrations/050_wlt1_screening.cjs` — `wlt1.chain_coverage.provider_id` added and deterministically backfilled (no runtime environment read; literal `stub-wallet-analytics-v1`, both accepted seed rows bound); `wlt1.wallet_screening_result` (23 columns) and `wlt1.vendor_result_inbox` (14 columns) created (WLT table count 4→6); `destination.status` CHECK deliberately unchanged (`draft`/`revoked` only — Phase 2C's own migration 051 will widen it alongside the route/guard that can reach the new states); deterministic single-pending and provider-result-replay uniqueness controls enforced at the database level, independently proven via real `23505` collisions; evidence-preserving down migration (refuses loudly if either new table holds a row — independently proven for both tables separately). Provider abstraction (`services/wlt1/src/lib/providers/{types,registry,stub-provider}.ts`): `WalletAnalyticsProvider` interface (chain/network/canonicalAddress input only); deny-by-default, runtime-frozen registry containing only the deterministic stub; `WLT1_SCREENING_PROVIDER` config (defaults to the stub) with a production+stub boot refusal traced to have no bypass; a 5000ms module-constant provider-call timeout, never an env var, mapping to `unavailable`, never `clear`. Runtime grants extended least-privilege on both new tables; `chain_coverage` remains SELECT-only including `provider_id`. No screening route, no receipt route, no lifecycle logic, no IAM-02 permission, no audit event, no business error, no real vendor. Baseline moved 2412/2412 (104 files) → **2453/2453 (105/105 files)**; WLT integration 77→89 (+12).

- **Independent Opus review of Phase 2B → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High; 1 Medium; 1 Low; 6 Informational), every claim independently reproduced on two separately created, genuinely fresh Postgres databases: `npx tsc -b --force` clean; WLT unit 76/76 across the targeted five-file Phase 2B unit set; WLT integration 89/89; import-boundary 49/49 across 9/9 files; **both fresh canonical runs 2453/2453 across 105/105 files**, one with the canonical suite as its literal first test action. Migration atomicity independently proven by injecting a temporary failure after `ADD COLUMN`+backfill — the whole migration rolled back cleanly (head stayed 049, `provider_id` absent, 4 tables). Down-evidence safety independently proven for both new tables separately. Two bookkeeping corrections applied: table column counts are **23**/**14** (not the report's 20/13 — schemas themselves correct) and the platform grant-file count is **9** (not 10 — Phase 2B modified the existing WLT grant file; no test depended on the wrong count). **P2B-MED-1** (mandatory Phase 2C entry condition, not closed this sync): the deterministic stub's seven scenario fixtures are symbolic labels, not valid canonical addresses — independently run through the real `canonicaliseAddress`, all seven are rejected on both supported chains, so a future screening route could never organically reach `clear`/`review_required`/`high_risk`/`hit` through the stub; non-blocking for Phase 2B (no screening route exists yet), must close before Phase 2C's screening route lands. **P2B-LOW-1** (non-blocking): migration 050's comment claims `risk_status` "transitions exactly once to a terminal value," but this is not database-enforced — independently proven (`pending→clear→hit→clear→pending` all succeeded under the runtime role); Phase 2C must implement the one-way guard at the application/transaction level. Six Informational findings (P2B-INF-1..6): the two bookkeeping corrections above; `direct_exposure`/`indirect_exposure` remain unnormalized `jsonb` pending a Phase 2C shape; grants must be re-applied after down/re-up (expected platform convention); `wallet_screening_result` intentionally has no `UNIQUE(provider_id, provider_result_id)` (a legitimate rescreen may reuse a provider-derived id; replay uniqueness correctly lives on the inbox instead); one review attempt reproduced the known warm-database bootstrap signature and was excluded from acceptance evidence. **Migration regression-test gap (non-blocking):** up/down/re-up and evidence-row down-refusal are independently verified against real databases but not yet automated as committed tests.

- **WLT-01 — partial through accepted Phase 0, accepted Phase 1A import-boundary control remediation, accepted Phase 1B wallet-destination core baseline, accepted Phase 2A chain-coverage/dispatcher control and deterministic hardening, and accepted Phase 2B screening-persistence/provider-foundation baseline.** P1B-MED-1 is closed.

- **WLT-01 Phase 2C-A (canonical-fixture/migration-regression/normalized-type baseline) implemented and accepted; P2B-MED-1 closed.** The Phase 2B stub's seven symbolic scenario lookup values were replaced by genuine synthetic-but-canonicalisation-valid Ethereum EIP-55 and TRON Base58Check addresses (exact accepted matrix: CLEAR `0x56445b274e67797c7a0C151919085239322a31c5`, REVIEW_REQUIRED `0x56445B274E67797c6b16101919085239322A31d4`, HIGH_RISK `TG1mwc48txnCWUGN3JK2ZxR54RpAHcuMeM`, HIT `TG1mwc48txnCVqsHLDw1ZZGg3s3hMuJTTt`, UNAVAILABLE `0x56445b274E67797C6c0E061919085239322a31D3`, INVALID_RESPONSE-MALFORMED `TG1mwc48txnE7hj2sDDzdCgtQsmHY4MyDS`, INVALID_RESPONSE-UNMAPPED-CATEGORY `0x56445b274E67797C6C0e0a1919085239322A31D3` — all synthetic/test-only, never customer addresses). Committed automated migration-050 regression coverage added (`tests/integration/wlt1-migration-050-regression.test.ts`, disposable database via the real `node-pg-migrate` runner) proving 049→050, both evidence-preserving down-refusals independently, clean down/re-up, and provider-binding restoration — closing the previously carried-forward automated-test gap. Normalized exposure/screening-result type freeze delivered: `NormalizedExposureEntry {category: RiskCategory}`, bounded to 16 entries, category-only, `[]` on a constructed result (never `null`); sanctions cross-field consistency rule invoked live during every stub construction. No migration 051, no route, no lifecycle mutation, no business error, no audit event, no IAM-02 permission. Baseline moved 2453/2453 (105 files) → **2506/2506 (106/106 files)**; provider unit tests 20→67 (+47), new migration-regression file (+6) = **+53 net**.

- **Independent Opus review of Phase 2C-A → ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High; 1 Medium; 1 Low; 4 Informational), every claim independently reproduced on two separately created, genuinely fresh Postgres databases: `npx tsc -b --force` clean; provider unit 67/67; targeted WLT unit 123/123 across 5 files; WLT integration 89/89; import-boundary 49/49 across 9/9 files; CLT-01 regression 415/415; idempotency regressions 237/237 across 4 files; **both fresh canonical runs 2506/2506 across 106/106 files**, one with the canonical suite as its first test action. All seven fixtures independently re-verified against the real `canonicaliseAddress` (genuine EIP-55/Base58Check, idempotent, mutually unique, 4 Ethereum/3 TRON) and organically reachable end-to-end. Migration 050 confirmed byte-identical to the accepted Phase 2B artifact (hash `21f0ac852e340961f707d40b1a678718`). The migration-regression test's cleanup was proven to run even on a forced failure (temporary, fully-reverted probe). **P2CA-MED-1** (mandatory before any result-persistence path, not blocking Phase 2C-A): `NormalizedScreeningResult` carries non-nullable `sourceAuthenticated`/`payloadHash`, and the synchronous stub returns `sourceAuthenticated: true` — conflicting with the approved semantic (`source_authenticated = NULL` for a synchronous in-process result); both fields must move to the application/receipt envelope before Phase 2C-B's result-application service can persist anything. **P2CA-LOW-1** (non-blocking): the stub selects outcomes by `canonicalAddress` alone, not `(chain, network, canonicalAddress)`; harmless today because real canonicalisation is chain-specific, but should close before/with the Phase 2C-C `/screen` route. Four Informational findings (P2CA-INF-1..4): prototype/Symbol-keyed exposure input cannot smuggle prohibited fields (the normalizer emits a fresh literal, never passes input through); the migration-regression A–H steps are intentionally order-dependent within one file; the `count:49` baseline is self-guarding; the stub's `payloadHash` values are placeholders, subsumed by P2CA-MED-1.

- **WLT-01 — partial through accepted Phase 0, accepted Phase 1A import-boundary control remediation, accepted Phase 1B wallet-destination core baseline, accepted Phase 2A chain-coverage/dispatcher control and deterministic hardening, accepted Phase 2B screening-persistence/provider-foundation baseline, and accepted Phase 2C-A canonical-fixture/migration-regression/normalized-type baseline.** P1B-MED-1 and P2B-MED-1 are both closed; the migration-050 automated-regression coverage gap is closed. WLT-01 Phase 2C-B planning/implementation-plan confirmation is authorized after this status synchronization; **Phase 2C-B implementation has not started and is not yet authorized**. **P2CA-MED-1 must be resolved before any screening-result persistence path is implemented.** Mandatory pre-2C-B-implementation freezes: the P2CA-MED-1 envelope refactor; the exact business-error inventory (open — current 10, not yet frozen, do not assume any total); the validity ceiling; the receipt-token design; migration 051's exact design; the one-way result-application transaction design (P2B-LOW-1, still open). Phase 2C-C carry-forward: close P2CA-LOW-1 before or with the `/screen` route. Carry-forwards into Phase 2C-B: P2CA-MED-1, P2CA-LOW-1, P2CA-INF-1..4, P2B-LOW-1, migration 051, receipt-token config, validity config, `applyNormalizedScreeningResult`, screening audit inventory, screening business-error inventory, P2B-INF-1..6, P2A-LOW-1, P2A-INF-1..6, P1B-LOW-4 (refusal-idempotency behaviour retained by explicit architecture decision), the remaining CFG-01/AML-01/CLT-01/KYC-01 outbox-ACL sites not yet migrated to the shared test lock helper, Phase 1A's LOW-2 (SEC-01 private regex scanner migration), full-sibling-set widening, dedicated FND-01 boundary suite, same-module package-alias policy, fiat payout tokenisation seam, proof-of-control deferred to Phase 3, own-name/KYC-verified-identity binding blocked; **AML-01 Phase 3E** required before **WLT-01 Phase 4A**; WLT Phase 4 remains split into 4A/4B; **LED-01 remains blocked until WLT-01 Phase 4B acceptance**; real vendor onboarding deferred until after WLT-01 Phase 6 — Phase 2C-A remains deterministic stub-only, no vendor URL/SDK/API key/live request/RPC.

- **WLT-01 Phase 2C-B (screening-lifecycle/result-application baseline) implemented and accepted; P2CA-MED-1 and P2B-LOW-1 both closed.** `infra/migrations/051_wlt1_screening_lifecycle.cjs` widens `wlt1.destination.status`'s CHECK from the Phase 1B two-state set (`draft`/`revoked`) to the accepted four-state Phase 2C set (`draft`/`pending_screening`/`pending_review`/`revoked`) under an explicit stable name (`chk_wlt1_destination_status`), plus a necessary compatibility widening of the `status` column from `varchar(16)` to `varchar(32)` (`pending_screening` is 17 characters, one over the Phase 1B width — approved as part of this phase's scope, not undeclared drift); baseline verified via `pg_constraint` before touching anything; down migration refuses if any destination is `pending_screening`/`pending_review`, independently proven for both states. `services/wlt1/src/lib/screening-application.ts`'s `applyNormalizedScreeningResult` is the ONE authoritative result-application function — owns its own transaction, advisory lock then `FOR UPDATE` on both destination and screening rows, enforces the one-way `pending → exactly one terminal state` gate (P2B-LOW-1), verifies current/latest screening version and provider binding against the pending row, computes effective validity as `min(providerValidUntil ?? +Infinity, issuedAtUtc + ceilingHours)`, and publishes 1-3 audits in the same transaction as the terminal update. P2CA-MED-1 remediation: `NormalizedScreeningResult` no longer carries `sourceAuthenticated`/`payloadHash` — both moved to an application-owned `ScreeningEvidenceEnvelope` discriminated union; the stub's fake `sha256:stub:*` placeholder is removed entirely. Two new business errors (`WLT1_DESTINATION_INVALID_STATE` 409, `WLT1_VENDOR_RESULT_INVALID` 422 — inventory 10 → 12); three new audits (`wallet_screening_completed`, `wallet_high_risk_detected`, `wallet_sanctions_exposure` — inventory 3 → 6); `WLT1_SCREENING_MAX_VALIDITY_HOURS` (default 720, range 1-8760) and `WLT1_PROVIDER_RECEIPT_TOKEN` (optional, >=32 chars, latent) added to config. No route, no lifecycle initiation, no IAM-02 permission, no grant change. Baseline moved 2506/2506 (106 files) -> **2556/2556 (108/108 files)**; +50 net tests (provider unit +6, config unit +12, new screening-application service tests +25, new migration-051 regression +7).

- **Independent Opus review of Phase 2C-B -> ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High; 2 Medium; 3 Low; 4 Informational) — every claim independently reproduced via adversarial empirical probes against two separately created, genuinely fresh Postgres databases: `npx tsc -b --force` clean; provider unit 73/73; config unit 36/36; screening-application 25/25; migration-051 regression 7/7; targeted WLT unit 141/141 across 5 files; WLT integration 89/89; boundary 49/49 across 9/9 files; CLT-01 regression 415/415; idempotency regressions 237/237 across 4 files; **both fresh canonical runs 2556/2556 across 108/108 files**, one with the canonical suite as its literal first test action. The one-way `pending -> terminal` gate was independently reproduced in all four directions (`clear->hit`, `hit->clear`, `high_risk->clear`, `review_required->hit`, each rejected with state unchanged) — **P2B-LOW-1 closed**. `sourceAuthenticated`/`payloadHash` confirmed absent from the provider result type and the stub's fake hash confirmed fully removed — **P2CA-MED-1 closed**. Audit rollback proven fully atomic under a real `REVOKE INSERT ON foundation.outbox_event`. The updated audit-inventory assertion was independently proven to still be a strong allowlist by injecting an unapproved `wlt1.%` event type and confirming the test fails, then passes again once removed.

- **P2CB-MED-1** (mandatory Phase 2C-C entry gate, not blocking Phase 2C-B — no production caller exists yet): the screening validity ceiling is caller-selectable at the persistence boundary — `applyNormalizedScreeningResult` accepts `screeningMaxValidityHours` as an unvalidated primitive rather than obtaining it from authoritative validated `Wlt1Config`. Independently proven live: passing 8760 while boot config defaults to 720 persists 365 days instead of 30; passing 87600/876000 persists 10/~100 years. Phase 2C-C must obtain the ceiling from validated config through an authoritative composition seam, never as a caller-chosen primitive. **P2CB-MED-2** (mandatory Phase 2C-D entry gate, not blocking Phase 2C-B — no production caller imports the receipt path yet): `provider_receipt` evidence-envelope provenance is caller-forgeable — a plain object literal causes `source_authenticated = TRUE` to persist with zero authentication having occurred, and `payloadHash` is accepted with no format/length/canonical-fingerprint validation. Phase 2C-D must make `provider_receipt` unconstructible outside the authentication boundary and validate `payloadHash` as the canonical 64-character foundation fingerprint before persistence. **CLOSED as of Phase 2C-D2 acceptance** — see the dedicated "WLT-01 — accepted through Phase 2C-D2" bullet below for the full closure chronology, the ratified receipt-provenance threat boundary, and the non-blocking **M-D2R-1** residual it carries forward.

- **L1** (non-blocking, Phase 2C-C): `issuedAtUtc`/`validUntilUtc` parsing accepts timezone-naive/local-time strings, consistent with the existing SEC-01 `Date`-parsing precedent, but WLT's future `/screen` route should require an explicit-offset/UTC timestamp contract at the schema layer. **L2** (non-blocking, Phase 2C-C coverage): the implementation report's claim that a pending-but-superseded screening row is structurally impossible was independently disproven — the guard is correct, only the committed test coverage and stated rationale were incomplete; Phase 2C-C should add a committed superseded-version regression test. **L3** (non-blocking, Phase 2C-C): an audit/outbox write failure inside `applyNormalizedScreeningResult` currently surfaces as a raw PostgreSQL `42501` rather than `WLT1_AUDIT_REQUIRED` — consistent with the accepted Phase 1B library/route split; Phase 2C-C's `/screen` route must apply the same mapping.

- Four Informational findings (independently confirmed, non-blocking): **I1** migration 051's header cites `client_status_ref` as its `varchar(32)` width precedent — `client_status_ref` is actually `varchar(128)`; the width decision itself remains approved. **I2** the screening-application test file's header comment says audits are scoped by `entity_id`; the actual helper correctly scopes via `payload_ref::text LIKE`. **I3** `Number()` accepts non-canonical numeric forms that evaluate exactly to 720 — benign. **I4** `provider_result_id` intentionally carries no index or uniqueness constraint — confirmed correct evidence-not-identity design.

- **WLT-01 — partial through accepted Phase 0, accepted Phase 1A import-boundary control remediation, accepted Phase 1B wallet-destination core baseline, accepted Phase 2A chain-coverage/dispatcher control and deterministic hardening, accepted Phase 2B screening-persistence/provider-foundation baseline, accepted Phase 2C-A canonical-fixture/migration-regression/normalized-type baseline, and accepted Phase 2C-B screening-lifecycle/result-application baseline.** Corrected accepted baseline: **2556/2556 tests across 108/108 files**, two independently fresh acceptance databases; migration head `051_wlt1_screening_lifecycle`; 6 WLT tables; 9 grant files; 4 routes; 12 business errors; 6 audit event types; 0 WLT IAM-02 permissions; `destination.status varchar(32)` — `draft`/`pending_screening`/`pending_review`/`revoked`. P1B-MED-1, P2B-MED-1, the migration-050 automated-regression coverage gap, **P2CA-MED-1**, and **P2B-LOW-1** are all closed. **P2CB-MED-1 blocks Phase 2C-C implementation; P2CB-MED-2 blocks Phase 2C-D implementation.** WLT-01 Phase 2C-C planning is authorized after this status synchronization; **Phase 2C-C implementation has not started and is not yet authorized**, and must resolve P2CB-MED-1, P2CA-LOW-1, L1, L2, and L3 as entry gates before `/screen` may be implemented. Phase 2C-D remains gated on P2CB-MED-2 in addition to its own not-yet-started implementation. WLT-01 remains deterministic stub-only — no real vendor, vendor SDK, vendor URL/API key, RPC, or live wallet-analytics call.

- **WLT-01 Phase 2C-C1 (structural-screening-foundation baseline) implemented and accepted; P2CA-LOW-1, L1, and L2 all closed; P2CB-MED-1's apply-call layer closed.** `infra/migrations/052_wlt1_screening_address_hash_width.cjs` widens `wlt1.wallet_screening_result.address_hash` from `varchar(64)` to `varchar(128)`, matching `wlt1.wallet_destination.address_hash` — discovered empirically: the accepted `computeAddressHash(...)` foundation-fingerprint representation is 71 characters, which Phase 2C-C2 must copy from `wallet_destination` into a screening row, and the pre-052 column could not hold it. `services/wlt1/src/lib/screening-application.ts`'s `createScreeningApplication(config: Wlt1Config)` is now the sole exported entry point; the returned service's `applyNormalizedScreeningResult` accepts no `screeningMaxValidityHours` parameter at all (P2CB-MED-1 apply-call remediation) and enforces the L1 strict RFC-3339 timestamp grammar before `Date` construction. `services/wlt1/src/lib/providers/stub-provider.ts` fixture identity is now the composite `chain:network:canonicalAddress` triplet (P2CA-LOW-1 remediation — the previously-exploitable TRON fixtures paired with `chain="ethereum"` no longer leak); `createStubWalletAnalyticsProvider` replaces the fixed epoch-0 timestamp with an injectable clock built from the real `systemTime` singleton. L2 committed coverage closes the prior report's incorrect "structurally impossible" claim about the superseded-version guard. No route, no inventory change, no grant change. Baseline moved 2556/2556 (108 files) -> **2611/2611 (109/109 files)**; +55 net tests (provider unit +29, screening-application +18, new migration-052 regression +7, wlt1-db +1).

- **Independent Opus review of Phase 2C-C1 -> ACCEPT WITH NON-BLOCKING FINDINGS** (0 Critical/High; 1 Medium; 2 Low; 4 Informational) — every claim independently reproduced on two separately created, genuinely fresh Postgres databases: `npx tsc -b --force` clean; provider unit 102/102; config unit 36/36; screening-application 43/43; migration-052 regression 7/7; WLT integration 90/90; boundary 49/49 across 9/9 files; CLT regression 433/433 (the historical 415 figure covers only `clt1-db.test.ts`; the broader glob also matches `clt1-iam2-guard-real.test.ts` (18 tests) — no CLT coverage changed); **both fresh canonical runs 2611/2611 across 109/109 files**. Real-registration integration proof: a destination was registered through the actual `POST /internal/wlt1/wallet-destinations` route, its genuine 71-character `wallet_destination.address_hash` copied into `wallet_screening_result.address_hash` at head 052 — succeeded byte-for-byte. All seven provider fixtures reproduced on correct triplets and independently proven to fail closed on wrong chain/network. The RFC-3339 grammar was adversarially probed well beyond the three examples; `Z`/`+08:00`/`-08:00` forms of the identical instant confirmed to persist identically. L2's superseded-version rejection independently reproduced with full state preservation.

- **P2CC1-MED-1** (mandatory Phase 2C-C2 entry gate, not blocking Phase 2C-C1 — no production caller exists yet): `createScreeningApplication(config: Wlt1Config)` performs no runtime validation of its own config argument — `Wlt1Config` is a plain structural TypeScript interface, so an ordinary internal caller may construct or spread a valid config and replace `screeningMaxValidityHours` with an unvalidated value (`{...loadWlt1Config(env), screeningMaxValidityHours: 876000}` type-checks with no cast). Independently proven live: a forged 876000/8760000-hour ceiling persisted ~100/~1000-year validity. The apply-call layer is genuinely closed, but full construction-authority closure is conditional on Phase 2C-C2's server composition: `server.ts` must construct the screening-application service ONCE from `app.config` and inject the already-configured service into the route — the route must never receive a `Wlt1Config`, the raw ceiling, or the factory itself.

- **P2CC1-LOW-1** (non-blocking, platform-wide tooling property): the committed `@ts-expect-error` assertion inside `wlt1-screening-application.test.ts` is not load-bearing — `tsconfig.json` references only production `src/**/*.ts`, so `tests/**` is never typechecked by `npx tsc -b --force`. The runtime half of the same test remains valid. **P2CC1-LOW-2** (non-blocking, sub-case of P2CC1-MED-1): `NaN`/`Infinity` forged into a service's config still escape as an unmapped `RangeError`; becomes unreachable once Phase 2C-C2 makes `app.config`-only construction the sole composition site.

- Four Informational findings (independently confirmed, non-blocking): **INF-2** JavaScript `Date` may silently normalize an invalid leap day to the following day rather than reject it — a bounded provider-side parsing quirk, does not reopen L1. **INF-3** `24:00:00` may similarly normalize to next-day midnight. **INF-4** composite fixture-key delimiter collision is structurally absent — `CHAIN_NETWORK_PATTERN` forbids colons and both supported address formats are colon-free.

- **WLT-01 — partial through accepted Phase 0, accepted Phase 1A import-boundary control remediation, accepted Phase 1B wallet-destination core baseline, accepted Phase 2A chain-coverage/dispatcher control and deterministic hardening, accepted Phase 2B screening-persistence/provider-foundation baseline, accepted Phase 2C-A canonical-fixture/migration-regression/normalized-type baseline, accepted Phase 2C-B screening-lifecycle/result-application baseline, and accepted Phase 2C-C1 structural-screening-foundation baseline.** Corrected accepted baseline: **2611/2611 tests across 109/109 files**, two independently fresh acceptance databases; migration head `052_wlt1_screening_address_hash_width`; 6 WLT tables; 9 grant files; 4 routes; 12 business errors; 6 audit event types; 0 WLT IAM-02 permissions; `wallet_screening_result.address_hash varchar(128)`; `destination.status varchar(32)` — `draft`/`pending_screening`/`pending_review`/`revoked`. P1B-MED-1, P2B-MED-1, the migration-050 automated-regression coverage gap, P2CA-MED-1, P2B-LOW-1, **P2CA-LOW-1**, **L1**, and **L2** are all closed. **P2CB-MED-1's apply-call layer is closed**; its residual construction-authority issue is **P2CC1-MED-1**, which **blocks Phase 2C-C2 implementation**. **P2CB-MED-2 blocks Phase 2C-D implementation** (unchanged). WLT-01 Phase 2C-C2 planning is authorized after this status synchronization; **Phase 2C-C2 implementation has not started and is not yet authorized**, and must resolve P2CC1-MED-1 and L3 as entry gates before `/screen` may be implemented. WLT-01 remains deterministic stub-only.

- **WLT-01 Phase 2C-C2 (screening-initiation-route baseline) implemented; P2CC1-MED-1 and L3 both closed at entry.** `POST /internal/wlt1/wallet-destinations/:destination_id/screen` — internal-service-authenticated, `Idempotency-Key`-bound to the target `destination_id`; server-owned validated `ScreeningApplicationService` composition (`server.ts` sole ordinary production construction site); `draft` → `pending_screening` initiation inside TX-1 with `MAX(screening_result_version)+1` allocation and an exact real 71-character `address_hash` copy; server-side chain/network/provider resolution with `chain_coverage.provider_id` as the authoritative identity; provider invocation strictly outside TX-1 (depth 0, only after commit); `wlt1.wallet_screening_requested` audit inside TX-1; terminal application on a `screened` outcome (HTTP 200); HTTP 202 on `unavailable`/`invalid_response`; route-level `WLT1_AUDIT_REQUIRED` mapping (L3) for unexpected TX-1/terminal-application failures. Routes 4→5, audits 6→7.

- **Independent Opus review of Phase 2C-C2 → PHASE 2C-C2 ACCEPTANCE BLOCKED** (0 Critical; 1 High — P2CC2-HIGH-1; 1 Medium — P2CC2-MED-1; 2 Low — P2CC2-LOW-1, P2CC2-LOW-2; 4 Informational), every claim independently reproduced via adversarial empirical probes on genuinely fresh disposable Postgres databases. **P2CC1-MED-1 and L3 independently reconfirmed CLOSED, untouched by this finding set.** **P2CC2-HIGH-1** — the same-key pending-rehydration branch hardcoded `provider_attempt = "unavailable"` for every replay of a still-`pending` screening regardless of actual history; reproduced as factually false on multiple reachable paths (replay after a genuine `invalid_response`; replay while the provider call was still in flight; replay after TX-1 committed but before the provider was ever invoked). **P2CC2-MED-1** — `wlt1-screening-route.test.ts`'s own fixture cleanup performed unscoped, table-wide `DELETE`s colliding with sibling WLT-01 files under Vitest's default cross-file parallelism (16–22 failures per co-scheduled run). **P2CC2-LOW-1** — the fabricated replay disposition carried `reason_code = "screening_still_pending"`, outside the approved provider-reason allowlist. **P2CC2-LOW-2** — the test-only `screeningProviderImpl` seam could override not just the callable implementation but the persisted `provider_id` identity itself.

- **P2CC2-HIGH-1/LOW-1/LOW-2/MED-1 narrow remediation implemented and independently re-verified — all four CLOSED.** HIGH-1/LOW-1: two mutually exclusive 202 response shapes (`ScreenPendingObservedBody` — `provider_attempt` + bounded `reason_code`, ONLY when THIS request directly observed the provider outcome; `ScreenPendingUnknownBody` — both fields structurally absent, every same-key rehydration of a still-pending row) enforced at compile time via two dedicated builder functions as the sole 202 construction sites; `screening_still_pending` removed everywhere, never allowlisted. LOW-2: `resolveWalletAnalyticsProvider(coverage.provider_id)` now runs unconditionally first (never bypassable, even under test injection); an injected seam may replace only the callable `screen`/`adaptorVersion`, never `providerId`; the pending-row INSERT and terminal-application evidence read from the SAME resolved provider object. MED-1: fixture cleanup scoped by an owned `client_id` (subquery-scoped `DELETE`s, FK-safe order), plus a deterministic byte-for-byte foreign-sentinel proof (`toEqual` across all five WLT tables) as the load-bearing isolation oracle — repeated-parallel-run greenness alone was shown to be an unreliable signal once a separate, genuinely pre-existing, platform-wide `foundation.outbox_event` ACL race was independently discovered contaminating it.

- **Acceptance-harness stabilization implemented and independently re-verified — test-only, zero production source changed.** All remaining shared-database `foundation.outbox_event` ACL REVOKE/GRANT mutators (CFG-01 4, CLT-01 8, KYC-01 1, AML-01 1 — 14 total) now serialize behind the pre-existing common `aix.test.outbox_acl` advisory lock (single namespace, no per-module split, ordinary bystanders never required to take the lock). Three previously-unguarded CLT-01 GRANT-restoration sites made exception-safe via `finally`. WLT-01's own 5 outbox-ACL-failure tests relocated onto a dedicated private disposable database (`wlt1-outbox-acl-private.test.ts`) — moved, not duplicated, real `REVOKE` retained, original assertions preserved — because the advisory lock alone cannot protect an ordinary bystander request in a sibling WLT-01 file sharing `role_wlt1_runtime` (independently observed causing a spurious `WLT1_AUDIT_REQUIRED` in a non-mutating test). Migration-050/051/052 regression-test teardown hardened (disposable-database drop gated on actual creation, pool `error` listeners for the expected `DROP DATABASE ... WITH (FORCE)` administrative disconnect).

- **Independent Opus final re-review → ACCEPT WLT-01 PHASE 2C-C2 WITH NON-BLOCKING FINDINGS.** **P2CC2-HIGH-1, P2CC2-MED-1, P2CC2-LOW-1, and P2CC2-LOW-2 are all CLOSED** — independently reproduced via adversarial probes written separately from the implementer's own committed tests (a truthful-replay matrix including a provider-in-flight barrier proving exactly one provider invocation; an injected fake `providerId` proving `chain_coverage.provider_id` remains authoritative; mutation-testing both fixture-isolation sentinels to confirm load-bearingness). **P2CC1-MED-1 and L3 remain CLOSED.** Two independently fresh canonical databases (migrate 001→052, all 9 grant files, canonical suite as first action) both ran 100% green: **112/112 files, 2666/2666 tests each.** One Medium, harness-only, non-blocking finding: **M-1** — `clt1-db.test.ts`'s own Phase-7 assertion performs an unscoped whole-database count against `iam2.role_permission` for `clt1.*` permissions; a concurrent `clt1-iam2-guard-real.test.ts` test (a different file/worker) legitimately inserts exactly such a row for its own test duration (≈33% measured incidence). Independently confirmed structurally unrelated to WLT-01, `foundation.outbox_event`, any C2 runtime path, or the harness-stabilization change itself (reproduced against a database provisioned before the harness gate existed). **M-1 does not reopen or block Phase 2C-C2 acceptance but must close before the next acceptance gate.** One Low, harness-only, non-blocking finding: `wlt1-outbox-acl-private.test.ts` unnecessarily takes the canonical-database advisory lock though its own ACL mutations occur only on its private database (harmless).

- **WLT-01 — accepted through Phase 2C-C2.** Partial through accepted Phase 0, accepted Phase 1A import-boundary control remediation, accepted Phase 1B wallet-destination core baseline, accepted Phase 2A chain-coverage/dispatcher control and deterministic hardening, accepted Phase 2B screening-persistence/provider-foundation baseline, accepted Phase 2C-A canonical-fixture/migration-regression/normalized-type baseline, accepted Phase 2C-B screening-lifecycle/result-application baseline, accepted Phase 2C-C1 structural-screening-foundation baseline, and accepted Phase 2C-C2 screening-initiation-route baseline with accepted narrow replay-truthfulness/provider-identity remediation and accepted acceptance-harness stabilization. **Accepted baseline: 2666/2666 tests across 112/112 files**, two independently fresh acceptance databases; migration head `052_wlt1_screening_address_hash_width`; 6 WLT tables; 9 grant files; **5 routes**; 12 business errors; **7 audit event types**; 0 WLT IAM-02 permissions; 0 WLT `role_permission` rows. **P2CC2-HIGH-1, P2CC2-MED-1, P2CC2-LOW-1, and P2CC2-LOW-2 are all closed. P2CC1-MED-1 and L3 remain closed.** **`/screen` is TEST-REACHABLE, NOT GO-LIVE READY** — Phase 2C-C2 deliberately implements no resume/retry: a destination stranded in `pending_screening` (provider unavailable, invalid provider response, or a process failure after initiation commit) can only be rehydrated by a same-key replay (truthful generic-pending 202, no provider reinvocation) or refused by a new-key attempt (`WLT1_DESTINATION_INVALID_STATE`) — recoverable only by Phase 2C-C3. **WLT-01 Phase 2C-C3 planning is authorized after this status synchronization; Phase 2C-C3 implementation has not started and is not yet authorized.** Frozen Phase 2C-C3 planning scope: resume `pending_screening` on the SAME screening row/version, new-key retry, a durable 30-second retry claim/cooldown, prevention of duplicate provider amplification, deterministic concurrency, crash recovery, `provider_attempt = "throttled"`, and removal of the C2 stranded-pending dead-end — Phase 2C-C3 is a hard go-live prerequisite for `/screen`. **Open test-harness carry-forward M-1 (not a WLT-01 or Phase 2C-C2 defect) must close before the next acceptance gate.** P2CB-MED-2 (blocking — receipt provenance caller-forgeable) still must be resolved before Phase 2C-D implementation; the payload-hash 71-vs-64 width/fingerprint-format decision remains deferred to Phase 2C-D planning; WLT-01 remains deterministic stub-only — no real vendor, vendor SDK, vendor URL/API key, RPC, or live wallet-analytics call.

- **WLT-01 Phase 2C-C3 (durable same-row/version `pending_screening` resume) implemented on the existing route — no `/retry`, no new route.** `POST /internal/wlt1/wallet-destinations/:destination_id/screen` gained a `pending_screening`-resume branch reusing the SAME `screening_result_id`/`screening_result_version` (no INSERT, no version bump, no `destination_status_version` increment, no re-emitted `wlt1.wallet_screening_requested` audit). Durable 30-second retry claim implemented as an atomic CAS against `wallet_screening_result.updated_at_utc` itself (`UPDATE ... WHERE risk_status='pending' AND updated_at_utc <= now() - interval '30 seconds'`) — a deliberate architecture decision to avoid migration 053/a new column, sound only because exactly two production code paths ever UPDATE this table and the other (`screening-application.ts`) only ever transitions a row AWAY from `pending`; guarded by a committed source-scan invariant test. Throttled requests receive a truthful `provider_attempt: "throttled"` / `reason_code: "retry_cooldown_active"` 202 and complete their idempotency key without ever gaining provider-call ownership. Provider-identity drift (`chain_coverage.provider_id` changed since initiation) and adaptor-version drift both fail closed to `WLT1_SERVICE_UNAVAILABLE` before any provider call — the C2 stranded-`pending_screening` dead-end is removed for ordinary `unavailable`/`invalid_response`/crash-interruption cases, but NOT for identity/adaptor drift or a data-integrity failure, which explicitly require a future new-version rescreen workflow. No new migration, grant, audit type, error code, or route. **2687/2687 tests (113/113 files)** at implementation time.
- **First independent Opus review of Phase 2C-C3 → ACCEPT WLT-01 PHASE 2C-C3 WITH NON-BLOCKING FINDINGS, but M-1 REMEDIATION BLOCKED.** Every C3 control (route reuse, C2 draft-path non-regression, idempotency/replay semantics, same-row/version reuse, the 30-second CAS claim with PostgreSQL `now()` as sole time authority, both drift fail-closed rules, destination-data correlation, provider depth-zero, anti-amplification concurrency, crash-recovery windows, multi-retry lifecycle, audit/error/route/grant inventories) was independently reproduced via adversarial empirical probes on genuinely fresh disposable Postgres — two independently fresh canonical runs both landed on identical **113/113 files, 2687/2687 tests, 100% green**. Two non-blocking Medium findings recorded: **M-A** — the C3 `updated_at_utc`-reuse source guard matches only the literal string `"UPDATE wlt1.wallet_screening_result"` and is bypassable by an ordinary multiline/wrapped or search_path-unqualified future UPDATE statement (mutation-tested and confirmed, then reverted byte-exact). **M-B** — CFG-01 carries the identical unscoped-`role_permission`-assertion pattern with a live `cfg1-iam2-guard-real.test.ts` sibling; flagged as a carry-forward, deliberately not fixed (out of scope). Separately, however, **H-1**: the prior M-1 "remediation" had scoped only **1 of 5** structurally identical unscoped `iam2.role_permission` zero-seed assertions inside `clt1-db.test.ts` — independently proven by holding one legitimate `clt1-iam2-guard-real.test.ts`-style foreign fixture open and re-running the file (**4 failed / 412 passed**, the exact race M-1 was chartered to eliminate, still live in the same file). **M-1 could not be closed on this review; C3's acceptance stood independently of M-1's status.**
- **M-1 final closure implemented — all five sites, one shared helper, test-only.** The remaining four raw/unscoped `iam2.role_permission` zero-seed queries in `clt1-db.test.ts` were each replaced with the existing, already-accepted `countUnauthorizedCltRolePermissionBindings()` helper (regex `role_id !~ '^role_clt1_(p[0-9]+_)?it_'`, unchanged, not broadened or narrowed) — all five sites now share the ONE query construction site; zero raw variants remain anywhere in the file. Zero production, WLT, C3, migration, or grant changes.
- **Final independent Opus verification → ACCEPT M-1 REMEDIATION. M-1 CLOSED. H-1 CLOSED.** Independently reproduced the exact H-1 falsification scenario with BOTH namespace variants held open simultaneously (`role_clt1_it_*` and `role_clt1_pN_it_*`) — **416/416, 100% green** (previously 4 failed/412 passed). Ran a further adversarial sensitivity counter-probe not present in the implementer's own report: with the two legitimate fixtures still held open, inserted one genuinely unauthorized `clt1.*` binding OUTSIDE the excluded namespace and re-ran the file — **all five zero-seed sites plus the committed A/B/C proof test failed together**, proving the helper did not become a general "ignore all `clt1.*` bindings" bypass. One independent fresh canonical run: **113/113 files, 2687/2687 tests, 100% green, 0 unhandled errors.** M-A, M-B, L-B (the pre-existing `.replace("postgres@", ...)` runtime-role-swap test-harness fragility, unrelated to M-1/C3), the adaptor-drift operational limitation, and P2CB-MED-2 all deliberately left open/untouched — none are M-1 prerequisites.
- **WLT-01 — accepted through Phase 2C-C3. Current status supersedes the Phase 2C-C2 entry above.** Accepted baseline: **2687/2687 tests across 113/113 files**, two independently fresh acceptance databases for C3 plus one further independent fresh canonical for final M-1 closure, all 100% green, 0 unhandled errors; migration head `052_wlt1_screening_address_hash_width` (no migration 053); 6 WLT tables; 9 grant files (no grant content change); 5 routes; 12 business errors; 7 audit event types; 0 WLT IAM-02 permissions; 0 WLT `role_permission` rows. **M-1 is CLOSED (including its H-1 follow-on).** `/screen` is operationally retry-capable: an ordinary `pending_screening` stranding caused by provider `unavailable`, `invalid_response`, or crash/interruption can be retried with a NEW Idempotency-Key after the fixed 30-second successful-claim window, reusing the SAME screening row/version — but this is NOT universal recoverability: provider-identity drift, provider adaptor-version drift, and destination/configuration/data-integrity failure remain explicit fail-closed conditions that may require a future new-version rescreen workflow. **WLT-01 as a whole remains NOT go-live ready** — Phase 2C-D and Phases 3/4A/4B/5/6 are all still outstanding. **P2CB-MED-2 (receipt provenance caller-forgeable) remains OPEN and is the principal Phase 2C-D architecture entry issue — must be closed as part of that planning.** **Phase 2C-D architecture PLANNING is authorized; Phase 2C-D IMPLEMENTATION is NOT authorized.** Non-blocking carry-forwards into 2C-D planning: **M-A** (the `updated_at_utc` claim-clock source guard must be reconsidered if 2C-D receipt processing adds any new non-terminal mutation path to a pending `wallet_screening_result` row — the guard as written can miss a multiline/unqualified UPDATE), **M-B** (CFG-01's analogous unscoped-assertion pattern, not a WLT-01 defect), **L-B** (test-harness runtime-role URL fragility), and the adaptor-version-drift operational limitation (deliberate evidence-integrity fail-closed rule, not a defect).

- **WLT-01 — accepted through Phase 2C-D2. Current status supersedes the Phase 2C-C3 entry above.** Phase 2C-D0 (M-A/GAP-1/GAP-2 foundations), Phase 2C-D1 (authenticated `POST /internal/wlt1/provider-results/receipt` ingress, per-provider `timingSafeEqual` credential comparison, exact raw-body capture at a frozen 64 KiB limit, server-computed SHA-256 payload hash, `vendor_result_inbox` TX-A persistence — no raw provider payload ever persisted), and Phase 2C-D2 (replay/tamper integrity: durable replay identity `(provider_id, provider_result_id)` + immutable content identity `payload_hash`, `INSERT ... ON CONFLICT DO NOTHING RETURNING` with no `DO UPDATE`, same-hash replay = idempotent exact duplicate, different-hash replay = genuine tamper conflict → `WLT1_RECEIPT_CONFLICT` 409 + `wlt1.provider_receipt_conflict_detected` security audit committed transactionally before the 409 response, audit-persistence failure fails closed to `WLT1_AUDIT_REQUIRED`, deterministic no-sleep concurrency proof for both identical/identical and identical/conflicting replay races) are all now accepted. **P2CB-MED-2 is CLOSED.** Getting there took two independent-review rounds: the **first Phase 2C-D2 candidate was BLOCKED** — the D1-era standalone provenance-mint factory was genuinely removed, but its replacement (`authenticateProviderReceipt(secrets, providerId, token)`) still accepted an arbitrary CALLER-SUPPLIED credential map as a plain call argument; an adversarial foreign-module probe proved any production module could fabricate its own secrets and mint real, brand-matching provenance from it (**H-D2-1**; **M-D1-1** remained open; D2's own replay/tamper mechanics were independently reviewed as sound throughout and were never reopened). The remediation (**Phase 2C-D2R**) bound authentication to server composition: `createWlt1ProviderReceiptAuthenticator(config)` is now the sole construction path, called exactly once in `server.ts`'s `buildApp` (mirroring the already-accepted `createScreeningApplication(config)`/P2CC1-MED-1 pattern), and the returned authenticator's `.authenticate(claimedProviderId, providedToken)` takes no secrets argument at all — `provider-receipt.ts` receives only the already-bound instance via dependency injection. A **second, narrow re-review** confirmed the originally-demonstrated attack is closed, but adversarially proved a residual: since `Wlt1Config` carries no nominal/private branding and `loadWlt1Config` is itself exported, a malicious actor already able to inject and compile arbitrary trusted `services/wlt1/src/**` production source could still hand-build a structurally-valid `Wlt1Config`-shaped object and obtain genuine branded provenance this way — and separately, such an actor could bypass the provenance abstraction entirely by calling `@aix/foundation`'s already-freely-importable `query`/`withTransaction` directly. **The ratified accepted threat boundary**: the receipt-provenance control prevents forging authenticated provider-receipt evidence through the SUPPORTED RUNTIME INGRESS/API PATH, and prevents untrusted REQUEST input from choosing the credential authority/`source_authenticated`/payload hash — it does not, and cannot, defend against an actor who can already inject and execute arbitrary trusted WLT production source; that broader threat is governed by code-review/CI-CD/deployment/production-access controls, not by this type. Under that ratified boundary: **P2CB-MED-2 CLOSED (10/10 criteria), H-D2-1 CLOSED, M-D1-1 CLOSED, M-D2-1 CLOSED** (corrected misleading "unforgeable"/"REAL credential" wording — the source now carries an explicit "HONEST SCOPE" disclosure). Accepted baseline: **2812/2812 tests across 115/115 files**, two independently fresh acceptance databases for the final D2 acceptance, all 100% green, 0 unhandled errors; migration head `052_wlt1_screening_address_hash_width` (no migration 053); 6 WLT tables; 9 grant files (no grant content change); **6 routes**; **13 business errors** (12→13, `WLT1_RECEIPT_CONFLICT`/409 new); **8 audit event types** (7→8, `wlt1.provider_receipt_conflict_detected` new; no `WLT1_RECEIPT_STALE` yet — deliberately deferred to Phase 2C-D3); 0 WLT IAM-02 permissions; 0 WLT `role_permission` rows. **Non-blocking residual carried forward: M-D2R-1** (the receipt-provenance threat-boundary scope above — accepted as non-blocking because closing it requires arbitrary trusted production-code execution, which is outside this control's threat boundary and already permits direct DB-primitive bypass regardless). Also carried forward, all unchanged and non-blocking: **L-D2-1** (D2 concurrency tests use a deterministic holder-row technique rather than racing two full HTTP requests), **L-D1-1** (Unicode-escaped duplicate JSON config keys can evade the `WLT1_PROVIDER_RECEIPT_SECRETS` duplicate-key textual detector — boot-time/operator config only), **I-D1-3** (pre-existing Fastify malformed-JSON 500-vs-400 gap, not worsened), **L-D0-1**/**L-D0-2** (M-A guard regex-statefulness and quoted-SQL-identifier gaps), **M-A**, **M-B**, **L-B**, the adaptor-version-drift bounded limitation, and a pre-existing test-harness-only TypeScript compile gap in `wlt1-screening-application.test.ts` (`tests/**` is outside this repo's own `tsc -b` project graph; unrelated to D2). All C3/D1 controls (same-row/version retry, 30-second CAS, truthful throttling, drift fail-closed rules, config/auth/enumeration-resistance/raw-byte-hash/64 KiB limit/TX-A/raw-payload minimization/logger redaction) remain intact and unchanged. **WLT-01 as a whole remains NOT go-live ready** — Phase 2C-D3 and Phases 3/4A/4B/5/6 remain outstanding. **Phase 2C-D3 is AUTHORIZED (planning/implementation may begin); it is NOT yet implemented and NOT yet accepted.** Likely Phase 2C-D3 scope inputs (not designed or decided here): exact-version receipt adaptor normalization off the pending row's own frozen `provider_adaptor_version`, invalid-normalized-result handling, `ScreeningApplicationService` reuse, `received`→`processed`/`rejected` inbox transitions, stale/superseded receipt handling and the `WLT1_RECEIPT_STALE` decision, exact-duplicate recovery of already-`received` rows, crash recovery after TX-A, the synchronous-result/receipt race, the C3 retry/receipt race, and the terminal state-transition/terminal-audit path.

- **WLT-01 Phase 2C-D3 architecture freeze (Opus) → GO FOR D3 IMPLEMENTATION WITH PREREQUISITES.** Designed the terminal-application pipeline before any code: exact-version receipt normalization off the pending row's own frozen `provider_adaptor_version` (never current/latest), reuse of the SAME shared normalized-result validator the synchronous path uses, permanent stale/invalid classification, reuse of the existing `ScreeningApplicationService` as the sole terminal-application authority, `received`→`processed`/`rejected` inbox lifecycle, five-field terminal-evidence reconciliation for TX-B/TX-C crash recovery, and exact-duplicate `received`-row recovery superseding D2's own "same hash + received → 202" rule. Split into three subgates: **2C-D3A** (normalization/validation/stale classification only), **2C-D3B** (terminal application via the existing service), **2C-D3C** (full deterministic race-hardening matrix). No migration/grant/new lock domain/worker/raw-normalized-result persistence authorized at any subgate.
- **WLT-01 Phase 2C-D3A (exact-version normalization / permanent stale classification, NO terminal application) implemented and accepted with non-blocking findings.** Resolves the EXACT historical `(provider_id, provider_adaptor_version)` adaptor a pending screening was frozen against, normalizes via that adaptor's optional `normalizeReceipt` (pure/local/no I/O), validates via the SAME shared validator the synchronous path uses. Three permanent stale conditions (already-terminal/superseded/adaptor-unavailable) map to a NEW error `WLT1_RECEIPT_STALE` (409, 13→14 errors); invalid normalized result → `WLT1_VENDOR_RESULT_INVALID` (422). A receipt passing every check was deliberately left `received` with a TEMPORARY 503 subgate response — no worker exists, so a 2xx would falsely tell a webhook sender to stop redelivering. **Independent Opus review → ACCEPTED WITH NON-BLOCKING FINDINGS**: **M-D3A-1** (a known exact provider/adaptor with `normalizeReceipt` absent lacked a committed permanent regression test) and **L-D3A-1** (the `received → rejected` conditional UPDATE did not observe whether the transition actually happened) carried forward as mandatory D3B entry obligations.
- **WLT-01 Phase 2C-D3B (terminal application, `received`→`processed` lifecycle, five-field terminal-evidence reconciliation, TX-B/TX-C crash-window closure) implemented and accepted with non-blocking findings.** Replaced D3A's temporary 503 with real terminal application through the EXISTING, unmodified `ScreeningApplicationService` (zero duplicated locking/validation/audit logic in the receipt route). A shared `reconcileOrApplyReceivedReceipt` pipeline serves both a fresh `received` row and a same-hash duplicate replay, performing its OWN fresh unlocked re-read and either reconciling against an EXACT five-field terminal-evidence match (same target `screening_result_id`, `source_authenticated=TRUE`, matching `payload_hash`/`provider_result_id`/`provider_id` — never a terminal-status-only shortcut), classifying newly-discovered permanent stale, or terminally applying via the service. This is how the TX-B (service commits)/TX-C (`received→processed`) crash window closes on replay with zero reapplication and zero second audit. M-D3A-1 closed (permanent test committed); L-D3A-1 closed (one `transitionInboxFromReceived` helper, conditional UPDATE + authoritative zero-row re-read). No migration/grant/route/error/audit added. **Independent Opus review → ACCEPTED WITH NON-BLOCKING FINDINGS**, but with TWO Medium findings opened as MANDATORY Phase 2C-D3C closure items: **M-D3B-1** — a reachable defect where two genuinely concurrent exact-duplicate receipt deliveries (ordinary webhook timeout-and-retry) could race inside the service's own locked gate such that the loser's "stale" result got permanently mapped to `rejected` even though it was in fact this SAME receipt's own successful application winning moments earlier — producing a self-perpetuating false-rejection every future replay would repeat forever, adversarially proven live. **M-D3B-2** — a PRE-EXISTING, unrelated shared-database test-isolation defect (not a D3B production defect): two tests mutated the globally shared `wlt1.chain_coverage` row on the canonical database, causally proven to intermittently fail a concurrently-running bystander file (~50% under default parallelism). Flagged as a mandatory D3C precondition since a deterministic race suite cannot be validated against a flaky oracle. Two non-blocking Lows also carried forward: **L-D3B-1** (some transient paths map to `WLT1_AUDIT_REQUIRED` rather than `WLT1_SERVICE_UNAVAILABLE` — same redelivery semantics, weaker diagnostics) and **L-D3B-2** (the service's own provider-binding check is layered but vacuous on the receipt path — TX-A's own binding check remains authoritative).
- **WLT-01 Phase 2C-D3C (shared-DB isolation closure, exact-duplicate race healing, final five-race arbitration matrix) implemented and accepted with non-blocking findings — full Phase 2C-D now COMPLETE.** Closed both mandatory findings in the mandated order, then implemented the frozen race matrix. **M-D3B-2 closed**: the two mutating tests moved, verbatim, into a new private disposable-database file (`wlt1-screening-route-private.test.ts`, mirroring the already-accepted `wlt1-outbox-acl-private.test.ts` pattern) — no equivalent shared mutation remains reachable; proved stable via 5+ consecutive default-parallel WLT-only runs plus 2 cold default-parallel full canonicals, all clean. **M-D3B-1 closed**: one pure five-field evidence-match helper (`isTerminalEvidenceForInbox`) reused in three places — pre-normalization reconciliation, a NEW post-service stale-race catch (re-checks evidence before converting to a permanent rejection), and a NEW narrow route-level healing check on a same-hash rejected replay — with a single, DB-predicate-bounded exception to "rejected never reopens" (`healRaceProducedStaleRejection`, reachable only after independently-proven evidence match, bounded to the two race-producible reasons only; `invalid_normalized_result`/`adaptor_version_unavailable`/`screening_not_found`/`provider_binding_mismatch`/evidence-mismatched stale all remain permanently terminal; D2's different-hash conflict remains strictly final and unaffected). Closed with a deterministic committed test racing TWO REAL concurrent HTTP receipt requests inside the service's own advisory lock via a NEW precision lock-key-scoped barrier (verified against a live `pg_locks` encoding, replacing an earlier more-ambiguous "any advisory-lock waiter" barrier) — result: one inbox row, `processed`, one terminal audit set, both responses 200, zero stale loser. **Final race matrix** (all deterministic, committed): sync-vs-receipt both directions, C3-claim-vs-receipt both directions (provider call paused via the PRE-EXISTING `screeningProviderImpl` test-injection seam — never a new production backdoor), and two different `provider_result_id` values racing the same pending screening (not a D2 conflict — exactly one winner, matching terminal evidence, the loser rejected stale/409, two inbox rows persist, one terminal audit set). No new lock namespace, no inbox advisory lock, no migration/grant/route/error/audit added; M-A re-verified at exactly two production UPDATE sites; P2CB-MED-2 re-verified closed. **Independent Opus review → ACCEPTED WITH NON-BLOCKING FINDINGS. M-D3B-1 CLOSED. M-D3B-2 CLOSED.** No Critical/High D3C product findings. One High finding recorded, explicitly NOT a D3C product defect: **H-D3C-1** — a PRE-EXISTING, platform-wide test-harness fragility (reproduced identically with the new D3C files absent): under full-repository default-parallel startup load, several unrelated non-WLT service test files can silently skip substantive assertions when `schemaReady` resolves false — those files happen to carry a hard guard that surfaces this as a visible failure, but **no WLT test file has an equivalent guard**, so the SAME underlying condition could silently convert a WLT run into a false green. The reviewer independently proved WLT execution was NOT silently skipped in the accepted evidence (negative-control sentinel injections failing with real observed values, both in isolation and under three cold full-parallel canonical runs, plus a fully substantive cold SEQUENTIAL canonical) — D3C acceptance is NOT blocked — but the full-repository DEFAULT-PARALLEL canonical must not be trusted alone as acceptance evidence until H-D3C-1 is separately remediated (platform-wide, not WLT-01 scoped). One Medium, non-blocking: **M-D3C-1** — the route-level (as opposed to post-service-catch) race-healing path lacks a committed regression test; production behaviour independently proven correct by probe, coverage incomplete. Three Lows carried forward non-blocking: **L-D3C-1** (sync-vs-receipt tests are sequential, not barrier-controlled concurrent), **L-D3C-2** (C3 race infers the CAS claim via provider invocation rather than asserting `updated_at_utc` directly), **L-D3C-3** (TX-C zero-row→rejected branch does not independently attempt healing — currently structurally unreachable).
- **WLT-01 — Phase 2C-D is now COMPLETE / ACCEPTED (D0 + D1 + D2 + D3A + D3B + D3C).** Current status supersedes every "accepted through Phase 2C-D2"/"Phase 2C-D3 authorized, not yet implemented" entry above. **Accepted final baseline: 2862/2862 tests across 116/116 files.** The AUTHORITATIVE acceptance signal is the cold SEQUENTIAL canonical (fresh DB, migrations 001→052, all 9 grants, canonical SEC-01 variables, full suite as the first action, 100% green, 0 unhandled) — **not** the full-repository default-parallel canonical, which remains contaminated by the pre-existing, unrelated H-D3C-1 harness fragility until separately fixed. Migration head `052_wlt1_screening_address_hash_width` (no migration 053); 6 WLT tables; 9 grant files (no grant content change across the entire D3 arc); **6 routes**; **14 business errors** (13→14, `WLT1_RECEIPT_STALE`/409 added in D3A); **8 audit event types** (unchanged since D2 — D3A/D3B/D3C add zero new audit types); 0 WLT IAM-02 permissions; 0 WLT `role_permission` rows. **All findings from D0 through D3C are CLOSED** except explicitly tracked non-blocking carry-forwards: M-D2R-1, L-D2-1, L-D1-1, I-D1-3, L-D0-1, L-D0-2, M-B, L-B, the adaptor-version-drift bounded limitation, the pre-existing `wlt1-screening-application.test.ts` compile-gap carry-forward, L-D3B-1, L-D3B-2, L-D3C-1, L-D3C-2, L-D3C-3, and the two items needing action before the next gate — **H-D3C-1** (test-harness guard fix, platform-wide) and **M-D3C-1** (commit the missing route-level healing regression test). **Provider redelivery remains OPEN / LOAD-BEARING OPERATIONAL INTEGRATION PREREQUISITE** — no worker, no raw/normalized-result persistence anywhere in the accepted architecture; any real vendor's integration contract must guarantee redelivery after non-2xx/transport failure until terminal acknowledgement (or an equivalent guaranteed-delivery mechanism) before onboarding. **WLT-01 as a whole remains NOT COMPLETE and NOT GO-LIVE READY** — Phases 3, 4A, 4B, 5, 6 remain outstanding. **LED-01 remains BLOCKED** — Phase 2C-D's completion does not unblock it; the accepted dependency remains WLT-01 Phase 4B (verify-and-consume). **Next gate: remediate H-D3C-1 (add a hard `schemaReady` acceptance guard to WLT-01's own test files) before relying on any future default-parallel full-repository canonical as acceptance evidence — then an Opus Phase 3 architecture freeze (expected domain: Proof-of-Control / ownership-control) before any Sonnet implementation begins.**

- **H-D3C-1 (WLT false-green test-harness/acceptance-oracle finding) remediated and independently accepted — CLOSED.** Not a WLT product defect: a fail-loud canary (`if (!schemaReady) return expect(schemaReady, "run migrate:up + wlt1_runtime_grants.sql first").toBe(true);`, the established aml1/cfg1/fnd/iam/iam2/sec1 convention) was added to one existing test per readiness flag across the four affected shared-schema WLT files (`wlt1-db.test.ts`, `wlt1-provider-receipt-route.test.ts`, `wlt1-screening-application.test.ts`, `wlt1-screening-route.test.ts` — the last with two independent canaries), plus a new defense-in-depth source guard (`wlt1-schema-ready-fail-loud-guard.test.ts`, 10 tests, mutation-tested). The 5 private-disposable-DB WLT suites were independently confirmed to already fail loudly by construction — left unmodified. Zero production code changed. **New accepted baseline: 2872/2872 tests across 117/117 files** (cold sequential canonical, authoritative signal). A **WLT-scoped** default-parallel run is now trustworthy acceptance evidence; the **full-repository** default-parallel canonical remains unreliable due to a SEPARATE, pre-existing platform startup/setup-reliability issue affecting several non-WLT services (aml1-db, cfg1-db, db.test.ts, iam-db, iam2-db, sec1-db) — independently reproduced causing a genuine stampede that correctly turned every affected WLT file RED rather than false-green, proving the fix. **M-D3C-1 remains OPEN** (route-level race-healing regression test still uncommitted; production behaviour independently proven correct by probe). Phase 2C-D remains **COMPLETE / ACCEPTED** (D0+D1+D2+D3A+D3B+D3C, unchanged, no regression). **Next gate: Opus Phase 3 architecture freeze (Proof-of-Control / ownership-control) — design only; Phase 3 implementation NOT authorized.** **LED-01 remains BLOCKED** until WLT-01 Phase 4B (verify-and-consume).

- **M-D3C-1 (WLT route-level race-healing regression coverage gap) remediated and independently accepted — CLOSED.** Test-only closure: exactly one file changed, `tests/integration/wlt1-provider-receipt-route.test.ts` (+2 tests, no file added, zero production/migration/grant/dependency change). Positive regression: a historical race-produced `rejected`/`screening_not_pending` inbox row carrying THIS receipt's own matching terminal evidence self-heals to `processed`/`NULL` via the route's own `duplicate_exact`/`rejected` branch on a real exact-byte HTTP replay — 200, no re-normalization, no `ScreeningApplicationService` reapplication, zero new terminal audits, screening/destination rows byte-identical before/after. Negative control: the same rejected state with NON-matching terminal evidence never heals (409 `WLT1_RECEIPT_STALE`). Independent Opus review additionally ran a live adversarial probe — revoking the runtime role's `UPDATE` grant on `vendor_result_inbox` — which made the positive test fail closed (503), confirming the accepted runs genuinely executed under the restricted `wlt1_provider_receipt_route_test` role (never `postgres` superuser) and that healing fails safe. **New accepted baseline: 2874/2874 tests across 117/117 files** (+2 over 2872/117; migration head/tables/routes/errors/audits/IAM-02 permissions/role_permission/grant files all unchanged). Provider-redelivery prerequisite remains OPEN/LOAD-BEARING, unchanged. New non-blocking carry-forward **M-REV-1** (test-harness privilege-downgrade trap: a password-bearing `TEST_DATABASE_URL` breaks this repo's role-switch URL-rewrite convention and can silently connect as `postgres` superuser instead of the intended restricted role, letting a privilege-boundary test pass without proving least privilege — reproduced only in a discarded scratch database, NOT present in the final accepted runs, NOT a WLT-01 product defect, does not reopen M-D3C-1, does not block Phase 3A implementation, but SHOULD close before Phase 3A acceptance). Also carried forward, all LOW/non-blocking: **L-MD3C1-1/2/3** (negative-control single-variable coverage, `screening_superseded` route-level variant untested, destination immutability assertion scope). **Phase 2C-D remains COMPLETE / ACCEPTED**, unchanged, no regression — D3 history above is not rewritten by this closure.

- **WLT-01 Phase 3 readiness — architecture FROZEN, dependency APPROVED, Phase 3A AUTHORIZED.** Opus architecture freeze plus a narrow correction-pass addendum are both complete; Ratifications A-E are RATIFIED. Phase 3 = Proof-of-Control only (never ownership/beneficial-ownership/custody); EOA-only, signed-message verification; EVM (`eip191_personal_sign`) ships in 3A, TRON fails closed as `WLT1_POC_UNSUPPORTED` until 3B; migration 053 (`wlt1.proof_of_control`) forecast, not yet created. **`@noble/curves@1.9.7` dependency security gate PASSED / APPROVED** — exact pin, MIT, SLSA provenance, 0 `npm audit` findings, depends only on the already-pinned `@noble/hashes@1.8.0`; non-blocking **M-DEP-1** (frozen typed recovery path required: `secp256k1.Signature.fromBytes(sig, "recovered").recoverPublicKey(digest32).toBytes(false)`, with explicit `r||s||v`→`v||r||s` reordering and explicit low-`s`/range checks — never a `@ts-ignore` workaround). With M-D3C-1 closed, freeze/addendum/ratifications complete, and the dependency gate approved: **WLT-01 PHASE 3A IMPLEMENTATION IS NOW AUTHORIZED (not yet started)**; Phase 3B remains NOT authorized. M-REV-1 should close before Phase 3A acceptance but does not block Phase 3A implementation from starting. **LED-01 remains BLOCKED** until WLT-01 Phase 4B (verify-and-consume). **Next gate: WLT-01 Phase 3A implementation (Sonnet), starting with migration 053 and the EVM-only signed-message Proof-of-Control flow.**

- **WLT-01 Phase 3A-1 (PoC schema/crypto/message foundation) implemented and accepted.** Migration 053 (`wlt1.proof_of_control`), the frozen `@noble/curves@1.9.7` secp256k1 recovery path (`Signature.fromBytes(sig, "recovered").recoverPublicKey(digest32).toBytes(false)`, no `secp256k1.recoverPublicKey()`, no `@ts-ignore`), EIP-191 personal-sign digest construction, canonical PoC message builder, and AIX signature parse/range/low-`s` validation. Independent Opus review → ACCEPTED with finding M-3A1-1 (timestamp/nonce grammar not enforced at the builder boundary — closed in 3A-2).
- **WLT-01 Phase 3A-2 (challenge issuance + read routes) implemented and accepted.** `POST .../proof-of-control/challenges` and `GET .../proof-of-control` — server-generated nonce, PostgreSQL-authoritative `issued_at_utc`/`expires_at_utc`, one-active-challenge invariant, supersession, `Idempotency-Key` on issuance, verified-proof short-circuit. Independent Opus review → ACCEPTED with findings L-3A2-1 (calendar-invalid timestamps passed the grammar regex — closed in 3A-3), L-3A2-2 (superseded-challenge verify behaviour needed defining — closed operationally by 3A-3's verify-state handling), L-3A2-3 (reporting-only: `wlt1-config.test.ts` is 64/64, not 78/78).
- **WLT-01 Phase 3A-3 (EVM signature verification submission lifecycle) implemented and accepted.** `POST .../proof-of-control/verify` — the third and final frozen Phase 3A route. Explicit per-state handling (issued/verified/failed/expired/superseded, no catch-all), expiry-transition-commits-despite-409, message reconstruction + `message_hash` revalidation, destination-snapshot revalidation, EIP-191 verification via the 3A-1 crypto seam (`canonicalizeAixSignature` extracted, no duplicated r/s/v logic), attempt accounting (only cryptographically-evaluated invalid proofs increment), max-attempts terminal `failed` transition, same-signature-verified replay (canonical-hash idempotent, 200, no new audit), different-signature-verified replay (409, first accepted signature authoritative, never reopened), two new audits (`wlt1.proof_of_control_verified`, `wlt1.proof_of_control_failed`) both audit-atomic with forced-failure rollback proofs, four new errors, zero raw-signature persistence. Closed L-3A2-1 (calendar-validity hardening via `Date.parse` + ISO round-trip). Independent Opus review → ACCEPTED, confirming all of the above live plus the G3/D-CFG disposition (Phase 3A introduces no CFG-01 feature evaluation, so D-CFG's trigger never fired — **NOT a Phase 3A blocker**), with M-REV-1 as the sole remaining acceptance blocker.
- **M-REV-1 (test-harness privilege-downgrade trap) remediated and independently accepted — CLOSED.** Not a WLT product defect: a fragile `.replace("postgres@", ...)` restricted-role URL derivation silently failed to rewrite password-bearing `TEST_DATABASE_URL` values, letting a "restricted role" test pool silently remain `postgres` superuser. Repository-wide inventory found 75 active occurrences across 15 files and 8 modules (WLT-01, CLT-01, KYC-01, AML-01, CFG-01, SEC-01, IAM-01/02, FND); all replaced with the safe whole-userinfo regex (`replace(/^postgres:\/\/[^@]+@/, ...)`), verified correct for passwordless, password-bearing, and encoded-password source URLs via a committed pure regression test. Fail-loud `current_user`/`session_user`/`rolsuper`/`rolbypassrls` identity canaries added to the three privilege-sensitive files whose acceptance claims depend on restricted-role identity (`wlt1-db.test.ts`, `wlt1-poc-verify-route.test.ts`, `aml1-iam2-guard-real.test.ts`); AML-01/IAM-02 guard-test semantics otherwise unchanged. Independently re-verified by Opus, including a live probe proving a failed derivation is caught loudly (`current_user='postgres', rolsuper=true`) rather than passing vacuously, and independent `pg_stat_activity` confirmation that the PoC-verify route's real app pool runs under the restricted role. **L-3A3-4 closed** as part of this same remediation. Product diff: ZERO — test-only, no `services/`/`infra/migrations/`/`infra/grants/`/dependency change.
- **WLT-01 — Phase 3A is now COMPLETE / ACCEPTED (3A-1 + 3A-2 + 3A-3 + M-REV-1).** Current status supersedes the Phase 3 readiness entry above. **Final accepted baseline: 3051/3051 canonical full suite (125/125 test files)**; WLT-scoped 762/762 tests (27/27 files). Migration head `053_wlt1_proof_of_control` (no migration 054); WLT tables 7, routes 9, errors 19, audits 11, IAM-02 WLT permissions 0, WLT `role_permission` 0, grant files 9 unchanged (`@noble/curves@1.9.7`, `@noble/hashes@1.8.0`). EVM Proof-of-Control (`eip191_personal_sign`) is live for `ethereum/mainnet`; PoC demonstrates possession/signing authority over the registered address at proof time only — it does NOT establish legal/beneficial ownership, authorized use, custody, exclusive or continued control, source of funds, or source of wealth; no raw signature persisted, no private-key handling, no transaction signing/broadcast. Non-blocking Low carry-forwards: L-3A3-1 (raw signature absent from `WLT1_LOG_REDACT_PATHS`, defence-in-depth only, no actual leak), L-3A3-2 (canonical-message field mapping duplicated across two equivalent sites, both using the same builder), L-3A3-3 (Model-A second-verified-row backstop proven effective but lacks a committed regression test), L-MREV-5 (safe URL regex does not recognise `postgresql://`, unused anywhere in this repository; load-bearing WLT identity canaries fail loudly regardless), L-MREV-6 (identity canaries independently re-derive the restricted URL rather than querying the app pool directly; live-verified equivalent today). Provider redelivery remains OPEN/LOAD-BEARING (no worker, no raw/normalized-result persistence) — separate from Phase 3A product correctness. The full-repository default-parallel canonical run remains unreliable due to a separate, pre-existing, non-WLT platform startup-stampede issue (unrelated to H-D3C-1/M-REV-1); the cold sequential canonical and WLT-scoped default-parallel runs are the trustworthy acceptance oracles. **PHASE 3B: AUTHORIZED / NOT STARTED** (frozen scope: TRON `signMessageV2`/TIP-191-style verification, migration 054 widening only the `verification_scheme` CHECK, independent TronWeb/TronLink interoperability vectors — no whitelist logic, no Phase 4A/4B, no LED). **Phase 4A/4B remain BLOCKED; LED-01 remains BLOCKED until WLT-01 Phase 4B verify-and-consume.** **Next gate: WLT-01 Phase 3B implementation (Sonnet), TRON signature verification.**

- **WLT-01 Phase 3B (TRON `signMessageV2`-compatible Proof-of-Control) implemented and accepted.** Migration 054 widens ONLY the `wlt1.proof_of_control.verification_scheme` CHECK (`eip191_personal_sign` → `eip191_personal_sign, tron_personal_sign`) — no new table, column, index, grant, IAM permission, route, audit event, or error; evidence-preserving down refuses while any `tron_personal_sign` row exists. Adds `tron/mainnet` support to the existing single `resolvePocVerificationScheme` resolver — no second chain-support implementation, no TRON-specific route (still 9 routes total). TRON digest: `keccak_256(UTF8("\x19TRON Signed Message:\n" + UTF-8 byte length) || UTF8(canonicalMessage))` — the dynamic-length `signMessageV2`-compatible construction; the legacy fixed `"...\n32"` prefix is deliberately never implemented anywhere in production code. Reuses the EXACT accepted Phase 3A signature parser (`canonicalizeAixSignature`) and Noble secp256k1 recovery path (via an extracted shared `recoverUncompressedPublicKey` helper) — no second r/s/v validation. TRON address derivation reuses the existing `lib/address/tron.ts` Base58Check encoder (new `deriveTronMainnetAddress`, round-tripped through the existing `canonicaliseTronAddress` — never a second encoder). The verify route dispatches on the PERSISTED `verification_scheme` with no fallback either direction; every other Phase 3A behavior (destination gate, message-hash/snapshot integrity, attempt accounting, max attempts, audits, replay, concurrency) is the SAME shared code TRON now also runs through — no forked state machine. Independent Opus review → **ACCEPTED WITH NON-BLOCKING FINDINGS**: **M-3B-1** (the initial manual TronLink interoperability procedure incorrectly implied "any account" could sign the static committed vector message — the verifier correctly rejects this, since it checks the recovered signer against the challenge's own persisted `canonical_address`; the defect was procedural documentation only, not a cryptographic implementation defect), **L-3B-1** (the committed static TronWeb interop vector is ASCII-only, so does not itself discriminate UTF-8 byte length from JS character length — correctness independently proven via a separate genuinely multi-byte external TronWeb test recovering the identical address), **L-3B-2** (the committed TRON digest constant's provenance comment was initially self-referential to this codebase's own function — independently recomputed via an external dependency's own Keccak implementation and confirmed to match exactly). Independent TronWeb 6.5.0 interoperability validated (temporary external install only, zero TronWeb footprint in `package.json`/`package-lock.json`/any workspace, fully deleted afterward) — TronWeb signed, TronWeb's own `verifyMessageV2` confirmed, AIX independently recovered the identical address, including a live end-to-end HTTP-route proof with a freshly-generated arbitrary TRON account (not the committed key).
- **Real TronLink Chrome-extension wallet interoperability independently confirmed — CLOSES the sole remaining Phase 3B acceptance gate.** Using the real TronLink wallet at `TFuEiLEx1NnNYeJq9VtwZUw6CwaSFvvvTE`: registered as the WLT-01 test destination, screened to `pending_review` via the existing accepted test path, AIX issued a real challenge (`wlt1poc_d246943e-0ec4-430f-b7b2-54fe2c7732c8`) through the existing accepted endpoint, TronLink displayed its own native Message Signature approval UI, the user manually approved, and the resulting signature was submitted through the real `POST .../proof-of-control/verify` route — **HTTP 200, `verified`, `verification_scheme: tron_personal_sign`**. Registered destination address, TronLink-connected address, AIX `verified_address`, and the persisted DB `recovered_address` were all FOUR-WAY IDENTICAL; independently re-derived by re-running the message reconstruction and signature recovery from the persisted snapshot alone (never trusting the HTTP 200 at face value) — message hash, TRON V2 digest, and `signature_hash` all independently recomputed and matched exactly; the recovered address was proven NOT the committed golden test key's address, and an EVM-verifier cross-check on the same raw signature confirmed domain separation. `attempt_count` unchanged (0→0, first-submission success); exactly one `wlt1.proof_of_control_verified` audit and zero `wlt1.proof_of_control_failed` audits for this challenge; database-wide sweep confirmed the raw signature is NOT persisted anywhere (only the SHA-256 `signature_hash`); zero repository changes resulted from the entire manual interoperability exercise (the temporary signer page lived entirely outside both repositories). **M-3B-1 CLOSED** — the corrected procedure (register the real account, issue a challenge bound to that exact account, connect and sign as that same account, verify that same recovered account) required no product-code remediation.
- **WLT-01 — Phase 3B is now COMPLETE / ACCEPTED.** Current status supersedes the Phase 3B-authorized entry above. **Final accepted baseline: 3098/3098 canonical full suite (128/128 test files)**; WLT-scoped 809/809 tests (30/30 files). Migration head `054_wlt1_proof_of_control_tron_scheme` (no migration 055); WLT tables 7, routes 9, errors 19, audits 11, IAM-02 WLT permissions 0, WLT `role_permission` 0, grant files 9 unchanged (`@noble/curves@1.9.7`, `@noble/hashes@1.8.0`, no TronWeb runtime dependency). Accepted PoC schemes: `ethereum/mainnet` → `eip191_personal_sign`, `tron/mainnet` → `tron_personal_sign`; every other chain/network fails closed. Wallet-type applicability unchanged: `unhosted` and `unknown` supported, `hosted` unsupported. Security boundary unchanged and reaffirmed: PoC demonstrates possession/signing authority over the registered address at verification time only — it does NOT establish legal ownership, beneficial ownership, authorized corporate use, custody relationship, exclusive control, continued control, source of funds, or source of wealth; AIX never receives or retains a private key, mnemonic, wallet password, or raw signature; no transaction signing; no transaction broadcasting; no blockchain RPC is required for normal PoC verification. Non-blocking Low carry-forwards (all unchanged from Phase 3A plus the two new Phase 3B ones): L-3A3-1, L-3A3-2, L-3A3-3, L-MREV-5, L-MREV-6, L-3B-1, L-3B-2. **PHASE 4A: AUTHORIZED / NOT STARTED** — frozen scope is evaluate-use + decision token + read-only verify only (explicitly NOT verify-and-consume, compensation/release, ledger posting, settlement, or LED-01); **implementation start is subject to confirming the AML-01 Phase 3E (synchronous stub-backed pre-transaction gate) prerequisite is satisfied** — not yet confirmed as of this entry, and this document does not assert it is complete. **Phase 4B remains BLOCKED; LED-01 remains BLOCKED until WLT-01 Phase 4B verify-and-consume** — completing Phase 3B does NOT unblock LED-01. Provider redelivery remains OPEN/LOAD-BEARING external integration prerequisite (kept separate from PoC product correctness). Platform startup stampede remains a separate, pre-existing platform test-infrastructure issue — the full-repository default-parallel run remains unsuitable as the sole canonical acceptance oracle; the cold sequential canonical and WLT-scoped default-parallel runs remain the trustworthy acceptance oracles. **Next gate: confirm the AML-01 Phase 3E prerequisite, then WLT-01 Phase 4A implementation (Sonnet).**

- **AML-01 — Phase 3E (synchronous pre-transaction AML gate) is now COMPLETE / ACCEPTED — the WLT-01 Phase 4A prerequisite is satisfied.** AML-01 had paused after Phase 3D (no further hardening phase planned at the time). WLT-01 Phase 4A planning then surfaced a load-bearing gap: WLT-01 needed a synchronous, per-transaction AML decision it could consult before authorizing destination use, and no such seam existed — AML-01's prior routes were all asynchronous case/monitoring workflows. Independent Opus review confirmed the gap, froze a Phase 3E architecture (plus a narrow addendum resolving load-bearing ambiguities), and cleared it for implementation. **Exactly one new route**, `POST /internal/aml1/pre-transaction/screen`, machine-to-machine only (`makeAml1InternalIdentityGuard`, no IAM-02 permission). **Evidence-based, not live**: it never calls a screening provider on the request path — it evaluates already-persisted `aml1.screening_request`/`screening_result`/`screening_match`/`screening_provider_attempt` evidence inside one ordinary read-write transaction. Accepts only `authorised_party` subjects (`client_application` rejected at the schema level — AML-01 has no local means to bind it to a `client_id`). **Client/subject binding** is proven positively, not just checked: `screening_request.subject_parent_ref = client_id` is embedded in the same query predicate that selects each subject's newest lifecycle row, so a NULL parent, a foreign-client subject, and a genuinely unknown subject are structurally indistinguishable — all collapse to `review/subject_client_binding_unprovable`, closing a cross-client enumeration oracle by construction rather than by a separate check. **Newest-lifecycle-wins**: `ORDER BY created_at_utc DESC, screening_request_id DESC` selects each subject's evidence — a newer `requested`/`failed` request always masks an older `clear` result, never the other way round. Decision vocabulary `allow`/`review`/`deny` (only `allow` affirmative), exactly 11 frozen reason codes, strictest-decision-wins precedence (`deny > review > allow`). **Provider provenance is load-bearing**: `evidence_provider_ids` is sourced only from `screening_provider_attempt` rows with `status='succeeded'` for the selected evidence — the initial implementation candidate let otherwise-affirmative `clear`/`matches_dismissed` evidence return `allow` even with zero successful provider attempts (**C-3E-1**, Critical, fail-open), caught by independent Opus review together with a companion test that could not have caught it (**H-3E-1**). A surgical 3-file Sonnet remediation gated both affirmative branches on `providerIds.length > 0` (falling to `review/unsupported_evidence_state` otherwise) while leaving `confirmed_hit` — which returns earlier in the evaluator and is never rescued by staleness or absent provenance — structurally unweakened; both closures were independently re-verified, including a deliberate revert-then-restore proving the strengthened tests actually fail without the fix. Two Low findings (newest-lifecycle-over-provenance-complete-evidence coverage gap; a mislabeled auth test) closed test-only. **Final independent Opus re-review (fresh DB, cold sequential, canonical SEC-01 tokens): AML-01 PHASE 3E — COMPLETE/ACCEPTED.** M-3E-1/C-3E-1/H-3E-1/L-3E-1/L-3E-2 all CLOSED. **M-3E-2** (`no_current_evidence` is defensive but effectively production-unreachable under the current atomic screening-write path) carried forward **OPEN/NON-BLOCKING**. Zero new migration (AML migration state unchanged at 041; platform head remains `054_wlt1_proof_of_control_tron_scheme`), zero new grant, zero new IAM-02 permission, zero new AML error code (catalogue confirmed at **26** — corrects a pre-existing documentation drift that had recorded 25). AML routes 21→22; audit event types 9→10 (new `aml1.pre_transaction_evaluated`, one per allow/review/deny, atomic with the decision — a forced audit failure returns 503 `AML1_AUDIT_REQUIRED` with no decision field and full rollback). **Final accepted baseline: platform 3185/3185 tests (130/130 files, fresh DB, cold sequential, `--no-file-parallelism`); AML-01 scoped 480/480 (18 files); Phase 3E focused 75/75 (38 unit + 37 integration).** **WLT-01 Phase 4A implementation start is now CLEARED** — the former blocking prerequisite is closed. **Next gate: WLT-01 Phase 4A implementation (Sonnet).**

- Use **one Claude Code session per module**.
- Use **Sonnet** for coding/testing/refactoring.
- Use **Opus** only for final architecture/security/ledger/compliance review.

(For the full accepted-module list and versions, see [MODULE_STATUS.md](MODULE_STATUS.md) and [INDEX.md](INDEX.md). The "Accepted modules" table above is a legacy snapshot from the review phase.)

## Implementation progress
- **FND-01 implementation accepted** (independent Opus final review; `tsc -b` clean; 44/44 tests passing against fresh PostgreSQL). F1 (idempotency wiring) and F2 (readiness licence-lock placeholder) confirmed closed. F3 (module-boundary / cross-schema isolation) carried into and closed by IAM-01.
- **IAM-01 implementation accepted** after independent Opus final review (verdict: ACCEPT WITH MINOR CONDITIONS) and the **C1 grant tightening**. `tsc -b` clean; **118/118 tests passing** against fresh PostgreSQL; the F3 trio (foreign-schema-denied, missing-client-scope fail-closed, import-boundary) is closed and proven under the least-privilege `role_iam_runtime`, not just superuser.
- **C1 fix (done):** `role_iam_runtime` access to `foundation.outbox_event` narrowed to **INSERT-only** (append-only audit/outbox contract). This required removing the `RETURNING` clause from `@aix/foundation`'s `enqueueOutbox` (Postgres needs SELECT on any RETURNING column) — the row is now built from local values; return signature is byte-identical, all FND-01 + IAM-01 tests re-verified green.
- IAM-01 design baseline: **Argon2id** password hashing; **opaque DB-backed sessions** (instant revocation) — **do NOT use stateless JWT for user sessions**; **MFA enrolment** (enrolment-during-login, AES-256-GCM-encrypted TOTP seed); **password reset** (non-enumerating, hash-only single-use token); **refresh rotation** with reuse→family-revoke; **runtime-role-tested RLS + SECURITY DEFINER** pre-auth token-hash lookup pattern.
- **IAM-02 implementation plan drafted** (`aix-platform/docs/implementation/IAM-02_Implementation_Plan_v1.0.md`) — planning only, no code yet; proposes a phased build (guard + approval-payload-hash/decision-token binding + maker-checker/SoD first, delegation/break-glass/SoD-risk-acceptance/meta-SoD as a tracked follow-up), and flagged C2 as a platform-level blocker to close before IAM-02 lands.
- **C2 closed:** `foundation.idempotency_record` (shared by FND-01 + IAM-01, and about to be used by IAM-02) now has a `source_module` column + `ENABLE/FORCE ROW LEVEL SECURITY` scoped to `current_setting('aix.module', true)` — the same `aix.user_id`/`withUserScope` pattern IAM-01's S1 fix proved, applied per-module (`infra/migrations/005_fnd_idempotency_module_scope.cjs`). `@aix/foundation`'s `beginIdempotent`/`completeIdempotent` now require a `sourceModule` field (a compile-time literal in each module's own code, never derived from request input). No grant was widened or narrowed — RLS is the isolation layer, not the grant. Verified independently on a fresh Postgres: 123/123 tests pass, each module's runtime role can only read/write its own idempotency rows (proven under dedicated non-superuser LOGIN roles, cross-module UPDATE blocked both directions, missing-scope fails closed to zero rows), and C1 (`foundation.outbox_event` INSERT-only) remains intact.
- **IAM-02 Phases 0-5 implemented** (`services/iam2`, migrations 006-007) — new schema `iam2` + `role_iam2_runtime`, permission guard core (11-step precedence chain), approval-to-execution binding (payload-hash + short-lived decision tokens), maker-checker + SoD blocking (meta-SoD rule seeded and actually evaluated at the permission level, not inert), a config-sealed one-time bootstrap-to-RBAC transition, and step-up integration with IAM-01 over HTTP (never imports IAM-01 internals). Built in two independently-verified stages (Sonnet); **196/196 tests passing** against a fresh disposable Postgres, connected as `role_iam2_runtime` via a real LOGIN role from the start. Full details: `aix-platform/docs/implementation/IAM-02_IMPLEMENTATION_NOTES.md`.
  - A real bug was found and fixed during independent verification (not caught by the implementing agent itself): the `reject` approval endpoint's expiry branch threw an error *inside* its own transaction after writing the expiry status, silently rolling back that write. Not a security bypass (expiry is always re-derived from the timestamp), but a data-consistency defect — fixed to match the already-correct pattern used elsewhere in the same file, with a permanent regression test added.
  - Phases 6-7 **deliberately deferred** as a tracked gap-closing pass: delegation, temporary permissions, the steady-state break-glass module (distinct from the one-time bootstrap transition, which IS built), SoD risk-acceptance override, protected-action registry + reconciliation jobs, sensitive-read evidence logging, and a role-revocation endpoint.
- **IAM-02 independent Opus review complete → ACCEPT WITH CONDITIONS, then conditions cleared.** The post-implementation Opus review (`aix-platform/docs/IAM-02_Security_Review_Opus_v0.1.md`) reproduced two High findings empirically against a live DB: **F1** — `execute-verify` did not bind the decision token to the presented actor/action/resource/entity/client (binding rested on the optional payload hash alone; a token minted for one withdrawal was redeemed for a different actor/action/entity), and **F2** — `checkApprovalSodConflict` raced the RLS `set_config` via `Promise.all` on one pooled connection, deterministically fail-open on the SoD control. A scoped Sonnet patch closed both (binding check moved *inside* `verifyAndConsumeDecisionToken` so every caller gets it, revoke-on-mismatch + distinct Critical audit event, null-payload approvals mint no token, real `verified_*` flags; both SoD functions serialized to sequential awaits). Independently re-verified by Opus (`IAM-02_Security_Review_Opus_v0.2_reverify.md`): **211/211 tests on a fresh disposable Postgres**, both original attacks now permanent regression tests through the real app path (F1 test 12 at `iam2-db.test.ts:1039`; F2 meta-SoD at `:1493`). **IAM-02 Phases 0-5 is now an accepted implementation baseline.** Carry-forward L1-L3 (execute-verify freeze/auth-level re-check pending an IAM-01 session-status endpoint; step-up reuse window; interim shared-internal-token trust model) tracked and non-blocking. Phases 6-7 remain deferred as a tracked gap-closing pass.
  - Reuse the `role_iam_runtime`/`role_iam2_runtime`-connected integration pattern as the standard for all future modules (no module's DB tests run solely as superuser) — and reuse the `source_module`-scoped RLS pattern for any further shared infrastructure tables.
- **IAM-02 implementation baseline accepted after Opus re-review.** Summary:
  - **Implemented (Phases 0-5):** scaffold; schema/grants/catalogues; permission guard core; approval-to-execution binding; maker-checker and SoD blocking; step-up integration and cache invalidation.
  - **Deferred (Phases 6-7, tracked gap-closing pass):** delegation; temporary permissions; steady-state break-glass; SoD risk acceptance; protected-action registry; orphan-action reconciliation; sensitive-read evidence logging.
  - **Carry-forward (non-blocking):** L1 execute-verify freeze/auth-level re-check needs an IAM-01 endpoint later; L2 step-up assertion reuse policy for high-value approvals; L3 interim shared internal-service token replacement by a real service-identity model.
- **SEC-01 Phases 0-2 implemented** (`services/sec1`, migration `008_sec1_core.cjs`) — new schema `sec1` + `role_sec1_runtime`; 5 core tables (`audit_event`, `event_schema`, `audit_stream`, `audit_seal_batch`, `source_identity_binding`); audit ingestion API (`POST /internal/sec1/audit-events` + `/batch`) with schema-registry/mandatory-field validation, per-module source-identity binding (hash-only-at-rest bearer tokens per FND-01/IAM-01/IAM-02, `crypto.timingSafeEqual` comparison — never a shared secret, so `source_module` spoofing is structurally blocked), dual-layer idempotency (standard `Idempotency-Key` + business-level `(source_module, event_id)` dedup via FND-01's own documented `INSERT...ON CONFLICT DO NOTHING` + `SELECT` pattern), and a per-stream hash-chain (`SELECT...FOR UPDATE` row-locking for sequence assignment — the same race class as IAM-02's F2, deliberately built race-safe from day one and proven under 8 real concurrent HTTP requests, not simulated). Internal seal stub only (`seal_method='internal'`, external WORM/timestamp anchoring explicitly deferred).
  - **Required a shared `@aix/foundation` change**, resolved via the approved option: `AuditEvent`/`publishAudit` gained a **required** `source_module` field (kept snake_case, consistent with the file's existing convention) plus optional SEC-01-compatible fields (severity, event_category, action, result, session_id, client_id, source_emission_sequence, reason_code). This is a deliberate breaking change — same shape as the C2 patch's required `sourceModule` on `IdempotencyScope` — and broke all 54 existing `publishAudit` call sites across FND-01/IAM-01/IAM-02 at compile time until each was updated with its own hardcoded module literal (never request-derived, so a caller can never spoof another module's identity).
  - **SEC-01 granted a narrow, documented exception**: `role_sec1_runtime` is the ONLY runtime role with `SELECT, UPDATE` (not INSERT, not DELETE) on `foundation.outbox_event` — every other module stays INSERT-only (C1). Independently verified in both directions: SEC-01 genuinely has the exception, and it did not leak to `role_iam_runtime`/`role_iam2_runtime`.
  - **Independently verified by the orchestrating session** (not just the implementing agent's self-report): read the core changed files in full, built a genuinely fresh disposable Postgres, applied migrations 001→008 and all four grant files, ran `tsc -b` (clean) and the full suite once — **275/275 tests passing** (211 baseline FND-01/IAM-01/IAM-02 unregressed + 64 new SEC-01 tests). Directly reproduced the grant-boundary claims via `psql` under each role and confirmed the concurrency test is a genuine `Promise.all` HTTP-level proof, not a simulated one.
  - **Outbox consumer deferred** ("Phase 2b" — direct ingestion API was built instead; the grant for the future consumer is already in place so no further grants-file patch will be needed later).
  - Full detail: `aix-platform/docs/implementation/SEC-01_IMPLEMENTATION_NOTES.md`; plan: `aix-platform/docs/implementation/SEC-01_Implementation_Plan_v1.0.md`.
  - **Phases 0-2 Opus-reviewed → ACCEPT WITH MINOR CONDITIONS** (`aix-platform/docs/SEC-01_Security_Review_Opus_v0.1.md`). One High finding **F1**: `event_category` divergence — the value bound into `event_hash` (caller-supplied) could differ from the stored column (schema default), so a later reconstruction would falsely flag tampering. **F1 closed by a scoped Sonnet patch** (resolve-once: `resolvedEventCategory = input.event_category ?? schema.category` feeds BOTH the hash and the INSERT), with regression tests; independently re-verified.
- **SEC-01 Phase 3 implemented** (`services/sec1`, migration `009_sec1_integrity_verification.cjs`) — internal seal verification (`POST /internal/sec1/seal-batches/:id/verify`, recomputes batch_hash from STORED event hashes, updates only `verification_status` via a column-level UPDATE grant, returns `production_authoritative: false`), integrity verification run (`POST /internal/sec1/integrity/verify-range` + `sec1.integrity_verification_run` table, blueprint §2.11), canonical-format-versioning L1 fix (`canonical_format_version` column: existing rows backfilled v1, new rows v2 binding `classification`/`retention_class` into the hash — versioning by field OMISSION through `canonicalJson`'s undefined-filter, no branching in `computeEventHash`), and an external-anchor interface stub ONLY (`ExternalSealAnchorProvider` — no implementation, no NullAnchorProvider, zero call sites; real WORM/TSA deferred). No scheduler. **316/316 tests**, migrations 001→009 + 4 grant files clean on fresh Postgres, orchestrator-verified incl. psql grant-boundary reproduction.
  - **Phase 3 Opus-reviewed → DO NOT ACCEPT (yet)** (`aix-platform/docs/SEC-01_Phase3_Security_Review_Opus_v0.1.md`). One High finding **P3-F1**, empirically reproduced against the live app: `occurred_at_utc` hash/reconstruction divergence — ingestion hashes the caller's RAW timestamp string (`ingest.ts` `buildCanonicalFields`), but `runIntegrityVerification` reconstructs from the DB-normalized `row.occurred_at_utc.toISOString()` (`integrity.ts`). Any legitimate caller format other than JS-millisecond ISO (e.g. `2026-01-01T00:00:00Z` — the blueprint's own API sample; `+00:00` offsets; microsecond precision) verifies as a FALSE hash-mismatch = false tamper alarm. Same class as the fixed F1, in a different field, masked by all 316 tests because every test uses `new Date().toISOString()`. Seal verification is immune (compares stored hashes, never recomputes). Lows: P3-L1 `Idempotency-Key` on verify-range doesn't actually dedupe (duplicate run rows on retry); P3-L2 partial-range first-row inbound-link blind spot; P3-L3 stale grant-file comment.
  - **P3-F1 gap patch DONE + Opus-re-reviewed → ACCEPTED** (`aix-platform/docs/SEC-01_Phase3_Security_Review_Opus_v0.2_reverify.md`). `occurred_at_utc` is normalized ONCE at ingestion (`new Date(input).toISOString()`, resolve-once discipline identical to the F1 `event_category` patch) and that single value feeds both the hash and the INSERT, so hashed and reconstructed forms agree by construction; unparseable input fails closed with `SEC1_AUDIT_EVENT_INVALID`. P3-L1 (verify-range now honours idempotent replay via `getIntegrityVerificationRunById`) and P3-L3 (stale grant comment) also closed in the same pass. Similar-field review cleared `metadata_redacted` (jsonb) and `source_emission_sequence` (bigint) — neither shares the divergence class. Regression tests added for `Z`-no-millis / `+00:00` / microsecond / occurred_at_utc-tamper / unparseable / verify-range-replay; **325/325 tests** on fresh Postgres, `tsc -b` clean, grant boundaries + append-only re-reproduced under `SET ROLE`. Opus re-review independently probed both directions (post-patch reconstruction matches; pre-patch raw-string hash would NOT have). **P3-L2** (partial-range first-row inbound-link blind spot) remains a tracked non-blocking carry-forward. **SEC-01 Phase 3 is an accepted implementation baseline; Phase 4 is GO.**
- **SEC-01 accepted through Phase 3 — summary:**
  - authoritative audit ingestion baseline
  - source identity binding
  - source_module-scoped idempotency
  - per-stream hash-chain
  - canonical_format_version v1/v2
  - classification and retention_class hash-bound for v2
  - internal seal verification
  - integrity verification run
  - external WORM/TSA interface only, no implementation
  - no scheduler wiring yet
  - no fake external anchor
  - **Carry-forward:** `computeIngestPayloadHash` hashes raw input, so retries must reuse identical payload formatting. L2 metadata redaction remains key-name based; source-side no-secrets discipline remains primary. External WORM/object-lock and trusted timestamp authority remain deferred. Scheduler/cron integrity job remains deferred. SEC-01 Phase 4 should cover read/search, sensitive-read logging, and IAM-02 permission-guard integration.
- **SEC-01 accepted through Phase 4 — summary** (Opus-reviewed → ACCEPT WITH MINOR CONDITIONS; F-1 closed and cleared in an independent Opus re-review; 401/401 tests, migrations 001→011 clean, all 4 grant files clean; no Exchange/business-module code added):
  - authoritative audit ingestion
  - source identity binding
  - source_module-scoped idempotency
  - per-stream hash-chain
  - canonical_format_version v1/v2
  - internal seal verification
  - integrity verification run
  - IAM-02 permission guard integration (HTTP-only, fail-closed on deny/unavailable/non-2xx/malformed)
  - IAM-02 SEC-01 permission catalogue registration (`sec1.audit_event.search`/`.read`/`.read_sensitive`; additive, no IAM-02 guard/grant change)
  - `sensitive_read_log` (append-only; write-before-return, same-tx fail-closed)
  - internal audit read/search routes (`POST /internal/sec1/audit-events/search` + `.../read`; internal-only, public `/sec1/*` deferred to PRT-01)
  - tier-based redaction (normal vs sensitive; hash-chain fields omitted both tiers)
  - fail-closed read posture (baseline IAM-02 deny/unavailable → `SEC1_UNAUTHORISED_AUDIT_READ`; authz-before-existence)
  - full-precision pagination cursor (F-1 fix: Postgres-rendered `to_char(...'US'...)` cursor + `::timestamptz` boundary; no same-millisecond row skipping)
  - **Carry-forward (non-blocking):** F-2 confirm sensitive-read logging policy for normal-tier access to sensitive events; F-3 finer object/client-scoped read authorization; F-4 purpose/reason requirement for sensitive reads; F-5 replace shared internal token + trusted actor_id model before PRT/public exposure; F-6 `search_scope_hash` does not capture exact disclosed event set; P3-L2 partial-range first-row inbound-link blind spot; L2 metadata key-name redaction limitation; `computeIngestPayloadHash` raw-input retry-formatting caveat.
- **SEC-01 accepted through Phase 5 — summary** (two Sonnet self-review passes + Opus-reviewed → ACCEPT WITH MINOR CONDITIONS, both conditions closed same pass; 476/476 tests, migrations 001→013 clean, all 4 grant files clean; no Exchange/business-module code added):
  - threshold/count-window security monitoring rule engine (`security_monitoring_rule`/`monitoring_dead_letter`/`security_alert`/`alert_triage_note`), post-ingest evaluation (after commit, never inside the ingest transaction), fail-open-for-monitoring/fail-closed-for-ingestion, `pg_advisory_xact_lock`-serialized de-dup (concurrency-proven)
  - Phase 3 integrity/seal verification-failure → Critical `security_alert` hooks (closes the `failed → alert_created` state-machine transition), same advisory-lock de-dup pattern
  - alert lifecycle (`open→assigned→triaged→{closed,escalated}`, exactly per the state machine; `SEC1_ALERT_INVALID_TRANSITION` on violation; authz-before-existence; append-only triage notes)
  - Critical-alert-closure approval via IAM-02's **existing** `execute-verify` endpoint (shared `@aix/foundation` `fingerprint()` payload-hash binding to `{alert_id, severity, closure_reason, closure_evidence_ref}` + actor + entity_id + client_id; no IAM-02 code changed; `sec1.security_alert.close` registered with `requires_approval=false`, Critical-only gating enforced at the SEC-01 route layer)
  - IAM-02 catalogue registration (`sec1.security_alert.read`/`.triage`/`.close`; additive, no IAM-02 guard/grant change)
  - dead-letter replay (route-triggered only, IAM-02-gated, retry/escalate, no scheduler)
  - LOW-1/LOW-3 minor-condition patch: dead-letter-write-failure observability (structured warn logging, no sensitive payload/token) + `decision_token` added to log redaction
  - **Carry-forward (non-blocking):** P3-L2, F-2…F-6 (F-5 now more load-bearing after Phase 5's added routes), SoD rule 2 (alert self-close block, deferred/documented), full IAM-02 `sod_rule`-table integration, live IAM-02-backed Critical-closure test (currently proven by shared-`fingerprint()` construction + stubbed fail-closed tests, same precedent Phase 4 established).
- Remaining IAM-01 carry-forward (non-blocking): C3 SECURITY DEFINER ownership hardening, C4 KMS/key rotation for the MFA seed, C5 password-history reuse enforcement, C6 `last_seen` write throttling.

## Cost-control rules
- **One module per session.** Run `/compact` or start fresh after each module.
- Search/list before opening files. Open only INDEX + handover + the specific module being worked.
- Focused diffs only. Do not rewrite whole packs or unrelated blueprint content.
- Keep these handover docs short and current.
- See [CLAUDE_CODE_USAGE_RULES.md](CLAUDE_CODE_USAGE_RULES.md) for model selection.

## What Claude should / should not do
**Should:** work one module at a time; enforce the licence lock in every review; use the standard review format (Critical Gaps → Corrections → Additional Parameters → Consistency Note → Top Priorities); save module reviews into `modules/`, master reviews into `reviews/`; update MODULE_STATUS.md + INDEX.md after acceptance.
**Should not:** introduce any locked exchange feature; scan/upload unrelated files; deep-analyse the whole repo; produce code during architecture reviews; rewrite accepted packs.
